---
id: 20260512_104120_903_m01_t03_per_sub_action_desync_attack_a0
failure_id: 20260512_104120_903_m01_t03_per_sub_action_desync_attack_a0
run_id: 20260512_104120_903
mission: 1
turn: 3
action_index: 0
sim_version_at_diagnosis: 100
status: agent_proposed
confidence: medium
applied_in_commit: null
applied_at: null
retired_in_sim_version: null
duplicates_of: null
target_language: rust
proposed_files:
  - path: rust_solver/src/simulate.rs
    lines: [2635, 2640]
  - path: rust_solver/src/simulate.rs
    lines: [3036, 3039]
rules_matched:
  []
agent_invoked: true
agent_tokens: 0
proposed_fix_sig: "18f9c8d45dad85d9"
fix_snippet:
  before: |
    apply_damage(board, nx as u8, ny as u8, wdef.damage, result, DamageSource::Weapon);
    
    if weapon_id != WId::None {
        board.units[mech_idx].set_active(false);
        board.units[mech_idx].set_boosted(false);
    }
  after: |
    if wdef.smoke() && board.tile(nx as u8, ny as u8).terrain == Terrain::Forest {
        let t = board.tile_mut(nx as u8, ny as u8);
        t.terrain = Terrain::Ground;
        t.set_on_fire(false);
    }
    apply_damage(board, nx as u8, ny as u8, wdef.damage, result, DamageSource::Weapon);
    
    if weapon_id != WId::None {
        let gained = board.units[mech_idx].pilot_chemical() && result.enemies_killed > 0;
        board.units[mech_idx].set_active(false);
        board.units[mech_idx].set_boosted(gained);
    }
diff_signatures:
  - "unit:JetMech:status.boosted:False->True"
  - "tile:(3,3):terrain:forest->ground"
  - "tile:(3,3):fire:True->False"
---
## Symptom

- Action: JetMech, move D4→F5, fire Aerial Bombs at D5
- Mech: JetMech (uid=0)
- Weapon: Aerial Bombs (id=`Brute_Jetmech`)
- Target: D5 (3,4)

Unit diffs:
  - JetMech (uid=0) `status.boosted`: pred=`False` actual=`True`

Tile diffs:
  - E5 (3,3) `terrain`: pred=`'forest'` actual=`'ground'`
  - E5 (3,3) `fire`: pred=`True` actual=`False`

Scalar diffs:
  (none)

## Hypothesis

Two observed effects come from Aerial Bombs killing a 1 HP Burnbug on a forest transit tile while JetMech is piloted by Morgan Lejeune (`Pilot_Chemical`). The simulator currently treats the transit bomb as ordinary weapon damage, so forest damage leaves `Terrain::Forest` with `ON_FIRE`; the engine instead consumes the forest into ground under the smoke bomb with no fire. Separately, Morgan's on-kill Boost is not modeled, so the sim clears Boost at action end and never restores it when the attack kills a Vek.

## Proposed fix

Confidence: **medium**

Suspect files:
  - `rust_solver/src/simulate.rs` lines [2635, 2640]
  - `rust_solver/src/simulate.rs` lines [3036, 3039]

```diff
// BEFORE:
apply_damage(board, nx as u8, ny as u8, wdef.damage, result, DamageSource::Weapon);

if weapon_id != WId::None {
    board.units[mech_idx].set_active(false);
    board.units[mech_idx].set_boosted(false);
}
// AFTER:
if wdef.smoke() && board.tile(nx as u8, ny as u8).terrain == Terrain::Forest {
    let t = board.tile_mut(nx as u8, ny as u8);
    t.terrain = Terrain::Ground;
    t.set_on_fire(false);
}
apply_damage(board, nx as u8, ny as u8, wdef.damage, result, DamageSource::Weapon);

if weapon_id != WId::None {
    let gained = board.units[mech_idx].pilot_chemical() && result.enemies_killed > 0;
    board.units[mech_idx].set_active(false);
    board.units[mech_idx].set_boosted(gained);
}
```

## Verification plan

- `bash scripts/regression.sh` (Rust + Python corpus)
- Add a Rust regression with `Pilot_Chemical` JetMech using `Brute_Jetmech` over a 1 HP enemy on forest; assert transit tile becomes ground with no fire and JetMech is boosted after the attack.
- Wire `Pilot_Chemical` into `PilotFlags`/`pilot_flags_from_id`, then run the targeted Rust test and the project regression script.
- `python3 game_loop.py replay 20260512_104120_903 3` to confirm the fixed simulator reproduces the actual state

## Notes

If this proposal is wrong, run `python3 game_loop.py reject_diagnosis 20260512_104120_903_m01_t03_per_sub_action_desync_attack_a0 --reason "..."` to suppress it from future diagnose runs (diagnoses/rejections.jsonl).

Generated by agent fallback at 2026-05-12T17:07:59.867831+00:00.
