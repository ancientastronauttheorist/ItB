# Lightning War Goal

## Objective

Earn the Lightning War achievement with the Blitzkrieg squad.

## Current Checkpoint

- Latest speed ladder attempted three fresh precombat branches:
  `recordings/20260621_224439_478`, `recordings/20260621_224949_737`, and
  `recordings/20260621_225418_964`.
- No mission was started in these branches. All progress stopped at route proof:
  Airstrike was safely vetoed once, then an OCR conflict on a Tides preview plus
  `Protect the Emergency Batteries` blocked two route starts.
- The third branch is currently paused behind a real macOS private-window /
  screen-audio access prompt. Do not run another live/screenshot/UI command
  until the user authorizes or dismisses it.
- Latest visible timer proof is
  `run_notes/lightning_war_smoke_2026-06-21/screenshots/106_speed_attempt3_privacy_prompt_pause_0135.png`,
  showing `0:01:35` and the macOS prompt.
- Stronger terminal pre-prompt stop proofs are preserved as screenshots `107`
  (`0:01:39`) and `108` (`0:01:49`). Prefer those raw screenshots over the
  earlier telemetry clock fields for attempt 1 and attempt 2 pace.

## Definition Of Done

- Complete the first two Corporate Islands before the in-game timer reaches
  `0:30:00`.
- Keep each Start-to-Start mission segment, including route/deploy/combat/
  reward/shop/panels, at or under `0:03:00` by visible timer proof.
- Use Blitzkrieg on Easy with Base Content unless fresh setup proof says the
  active target intentionally differs.
- After the second island HQ/reward proof, sync achievements and confirm
  Lightning War is unlocked.

## Pace Gates

- Each ordinary mission segment should stay under `0:03:00` in-game time.
- First island should finish under `0:15:00`.
- Timer truth is screenshot/OCR only. Preserve raw screenshots whenever parsed
  OCR and visible timer disagree.

## Constraints

- One Into the Breach run is active at a time.
- Live execution is allowed through the documented Lightning runner/UI helpers,
  one session-touching `game_loop.py` command at a time.
- Do not click Start Mission, Confirm Deployment, combat commands, or End Turn
  from stale or ambiguous proof.
- External prompts are stop signs requiring user action.
