# Agent Solver Reference

This file carries architecture, build/test discipline, core game mechanics, and the simulator/bridge regression casebook. When editing Rust, bridge Lua, parser/model code, or weapon semantics, read the relevant sections here first.

See also `docs/reference.md`, `docs/lua_bridge_architecture.md`, `docs/env_hazards_by_island.md`, and `data/ref_game_mechanics.md`.

## Architecture

**Five layers:**

- **Layer 0 — Game loop:** `game_loop.py` CLI + `src/loop/` (session, logger, commands). The agent is the control loop; Python commands are stateless.
- **Layer 1 — State extraction:** `src/bridge/` (primary) and `src/capture/save_parser.py` (fallback). The bridge delivers richer data: targeted tiles, per-enemy attack data, environment hazards with kill flag, deployment zones, objective buildings, attack order.
- **Layer 2 — Game state:** `src/model/` (Board, Unit, WeaponDef). Single source of truth for solver.
- **Layer 3 — Solver:** `rust_solver/` (the only solver AND the only simulator, pyo3 extension `itb_solver` with entry points `solve` / `solve_top_k` / `solve_beam` / `score_plan` / `project_plan` / `replay_solution`). Search is constraint-based threat response + bounded search. `EvalWeights` tune scoring without changing search logic. On the Python side: `src/solver/solver.py` is now a 121-line wrapper holding `MechAction` / `Solution` dataclasses and `replay_solution()` (a thin shim around `itb_solver.replay_solution` that adds Python's `evaluate_breakdown` for audit-only `score_breakdown`). `src/solver/evaluate.py` owns weight/threat/psion machinery for that breakdown — never touched by `solve()`. **`src/solver/simulate.py` is gone** (deleted in the simulate.py-removal PR series); every diagnose-loop fix targets `rust_solver/src/*.rs`, validator + agent prompt enforce `target_language=rust`.
- **Layer 4 — Strategist:** `src/strategy/` picks `EvalWeights` per achievement target; manages squad/island/shop choices.

**Rebuild the solver** after editing any `rust_solver/src/*.rs`:
```bash
cd rust_solver && maturin build --release && \
  pip3 install --user --force-reinstall target/wheels/itb_solver-0.1.0-cp39-cp39-macosx_11_0_arm64.whl
```
If `maturin` is not on `PATH`, use `python3 -m maturin build --release`
from `rust_solver/` with the same `pip3 install` command.

If `cargo test` hits a rustc incremental-compilation ICE, rerun with
`CARGO_INCREMENTAL=0` before treating it as a code/test failure. The regression
script already sets this guard.

Focused Rust unit tests in this PyO3 crate must use `cargo test --no-default-features`
so they link against Python instead of the extension-module defaults; otherwise
macOS reports unresolved `_Py*` symbols before any simulator assertion runs.

If `cargo test` or `maturin build --release` appears idle while Rust is reading
generated Cargo fingerprint files under `rust_solver/target/{debug,release}/.fingerprint`,
stop the stuck build, remove only that generated `.fingerprint` directory for the
affected profile, and rerun the same command. This is build-cache cleanup, not a
source revert. On macOS pyo3 test binaries may also need:
`DYLD_FRAMEWORK_PATH="/Applications/Xcode.app/Contents/Developer/Library/Frameworks:/Library/Developer/CommandLineTools/Library/Frameworks"`.

## Core Game Rules (Solver-Critical)

The solver enforces these; use them when reviewing solver output or writing tests.

**Terrain kills:** Water and Chasm kill non-flying ground units. Lava kills like water but sets flying units on Fire. Pushing enemies into these is a primary kill method.

**Push:** 1 tile in a direction. If blocked by unit/mountain/edge, the pushed unit takes 1 bump damage instead. If blocked by a building, *both* pushed unit and building take 1 bump. Chain pushing (A pushed into B) doesn't move B — both take bump. Push/bump damage ignores Armor and ACID.

**Damage types:**
- Weapon damage: −1 from Armor, ×2 from ACID
- Push / bump / fire / blocking damage: ignores Armor and ACID
- Blocking damage fires when a Vek tries to emerge on an occupied tile

**Status effects:**
- **Fire:** 1 dmg/turn start. Removed by repair, water, or freezing.
- **Frozen:** invincible + immobilized. Any damage unfreezes (0 dmg dealt).
- **ACID:** doubles weapon damage. Persists until unit dies.
- **Smoke:** prevents attack AND repair. Cancels Vek attacks that are already
  on smoke when the queued enemy attack loop begins; smoke created by an
  earlier enemy attack in the same loop does not retroactively cancel later
  queued attacks.
- **Shield:** blocks one instance of damage + negative effects. Removed by direct damage.
- **Armor:** −1 weapon damage (floor 0). No effect on push/fire/bump.
- **Webbed:** can't move, can still attack. Breaks only when the unit actually changes tiles, or when the webber moves/dies. A blocked push/bump leaves the unit webbed. If multiple live queued web sources target the same unit, killing/pushing one source transfers ownership to another source rather than freeing the unit.

**Terrain HP:**
- **Mountain:** 2 HP (2=full, 1=damaged, 0=rubble/walkable). Any weapon damage reduces by 1.
- **Building:** 1 HP (objective buildings 2). Any damage destroys all on the tile. Reduces grid power by old HP.
- **Ice:** intact → cracked → water (non-flying drowns).

**Repair platforms:** Mission_Repair places `Item_Repair_Mine` tiles. Any live
unit that lands on one triggers the item, heals up to max HP, consumes the
platform, and increments `repair_platforms_used` toward the 3-platform objective.
If the unit is already at or above max HP when it lands, the platform still
consumes and counts but does not add HP. Raw progress can exceed the objective
target (for example `4/3`); clamp only in UI scoring/presentation. It is not the mech
Repair action; do not assume it clears Fire/ACID/Frozen unless a capture proves
the engine does so.

**Repair:** any mech can repair instead of attacking. +1 HP, clears Fire and ACID. Can't repair on smoke.

Extended rules: `data/ref_game_mechanics.md`.

## Detailed Solver And Simulator Rules

1. Always verify state after each mech action (the bridge does this in `auto_turn` automatically). Save file only updates at turn boundaries; bridge updates per sub-action.

6. Priority order: buildings > threats > kills > spawns.

8. Never move onto ACID tiles voluntarily — doubles damage, disables armor.

9. **Self-improvement:** every process error triggers an immediate AGENTS.md update with a guard or fix. Every mistake makes the process permanently better.

12. Use all mech actions every turn — even suboptimal moves beat skipping.

13. The solver handles environment hazards via `environment_danger_v2`: each entry is `[x, y, damage, kill_int]`. `kill_int=1` = lethal (Air Strike, Lightning, Cataclysm→chasm, Seismic→chasm, Tidal→water; bypasses shield/frozen/armor/ACID for grounded units). `kill_int=0` = non-lethal (Wind/Sand/Snow). Env ticks normally fire BEFORE Vek attacks in enemy phase. `Mission_Tides` is the exception: queued Vek attacks land before the wave advances, and flying units on the wave tile take 1 damage instead of being fully spared.

14. On crash/timeout recovery, always start with `cmd_read` + `cmd_solve`. Never resume a previous solution — the board may have changed.

15. Save file updates only at turn boundaries. Bridge does not have this limit — `verify_action` reads fresh bridge state.

29. **Final-island save phase anomaly.** During `Mission_Final` and `Mission_Final_Cave`, `saveData.lua` can report `mission_ending` while the Lua bridge and visible screen are still in live combat. If `cmd_read` / `auto_turn` says `mission_ending` but the screen shows active final combat, trust the fresh bridge when `mission_id` is `Mission_Final` or `Mission_Final_Cave` and at least one mech is active. Conversely, if `auto_turn` waits out with `active_mechs=0`, it now returns `status="TERMINAL_OR_MISSION_END"` and clears the stale active solution; check the visible screen for Victory / Region Secured / rewards / defeat before issuing more combat commands.

34. **Projectile-grapple attacks are line attacks, not melee.** Burnbug/Gastropod proboscis weapons (`BurnbugAtk1`, `BurnbugAtk2`, `BurnbugAtkB`, `GastropodAtk1`, `GastropodAtk2`) trace from the attacker's current tile along the original queued direction until the first projectile blocker, then damage that blocker and grapple: hit pawns are pulled toward the attacker, objects pull the attacker toward the object. A vacated first target tile does NOT cancel the shot. If an unexpected grid loss follows a moved Burnbug/Gastropod, first check for a building farther down that line. Regression anchor: Normal run `20260504_210332_088`, mission 1 turn 1, F7 `(1,2)` building loss fixed in simulator v44.

35. **Trust engine terrain ids over stale terrain names.** Shipped map files and Lua constants have terrain id `5` = Ice, not Lava. Older bridge builds mislabeled id 5 as `"lava"`, causing false terrain-death predictions on Pinnacle ice and the Coal Plant loss chain in Normal run `20260504_210332_088`, mission 1 turn 2. Parser code must prefer `terrain_id` normalization when present; bridge Lua must derive `TERRAIN_NAMES` from engine globals (`TERRAIN_ICE`, `TERRAIN_LAVA`, etc.) instead of hand-maintaining numeric tables.

46. **Detritus Contraption barrages must target a non-source unit.** `Missiles_Shield` / `Missiles_OneDmg` inherit `Support_Missiles:GetTargetArea`, which excludes the firing Contraption tile even though the effect is global. Clicking the Contraption's own tile can spend the action without emitting any missiles. The solver must pick a live non-source unit as the representative target, and the simulator must exclude the source from Shield/Missile Barrage effects. Regression anchor: Easy run `20260506_114649_974`, Mission_Missiles turn 2 source-click no-op, simulator v59.

46a. **Detritus Contraption can act from smoke.** `Missile_Unit` has
`IgnoreSmoke=true`; smoke on the Contraption tile must not prune
`Missiles_Shield` / `Missiles_OneDmg` from action enumeration. If
`Mission_Missiles` reaches a reward screen short of four Contraption uses after
the unit sat in smoke, check the smoke legality gate before blaming target
selection. Regression anchor: Bombermechs No Survivors run
`20260630_181556_177`, Detritus `Mission_Missiles` turn 4, where a smoked
Contraption was skipped and Region Secured showed `Use the Detritus Contraption
four times (3/4)`; fixed in simulator v310.

48. **Normal/Alpha Spiders are pushable.** `Spider1` / `Spider2` throw eggs but are still normal pushable Vek; do not mark them like Burrowers or Psions. If Repulse or another push leaves a Spider unmoved in prediction while the engine moves it, check `src/model/pawn_stats.py` and solve payload `pushable` enrichment before changing Repulse logic. Regression anchor: Easy run `20260506_114649_974`, Mission_Missiles turn 4 `Science_Repulse_A` pushed Spider1 from E5 to E6; fixed in simulator v60.

49. **Teleporter move-then-attack targets use the post-swap tile.** `pawn:Move()` fires Mission_Teleporter pads before the bridge sends `ATTACK`; if the solver keeps a self-AoE target on the pre-swap pad, `pawn:FireWeapon()` can silently spend the action as an invalid target and produce no Repulse/shield effect. Enumerate attacks, smoke checks, and no-op filtering from the post-teleport board while keeping `move_to` as the pad tile. Invalid diagonal SelfAoe replay events are no-ops, not centered attacks. Regression anchor: Easy run `20260506_114649_974`, Mission_Teleporter turn 4 `Science_Repulse_A` stale target D4 after teleport to E3; fixed in simulator v61.

50. **Every move-then-attack target is relative to the post-move tile.** The teleporter fix must not special-case only pad moves and accidentally leave normal moves targeting from the old tile. If `auto_turn` shows `click_miss|Brute_Jetmech|attack` where Aerial Bombs was legal from the pre-move tile but not the moved tile, check `enumerate_actions`: attack target enumeration, smoke checks, and effect filtering all need the post-move attack origin. Regression anchor: Easy run `20260506_114649_974`, Mission_Teleporter turn 4 dirty recovery emitted JetMech G1→D3 then Aerial Bombs at G3, which was only legal from G1; fixed in simulator v62.

53. **ACID pools are non-stoppable for player movement.** The operational "never move onto ACID voluntarily" rule is enforced in Rust movement enumeration, not merely by scoring penalties. If a plan moves a mech onto a tile with `tile.acid=true`, treat it as a movement enumeration bug before considering weapon logic; ACID-pool status reporting can be bridge-ambiguous after the move, so the safest policy is to keep those destinations out of search for player-controlled units. Regression anchor: Easy run `20260506_114649_974`, Corporate HQ turn 2 moved RocketMech D3→C5 onto an ACID pool and produced `status|Ranged_Rocket|move`; fixed in simulator v64.

54. **Smoke on a queued web source releases the web immediately.** Scorpion/Leaper-style current-turn webs are tied to the queued attack; when a player action places smoke on that enemy, the engine cancels the queued attack and `IsGrappled()` clears before the next mech acts. The simulator must break `web_source_uid` links when smoke is placed on a live enemy tile, including Rocket Artillery's behind-shooter smoke. Regression anchor: Easy run `20260506_114649_974`, Corporate HQ turn 3 RocketMech E3→B3 smoked ScorpionBoss at F3 and freed PulseMech at F4; fixed in simulator v65.

54a. **Enemy-created smoke does not cancel later queued enemy attacks.** The
enemy attack loop latches smoke cancellation after pre-attack enemy-phase
effects and before the first queued Vek attack resolves. A Mosquito, Smoldering
Shell, or other enemy attack that smokes another queued attacker during that
loop still leaves the later already-queued attack alive; only smoke present on
that attacker's tile at loop start cancels it. Regression anchor: Bombermechs
No Survivors run `20260630_181556_177`, Detritus `Mission_Missiles` turn 3,
where a Mosquito at E3 smoked the shielded F3 Mosquito, but F3 still destroyed
the G3 building; fixed in simulator v309.

55. **Rocket Artillery damage upgrades need save overlays and Rocket semantics.** `Ranged_Rocket_A` / `Ranged_Rocket_B` are 3-damage Rocket Artillery, and `Ranged_Rocket_AB` is 4-damage Rocket Artillery. They must preserve the base weapon's smoke-behind-shooter and center corpse-push behavior. If live Rocket kills a target that sim leaves at 1 HP after a blocked push/bump, inspect save-file `state.weapons` and add the upgraded ID to both `_MODELED_UPGRADED_WEAPONS` and Rust `WId` support before advancing. Regression anchor: Easy run `20260506_114649_974`, Volcanic Hive final turn 2 `Ranged_Rocket_A` killed Scorpion2 at C6 with 3 damage plus blocked bump; fixed in simulator v66.

57. **Cannon-Bot Mark I is a projectile with fire.** `SnowtankAtk1` / Cannon 8R Mark I inherits `FireflyAtk1` in `scripts/weapons_snow.lua` and sets `Fire=1`; it is not melee. The shot travels down the queued cardinal line to the first projectile blocker and sets the hit unit on fire. If Mission_FreezeBots or Mission_Hacking shows a Cannon-Bot damaging a mech behind the first empty target tile, check this weapon definition before blaming movement or Ralph/max-HP display noise. Regression anchor: Easy Rusting Hulks run `20260508_122657_124`, Mission_FreezeBots turn 1 Cannon-Bot at G3 fired through G4/G5 into PulseMech at G6, leaving Pulse at 2 HP and on fire; fixed in simulator v67.

60. **Mission_Trapped Coal Plant tiles may need inferred unique-building accounting.** The bridge can expose 2-HP Coal Plant structures as ordinary building tiles without `unique_building=true`, but live grid power still drops when one HP is lost to push/bump damage. On `Mission_Trapped`, infer objective-style grid accounting for 2-HP building tiles before solving; do not globally change non-unique 2-HP residential bump accounting without a fresh capture. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Scrapyard turn 3 Bouncer pushed a Moth into the D4 Coal Plant tile, reducing building HP 2→1 and grid 6→5; fixed in simulator v69.

62. **Leap weapons cannot land on chasm tiles.** Aerial Bombs / leap target enumeration must reject chasm landing tiles, and replay/score diagnostics must no-op illegal chasm landings instead of damaging transit tiles. Flying does not make a Cataclysm chasm a valid Aerial Bombs landing. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Mission_Cataclysm turn 3 JetMech B5→B3 click_miss where B3 was already chasm; fixed in simulator v70.

63. **Rocket Artillery center push does not edge-bump.** When `Ranged_Rocket` hits a live center target on the map edge, the engine applies Rocket damage but does not add a blocked-push bump into the off-board edge; the target stays in place. Keep Rocket's mountain/building/blocker corpse-bump semantics from rule 42, but do not count off-board edge bump as extra kill damage. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 2 RocketMech D7 fired at Moth1 on D1; live Moth stayed at D1 with 1/3 HP instead of dying to phantom edge bump, fixed in simulator v71.

64. **ACID status is not an instant pool under live targets.** ACID weapons such as `Acid_Tank_Attack` / A.C.I.D. Cannon acidify a live unit on the hit tile, but the ground beneath that unit does not become an ACID pool until an ACID unit dies there. Empty ground/rubble/water ACID hits can still create pools/tiles, and shielded or still-frozen targets can still use the "falls to feet" tile-acid rule when the unit cannot receive ACID. If a later move verify reports `tile_status` where `predicted acid=true` and `actual=false` on an occupied target tile, check for over-eager `apply_weapon_status` tile acid before blaming the moving mech. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Reclamation Zone turn 4 Acid Tank hit Scarab1 at E4, then JetMech D1→E5 surfaced the stale predicted pool; fixed in simulator v72.

65. **Starfish attacks are self-centered diagonal appendages, not melee.** `StarfishAtk1` / `StarfishAtk2` self-target the Starfish tile and damage the four diagonal tiles only. `StarfishAtkB1` / Starfish Leader does the same 3-damage diagonal pattern and additionally applies zero-damage outward pushes to the four cardinal adjacent tiles. If the bridge shows a Starfish queued target equal to its own tile, do not treat that as a no-op melee attack. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 1 StarfishBoss at E3 queued `StarfishAtkB1` on itself with eight telegraphed tiles; fixed in simulator v73.

66. **Aerial Bombs cannot land on water or lava.** Even though Jet Mech is flying, `Brute_Jetmech` / Aerial Bombs uses the weapon target area for its landing tile; forced water/lava landing clicks spend the attack as a no-op and do not damage or smoke transit tiles. Reject water/lava landings in target enumeration and replay diagnostics. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 2 JetMech D7 tried to land on B7 water over Blob1 at C7; live Jet stayed at D7 and Blob1 survived; fixed in simulator v74.

67. **Repulse edge pushes do not edge-bump.** `Science_Repulse` / Repulse is a zero-damage adjacent push. When an adjacent unit would be pushed off-board, the engine leaves it in place without adding the generic 1 bump damage. On-board Repulse pushes and blocked collisions still matter, but do not count off-board edge bump as kill damage for Repulse. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 3 PulseMech D2 repulsed a 1-HP Blobber at D1; live Blobber stayed alive at D1 instead of dying to phantom edge bump, fixed in simulator v75.

68. **Web Egg adjacency webs are tile-based, but do not steal active grapples.** A live `WebbEgg1` webs every living unit in the four cardinal adjacent tiles, including units that move next to it after the egg already exists. Bridge `IsGrappled()` / grappler source can miss or misattribute this, so solver inputs and Rust landing/move simulation must infer adjacency and set `web_source_uid` to the egg when the unit has no live non-egg source currently targeting it. If a Scorpion/Leaper-style source is alive and its queued target is that unit's tile, preserve that source; killing the adjacent egg should not free the other grapple. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_FreezeBldg` turn 2 JetMech moved H2→F2 next to a F3 egg and became webbed, while Pulse remained webbed by the E2 Scorpion after the egg died; fixed in simulator v76.

69. **Burrower Slam hits the perpendicular three-tile row.** `BurrowerAtk1` / `BurrowerAtk2` are melee attacks with `path_size=3`: the queued direction selects the center target tile, and the engine also damages the two perpendicular flank tiles around that center. Do not simulate Burrowers as single-tile melee. If an objective/building loss follows a Burrower attack, first check for a missed flank tile in the slam row. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Stasis` turn 1 Coal Plant at C4 was lost by a Burrower1 flank that the sim missed; fixed in simulator v77.

71. **Player artillery target areas are cardinal, not diagonal.** Standard player artillery weapons such as Artemis Artillery, Rocket Artillery, and Rock Accelerator fire along a row or column from the shooter. A bridge `FireWeapon` call can return OK on an off-axis point but spend the action as an engine no-op, so do not broaden `get_weapon_targets(... WeaponType::Artillery ...)` to board-wide targets. Regression anchors: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Reactivation` turn 2, RocketMech E6 fired `Ranged_Rocket_A` off-axis at C4 and live Burnbug1 survived untouched; Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, ArtiMech D6 fired Artemis at F2 and live Crab1/Firefly2 were unchanged. Fixed in simulator v80/v134.

72. **Diagnostic replay/score must reject impossible player actions.** `score_plan` / `replay_solution` are useful only when they obey the same legality gates as live search: movement destinations must be reachable and unblocked, smoke on the attack origin prevents attacks/repair, and weapon targets must be in that weapon's legal target area. If a hand-written plan looks clean but `solve` omits it, first check for `illegal_move:*`, `illegal_attack_smoke:*`, or `illegal_weapon_target:*` events before calling it a solver miss. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Reactivation` turn 2: JetMech A4→A5 then Aerial Bombs at C5 was blocked by smoke, while RocketMech E6 off-axis C4 was outside Rocket Artillery's target area; diagnostic guards hardened in simulator v80.

73. **Bridge tile arrays are records, not coordinate-indexed grids.** Recorded `bridge_state["tiles"]` can be a flat list whose order is not `x*8+y`; each tile carries authoritative `x` / `y` fields. When inspecting raw solve inputs, build a lookup from `(tile["x"], tile["y"])` instead of indexing the list directly, or you can mislabel terrain/buildings and chase a false legality bug.

75. **Normal Psions are pushable.** The shipped `pawns.lua` `Jelly_Health1` family inherits default `Pushable=true`; do not inject `pushable=false` for `Jelly_Health1`, `Jelly_Armor1`, `Jelly_Regen1`, `Jelly_Explode1`, `Jelly_Lava1`, or AE Psions. If Repulse/Rocket predictions miss bump damage through a Psion into a building, check pawn stats enrichment before changing push physics. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Repair` turn 2: PulseMech Repulse at C4 bumped Blast Psion from C5 into the 2-HP C6 building, damaging both; fixed in simulator v81.

76. **Blast Psion explosions chain through eligible non-minor Vek.** `Jelly_Explode1` grants "all other Vek explode on death"; if a Blast Psion aura burst kills another ordinary non-minor Vek, that secondary death emits its own 1-damage cardinal burst. Minor pawns still do not receive the aura, so old Totem/Spore/Blob cases remain non-chaining, and intrinsic Volatile Vek decay stays separate. Regression anchors: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Repair` turn 3, Totem D2 did not explode because it was Minor; Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, R.S.T. Perimeter turn 2, Dash Punch killed Burnbug C4, whose burst killed Moth B4 / Leaper C3 and secondary bursts destroyed B3; corrected in simulator v125.

77. **Dam flood drown deaths still fire death side effects.** Old Earth Dam flood converts eligible tiles to water, and grounded non-massive Vek drowned by that water must go through the normal instant-death bookkeeping: kill credit, Psion cleanup, Blast Psion aura burst, and Volatile decay. Do not raw-set `hp = 0` in flood helpers. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Dam` / Central Museums turn 2: Rocket destroyed the dam, flood drowned Shaman2 at D4, and live Blast Psion aura damaged the adjacent 2-HP Robotics Lab at D3; fixed in simulator v83.

82. **Ramming recoil on sand creates smoke.** `Brute_Beetle` / Ramming Engines self-damage is engine damage on the charger's final tile; if Charge Mech ends on sand after an impact, the sand becomes ground with smoke. Do not limit sand→smoke terrain mutation to target weapon damage only. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Mission_Terratide / Razor Bay turn 1: ChargeMech C3→E3 recoil converted E3 sand into smoke; fixed in simulator v90.

87. **Mission_BoomBots `*_Boom` pawns have Explosive Decay.** `Snowtank1_Boom`, `Snowlaser1_Boom`, and `Snowart1_Boom` use Lua `Explodes=true`: on death they deal 1 damage to the four cardinal-adjacent tiles, can kill adjacent mechs, damage/destroy buildings, and ignite forests into burning ground. Treat this as intrinsic Volatile-style decay rather than an enemy attack. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Pinnacle `Mission_BoomBots` / Central Processor turn 2: Defense Mech pulled `Snowart1_Boom` into itself, destroying the E5 building, killing Defense, damaging/burning Laser, and dropping grid after animation settled; fixed in simulator v93.

88. **Burst Beam Ally Immune needs upgraded weapon IDs.** `Prime_Lasermech_A` / `Prime_Lasermech_AB` must overlay from save-file powered mods; the Lua bridge's base SkillList only says `Prime_Lasermech`. The Ally Immune upgrade skips player-side unit damage but still lets the beam pass through the friendly tile and continue normal damage decay, while buildings/terrain remain vulnerable. If a C-Beam line appears to kill a friendly despite powered Ally Immune, check save overlay + Rust `WId` mapping before rejecting the line. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Detritus `Mission_AcidStorm` / Containment Zone D turn 4: LaserMech B3→A6 fired at B6, hitting B6/C6/E6/G6 through ScienceMech at D6 and unlocking Glittering C-Beam; fixed in simulator v94.

89. **Titan Fist Dash needs upgraded weapon IDs.** `Prime_Punchmech_A` / `_AB` must overlay from save-file powered mods; the Lua bridge's base SkillList only says `Prime_Punchmech`. Dash Punch is a `Charge`-style direction selector with no self-damage: click the adjacent cardinal direction tile, the Combat Mech charges to the last free tile, then punches the first blocker. `Prime_Punchmech_B` remains melee with 4 damage. The Lua script sets Dash cost to 2 cores and +2 Damage to 3 cores; do not assume cheaper metadata. Regression anchor: Ramming Speed setup on Easy Rift Walkers run `20260510_213059_819`, fixed in simulator v95.

93. **Mountain-destruction objectives need explicit attention until scorer coverage is proven.** Missions such as R.S.T. `Destroy 2 mountains` can be won while silently failing the mountain objective if the live solve spends all actions on kills/threats. Before ending the last two turns on these missions, check objective progress on the visible panel or solve output; if progress is still 0, prefer a clean mountain-damage line when grid/objectives remain safe, and file/fix scorer support after the run. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Thunderbolt Grid: kill-count and Coal Plant succeeded, but `Destroy 2 mountains` finished at 0/2.

95. **Do not bypass soft-disables during safety widening.** A soft-disabled weapon has already produced live evidence that its predicted outcome is unreliable. Emergency top-k widening should still search deeper, but it must keep `disabled_actions` in the payload; removing the mask can promote a plan that appears clean only because it repeats the bad prediction. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Mercury Ridge: `Brute_Tankmech` over-predicted an edge-bump kill on Scorpion1, the survivor plus later cataclysm flow killed Ralph, and the mission outcome only surfaced the KIA after the bridge phase flipped to `unknown`.

96. **Taurus/Artemis edge-push kills are v96 regression anchors.** Simulator v96 models Taurus Cannon direct edge pushes and Artemis adjacent edge pushes as weapon damage only, with no off-board bump damage; on-board blocker bumps remain intact. If a future solver/wheel again predicts a `Ranged_Artillerymech` or `Brute_Tankmech` off-board edge-push kill that the live board leaves at 1 HP, fresh-read before End Turn and treat it as a simulator regression. Regression anchors: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Restricted Area turn 3 Hornet1 survived Artemis edge push; R.S.T. Mercury Ridge turn 1 Scorpion1 survived Taurus edge push.

97. **Never double-click End Turn after an ambiguous transition.** After dispatching an emitted End Turn click, do not click End Turn again unless a fresh `auto_turn`, `read`, or `click_end_turn` command explicitly emits a new End Turn plan for the current turn. If the screen returns from enemy activity with the victory counter reduced, or if the End Turn button state is visually unclear, treat that as the next player turn and run `auto_turn --time-limit 10`; a second manual End Turn click can skip an entire turn. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Corporate HQ turn 2 was skipped after an ambiguous End Turn transition, causing ArtiMech destruction before Turn 3.

100. **Final Cave danger kills flyers.** `Mission_Final_Cave` uses `Env_Final` falling-rock/tentacle death effects, not ordinary Cataclysm/Seismic chasm conversion. Even Prospero/flying mechs die on marked tiles. If `environment_danger_v2` shows `Mission_Final_Cave` with `flying_immune=1`, treat the bridge as stale/wrong and fix the bridge/Rust hardening before advancing. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Final Cave turn 1 Combat E6→C6 was predicted safe with Prospero but died when C6 collapsed; fixed in simulator v99.

101. **Mission-end commits must push the current branch, never hard-coded main.** `mission_end` is bookkeeping for the active live branch; if its helper ever targets `origin main` or another unexpected branch, run it with `--no-commit`, then stage/commit/push the intended artifacts manually on the current branch. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, where Final Cave loss logging was correctly committed to `codex` manually because the helper still used `origin main`; fixed to push `origin HEAD`.

103. **Stable / guarding units must come from the live bridge, not static pawn stats.** Bosses and mission units can be normal-pushable in `pawn_stats.py` but currently Stable in the engine (`pawn:IsGuarding()`), which blocks push and teleport effects. The Lua bridge must export live `pushable=false` when `IsGuarding()` is true, Python `Board.from_bridge_data` must preserve that live override, and Rust should treat the payload as authoritative. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Final Cave turn 2: Taurus hit the B6 FireflyBoss for damage but the Stable boss did not move to A6; fixed in simulator v100.

112. **Mission_Belt conveyors fire before Vek attacks.** Conveyor tiles push live units one tile before queued enemy attacks resolve, and moved Vek re-aim from their conveyor-shifted tile while preserving the original queued direction. Save/bridge conveyor parsers must keep `loc` and `custom="conveyorN.png"` on the same serialized map row; cross-entry regexes can create phantom belts on buildings and miss the real belt that moves the attacker. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Detritus Venting Fields turn 1: the F3 Moth on bridge `(5,5)` rode conveyor3 to F4 `(4,5)` before firing and destroyed C7 `(1,5)`; fixed in simulator v106.

113. **Conveyors are tile-driven, not mission-id-driven.** Detritus conveyor tiles can appear outside `Mission_Belt`, including `Mission_Acid`; if any live tile has `conveyor >= 0`, the enemy phase must apply the belt tick before Vek attacks. Do not gate conveyor simulation solely on `mission_id == "Mission_Belt"`. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Chemical Field A turn 2 had live conveyors in `Mission_Acid`; generalized in simulator v107.

114. **Repair platforms heal only to max HP.** `Item_Repair_Mine` still consumes and increments `repair_platforms_used` when any live unit lands on it. Damaged units heal only up to max HP; full-health or already-overcapped units consume/count the platform with HP unchanged. Regression anchors: Hard Rusting Hulks run `20260512_181719_119`, Forgotten Hills turn 2 PulseMech moved to `G4` at `3/3`; live consumed the platform and counted progress but left Pulse at `3/3` instead of predicted `5/3`; fixed for full-health units in simulator v108. Bombermechs Complete Victory run `20260623_105703_708`, Bad Repairs turn 1 BomlingMech moved to `E4` at `1/3`; live healed to `3/3` instead of predicted `5/3`; damaged-unit cap fixed in simulator v274.

118. **Leap attacks resolve landing effects and break web.** If Aerial Bombs, Bombing Run, Leap, or another Leap-style attack moves the firing unit to a new tile, that tile change breaks the unit's own web and then resolves landing effects: fire/ACID pickup, mines, repair platforms, teleporter pads, and WebbEgg adjacency. A webbed JetMech can still fire Aerial Bombs; if it lands on an already-burning tile at 1 HP, it will catch Fire and die at the next enemy-phase fire tick unless repaired or shielded. Burning Forest landing tiles are consumed to burning Ground. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Archival Flats turn 3, Jet landed on burning `F2` after Rocket ignited the forest; fixed in simulator v109.

120. **Aerial Bombs occupied transit smoke is not generic terrain damage.** When JetMech flies over a live unit, the engine applies transit unit damage, and the bridge workaround then applies only smoke/status on that tile. Do not run the generic terrain damage path for that occupied transit tile: sand under a shielded Pulse stays Sand with smoke, while the shield is still consumed; bridge smoke-only occupied transit should use direct `Board:SetSmoke` rather than `SpaceDamage(0)` so the post-fire UI does not refresh through a bad weapon context. Empty transit terrain and explicit self-damage rules remain separate; Ramming Engines recoil on sand still consumes sand per rule 82. Regression anchors: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 2, JetMech `E3 -> C3` over shielded Pulse on sandy `D3`; Mission_Holes turn 3, JetMech `D5 -> D3` over Hornet at `D4` hit a visible `Brute_Jetmech::GetTargetArea` crash.

121. **Non-unique multi-HP bump damage can carry delayed grid debt.** A bump into a non-unique 2-HP building can reduce HP while the visible grid scalar stays unchanged during player-phase verification, but the debt must be charged no later than enemy-turn start even if that building survives; if the same building is destroyed before then, charge and clear the debt immediately. Do not mark every such building unique just to force immediate grid loss: action verification may correctly see HP damage with no instant grid drop. Carry per-tile deferred bump debt through the simulated line and flush it at the enemy-turn boundary. Regression anchors: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 2, Pulse Repulse at `E4` bumped a Hornet into the `C4` building (HP 2→1, grid stayed 7), then enemy-phase destruction settled true grid at 2 instead of predicted 3; fixed in simulator v111. Hard Rusting Hulks run `20260513_144310_771`, Mission_Holes turn 2, Rocket bumped `B4` 2→1, grid stayed 4 during player phase, then became 3 next player turn while `B4` survived; fixed in simulator v114.

125. **Synthetic no-belt boards must use `conveyor_dir=-1`.** `Tile::default()` is used heavily by focused simulator tests and ad hoc replay fixtures. Its `conveyor_dir` must mirror the bridge's no-conveyor sentinel (`-1`), not Rust's numeric default `0`; otherwise every default tile behaves like a belt and enemy attacks re-aim before the tested weapon resolves. Regression anchor: the first Scarab Leader v112 test shifted the attacker before firing and hid the actual artillery behavior until `Tile::default()` was fixed.

134. **Landing on smoke extinguishes carried unit fire.** The simulator's landing pipeline clears a unit's existing Fire status when it lands on a smoky tile, before any fire-tile pickup. This matters for Aerial Bombs and other movement/teleport effects that land a burning unit onto pre-existing smoke. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Mission_Airstrike turn 2, Jet used Aerial Bombs from H3 to already-smoked F3; live cleared Jet fire, fixed in simulator v116.

136. **Beetle charges push, and Bouncer edge recoil is harmless.** `BeetleAtk1` / `BeetleAtk2` are Charge attacks with forward target push; the target can be shoved into a later Vek's queued tile before that Vek attacks. Bouncer self-recoil into the board edge should not apply off-board bump damage to the Bouncer, but the horn hit still resolves. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Mission_Airstrike turn 4: Beetle shoved Rocket from B3 to B4, then Alpha Bouncer at A4 hit B4 and pushed the KIA Rocket to C4; fixed in simulator v117.

140. **Boss leader kills are unit-objective metadata, not just generic boss flavor.** Corporate HQ boss missions must map their leader pawn into `destroy_objective_unit_types` in `data/mission_unit_objectives.json`; the generic `boss_killed_bonus` is turn-scaled and can become too weak in late turns. If a Region Secured screen says `Destroy the <Leader> (Failed)`, check the solve payload for an empty `destroy_objective_unit_types` list before clicking Continue. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, R.S.T. Corporate HQ ended at 7/7 with the Tower protected but `Destroy the Spider Leader (Failed)` because `Mission_SpiderBoss` had no explicit `SpiderBoss` destroy objective.

145. **Effective mech max HP comes from saveData when bridge and save disagree.** The live bridge can report base pawn max HP after a repair/action while `saveData.lua` still carries the effective `pawn*.max_health` from pilot perks and powered +Health upgrades. Overlay save-backed `max_health` before Board construction and Rust solve/verify payloads, but do not overlay save `health` mid-turn because it can lag after bridge actions. When an overlay fires, surface `pilot_calibration_requests` so the harness can capture the visible pilot panel later and map perk text into solver metadata. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Corporate HQ turn 3, Jet at D5 with Michael Lee `+2 Mech HP` verified as `3/2` instead of effective `3/4`, and Pulse at C6 had a +Health core making bridge `4/3` vs save max `5`.

147. **Time Pods are fragile under non-collecting contact.** A player mech landing on a pod collects it, but an enemy or non-mech unit landing on a pod destroys it without `pods_collected` credit; direct tile damage also destroys it. Safety should then see `pods_present` drop with `pods_collected=0` and block as `pod_lost`. Do not treat a disappeared pod as recovered unless reward/save state says recovered (`pod == 3`); save `pod == 2` is failed/destroyed. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive The Pasture turn 2, Taurus pushed a Bouncer onto F3 and the reward screen showed `Protect the Time Pod (Failed)` while simulator v121 kept `has_pod=true`; fixed in simulator v122.

150. **Large Goo is a split-on-death boss, not a single kill.** `Mission_BlobBoss` / Large Goo (`BlobBoss`) splits immediately into 2 Medium Goos (`BlobBossMed`) on death; each Medium Goo splits into 2 Small Goos (`BlobBossSmall`), including when killed by chasm/instant-death terrain. The split search uses the Lua diamond around the death tile, primary radius 1-2 then backup radius 3-4, with deterministic representatives in Rust because the live game uses `random_removal`. Goo attacks are 4-damage queued squishes that persist after displacement, double-hit mountains, and move into non-mech target tiles when the squish clears them. If `auto_turn` wants to kill Large/Medium Goo and predicted post-attack does not show child Goos, stop and fix the simulator before continuing. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive HQ turn 1, Large Goo at B3; fixed in simulator v123.

151. **Moved queued attackers preserve their original target offset.** Save `piQueuedShot` / `piTarget` can remain expressed relative to `piOrigin` after a queued Vek moves, but the live attack target shifts by the same attacker displacement. Bridge/Python must normalize raw queued targets with `current_position + (piQueuedShot - piOrigin)`, and Rust enemy-phase replay must use the captured original position plus the current position rather than treating queued targets as fixed absolute tiles. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive HQ turn 1, BlobBoss B3->B2 retargeted A3->A2 and Scarab G3->G2 retargeted G6->G5; fixed in simulator v124.

152. **Bridge REPAIR must directly mutate HP/status.** In the bridge command context, `Board:AddEffect(Skill_Repair:GetSkillEffect(...))` can ACK as `[skill]` while leaving HP/status unchanged. The REPAIR handler should set mech HP directly, use zero-damage `SpaceDamage` only for Fire/ACID/Frozen/Injure cleanup, consume Boosted manually, preserve Kai Miller's full-HP state boost, and emulate Harold Schmidt's adjacent repair push. If repair verify shows `hp predicted +1/+2 actual unchanged` plus `status.boosted predicted false actual true`, patch/reinstall the bridge before treating it as a dirty tactical line. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, R.S.T. Perimeter turn 4, Tank repaired at E6 but live stayed `2 HP` and Boosted until bridge REPAIR stopped using `Board:AddEffect`.

153. **Bridge REPAIR max HP needs the same fallback as state export.** Some live pawn wrappers do not expose `GetMaxHealth()`, even though bridge state extraction already survives by falling back to `_G[pawn:GetType()].Health` and then current HP. The REPAIR handler must wrap `pawn:GetMaxHealth()` in `pcall` and use that same fallback before computing direct heal amounts; otherwise a tactical repair can crash after the mech moves, leaving a partial live turn that must be recovered by fresh `read` + `solve`. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Lone Mesa turn 3, Tank D3 repair crashed with `attempt to call method 'GetMaxHealth' (a nil value)`.

155. **Mission_Disposal gives you a controllable Disintegrator ally.** The `Disposal_Unit` / A.C.I.D. Launcher is a READY friendly non-mech with `Disposal_Attack`; it cannot move, is not pushable, ignores smoke for firing, and must be included in solver actor generation when active. `Disposal_Attack` targets an artillery tile and instantly kills/acids the target plus the four cardinal adjacent tiles, clearing mountains to ground; omitting this weapon can leave preventable building threats such as D5 unresolved. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 1, Disposal_Unit at G7 was omitted until simulator v126.

156. **Boost Psion death clears visible Boosted immediately.** When the last `Jelly_Boost1` dies, all surviving Vek immediately lose the `Boosted` status flag as well as the +1 attack-damage aura. If verify shows only `status.boosted predicted true actual false` after a Boost Psion kill, patch `on_enemy_death` / simulator status cleanup rather than treating the weapon push as wrong. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 1, Tank F6 fired at F5, killed Jelly_Boost1 at F4, and live cleared Boosted from E4/F3/D4; fixed in simulator v127.

161. **BombRock explosions are immediate under damage+push.** If Taurus Cannon, Rocket-style pushes, or another damage+push weapon kills a `BombRock`, the Unstable Boulder explodes from the hit tile before any corpse push; it does not continue onward as a dead pushable body. Adjacent BombRocks chain immediately and can damage buildings/mechs before the current action verifies. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Corporate HQ turn 2: Tank C7 fired at C6, C6 BombRock exploded into Tank/D6/C5, then C5 exploded into D5; fixed in simulator v131.

164. **Off-axis Artemis bridge shots are no-ops.** Artemis Artillery looks directionless in the Rust weapon definition, but live `pawn:FireWeapon()` still uses a cardinal target area: off-axis bridge calls can return OK, spend the mech action, and leave the target/adjacent tiles unchanged. Do not rely on C6→E2/F2-style lobs for rescue geometry; keep player `WeaponType::Artillery` enumeration and replay validation cardinal-only unless a live UI capture proves a specific weapon differs. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, ArtiMech D6 fired Artemis at F2 and live Crab1 stayed 3 HP while E2 Firefly did not move; fixed in simulator v134.

165. **Pending-grid-debt probes must be mission-keyed, not just region/turn.** Regions can be reused across islands and missions within one run, so `resist_probe.jsonl` entries for `region1` turn 1 from an older mission must not seed pending grid debt for a later `region1` turn 1. Match run id, region, turn, mission id, mission slot, and master seed when available; otherwise safety summaries can show false grid baselines such as 7/7 visible grid becoming 4/7 before any live damage. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, stale `Mission_Tides` region1 turn1 probe inflated pending grid debt by 3 before the B5/C6/D5/D7 solve.

168. **Repair does not clear ACID while ACID Storm is active.** On `Mission_AcidStorm`, while the `Storm_Generator` objective is alive, a mech Repair or Repair Drop should still heal/clear normal repair statuses but the unit remains `acid=true` in the same verification window. Do not soft-disable Repair or re-solve on a lone `status.acid predicted false actual true` for a repaired player unit after this sim rule is installed. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus The Wasteland turn 2, WallMech E3 repaired under the storm and stayed ACIDed.

169. **Disposal teleporter tiles can move mechs during action verification.** On Detritus disposal/teleporter missions, a solver move that lands on a teleporter tile can immediately relocate the mech and produce `mech_position_wrong` drift before the planned attack fires. Treat teleporter tiles as live hazards for movement planning and review any Rockart/Ranged move onto a teleporter before trusting the stored continuation; if the bridge has already resolved the teleport, fresh-read/re-solve from the actual board before End Turn. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus Nanite Farms turn 1, RockartMech D6->D7 landed on a teleporter and re-solved from the actual position.

171. **Rock Launcher side pushes outrun center Boom Bot decay.** When Rock Launcher kills a `*_Boom` / Volatile center target, its perpendicular side pushes resolve before the killed center target's Explosive Decay splash. Adjacent side Boom Bots can be shoved out of the blast radius and survive, then act in the enemy phase; do not count them dead from pre-push adjacency. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_BoomBots` turn 1, RockartMech D6 fired at D4, the E4 Boom Tank survived by moving to F4 and later burned the F7 building; fixed in simulator v138.

174. **Chain Whip is pawn/building-chain damage with narrow terrain side effects.** `Prime_Lightning` emits damage for pawns, and with `Prime_Lightning_A/AB` it treats live Grid Buildings as zero-damage chain nodes (`Buildings=true`) rather than damaging the building. Do not simulate Chain Whip as generic weapon tile damage: it must not crack Ice, break cracked Ice to Water, smoke Sand, or destroy Time Pods unless another effect actually touches those tiles. Live captures do show Forest under a zapped pawn igniting and applying Fire to that pawn, so model that specific terrain side effect. Regression anchors: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 2, Chain Whip hit `Starfish2` on cracked E4 ice and live left the tile as Ice while simulator v138 broke it to Water and falsely drowned the Starfish; same run/mission turn 4, Chain Whip hit WallMech on D6 Forest and live converted the tile to burning Ground with WallMech on Fire.

176. **Save `primary_power` is base weapon power, not upgrade suffix state.** Do not infer `_A` / `_B` / `_AB` weapon IDs from `primary_power` or `secondary_power`; those lists can merely indicate the base weapon is powered. Use the save's explicit weapon ID if it already has a suffix, or the upgrade mod groups (`primary_mod1` / `primary_mod2`, secondary equivalents) when they are fully powered. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 4, `primary_power={1}` on base `Prime_Lightning` was mis-overlaid as `Prime_Lightning_A`, causing the solver to expect Building Chain through D7/E7 and falsely predict the F7 Bouncer death.

212. **`cargo test` accepts one positional name filter.** To run multiple Rust tests, use a shared substring filter (for example `cargo test flame_shielding`) or run separate commands; do not pass two test names before `--`, because Cargo rejects the second as an unexpected argument. Regression anchor: simulator v147 patch validation after `Mission_Tanks` failure, where `cargo test -q test_a test_b` errored before compiling.

213. **Python `MechAction` has no `weapon_id` constructor field.** The replay shim dataclass in `src/solver/solver.py` only accepts `mech_uid`, `mech_type`, `move_to`, `weapon`, `target`, and optional `description`; if a recorded action JSON has `weapon_id`, drop it or call Rust `itb_solver.replay_solution` directly with raw action dictionaries. Regression anchor: simulator v147 validation script after `Mission_Tanks` failure, where reconstructing `MechAction(..., weapon_id=...)` raised `TypeError`.

214. **Refresh Computer Use state after long terminal work.** `mcp__computer_use__.get_app_state` can expire while builds/regressions run; before the first post-regression UI click, call `get_app_state` again even if one was captured earlier in the assistant turn. Regression anchor: simulator v147 validation after `Mission_Tanks`, where the Continue click was rejected with "Computer Use is not active" after the long regression run.

272. **Tri-Rocket killed ACID corpses resolve from their pushed destination.** `Ranged_Crack` / Tri-Rocket's three `SpaceDamage` hits are separate, but a unit killed by one rocket and pushed by that same hit does not leave a new ACID pool on its pre-push tile. The pool follows the pushed corpse destination. A later rocket that shoves another live unit into the just-vacated killed-corpse tile applies one corpse-bump damage to the entering unit, but the unit still occupies the vacated tile and does not pick up ACID from the old tile. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Missiles` turn 1, where `Ranged_Crack` C7→C3 pushed killed ACID `Scarab2` C3→C2; live left `Firefly2` at C3 with 2/5 HP and no ACID while pre-v167 Rust predicted 3/5 HP with ACID.

274. **Seismic Capacitor crack effects damage adjacent Mountains.** `Science_KO_Crack` emits `SpaceDamage(..., 0)` with `iCrack=EFFECT_CREATE` on the four adjacent tiles after a lethal hit. On Mountains, that crack effect reduces Mountain HP by 1 instead of doing nothing; a full Mountain becomes damaged, and an already-damaged Mountain can be broken. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Missiles` turn 1, where `Science_KO_Crack` killed `Jelly_Explode1` at G2 and live changed adjacent G3 Mountain HP 2→1 while pre-v168 Rust left it at 2.

277. **Tri-Rocket killed adjacent targets corpse-bump center blockers.** `Ranged_Crack` / Tri-Rocket resolves each missile as damage plus forward push. If an adjacent missile target dies and its corpse is pushed into the live center target, the center target still takes corpse-bump damage in addition to its own direct hit and any live blocker bump. Do not generalize this to Cluster Artillery outer dead-corpse absorption; this is Tri-Rocket-specific. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Teleporter` turn 3, where `Ranged_Crack` G8→G5 killed adjacent G6 Mosquito and live left Pitcher/Bottlecap at 1/4 HP while pre-v169 Rust predicted 2/4.

278. **Tri-Rocket terrain-killed landings do not leave vacated corpse-bumps.** The vacated-tile corpse-bump rule for `Ranged_Crack` applies only when the rocket hit itself killed the pushed target before the push. If the target survives the direct hit but dies after landing on newly opened Chasm / deadly terrain, a following rocket can move another unit into the vacated tile without taking the extra corpse-bump. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Teleporter` turn 4, where Tri-Rocket opened cracked B2 into Chasm, pushed `Firefly2` B3→B2 to its death, and live left `Mosquito2` at B3 with 1 HP while pre-v170 Rust killed it via a phantom vacated corpse-bump.

279. **VIP trucks move via a weapon skill, not pawn MoveSpeed.** `VIP_Truck` has `MoveSpeed=0` and primary `VIP_Truck_Move`; the skill's Lua `GetTargetArea` uses `Board:GetReachable(point, 3, Pawn:GetPathProf())`, and `GetSkillEffect` applies `AddMove(...)`. Model this as an attack-phase range-3 path move, not as normal movement and not as `WId::None`; normal attack legality still applies, so smoke can block it. If `Mission_Civilians` / Nano Silos shows READY VIP trucks on non-smoked tiles, the solver must emit legal `VIP_Truck_Move` actions for them before any dirty objective-loss consent. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Civilians` turn 2, where both VIP trucks were active and threatened but pre-fix Rust searched only the three mechs and offered a dirty one-VIP-loss line.

282. **Tri-Rocket cannot select an adjacent center target.** `Ranged_Crack` inherits the engine's `LineArtillery:GetTargetArea`, whose selectable center tiles start at distance 2. The near rocket can still hit an adjacent enemy by targeting the tile behind it, but sending the bridge `ATTACK` at an adjacent center can ACK and spend the action while producing no engine effect. Keep all Tri-Rocket variants at `range_min=2`, and add a regression before resuming if an adjacent Tri-Rocket shot records `click_miss`. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_AcidTank` turn 2, where Triptych at E3 fired `Ranged_Crack` at adjacent F3, the bridge ACKed `FireWeapon`, but live left `Firefly2` alive and Triptych unmoved.

284. **Tri-Rocket center BombRocks collide forward instead of side-blasting.** When `Ranged_Crack` destroys an Unstable Boulder on the center rocket tile, live resolution treats the killed boulder as a forward line collision before the front rocket's own hit, and it does not emit the normal four-way BombRock side blast. Keep the generic BombRock explosion path intact for Taurus/ordinary damage; this is a Tri-Rocket-specific exception. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, R.S.T. `Mission_Lightning` turn 2, where `Ranged_Crack_B` B7→B4 killed the B4 `BombRock`, live damaged `Dung2` B3→B2 to 2/5 HP, and left adjacent `Scorpion2` at C4 on 4/5 HP.

287. **Mission_Wind raw WindDir needs solver-coordinate conversion.** `LiveEnvironment.WindDir` is an engine DIR_* constant, not the solver's bridge-coordinate direction index. Convert it before storing `Board.env_wind_dir`; in particular raw `WindDir=2` pushed the marked E3 Vek egg sack toward D3, not F3. If a wind solve uses an egg sack, boulder, mech, or Vek as a projectile blocker, replay the wind push with the converted direction before trusting the safety result. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, R.S.T. `Mission_Wind` turn 1, where pre-v177 Rust wind-pushed `BonusDebris` into an Alpha Burnbug lane and falsely predicted Trimissile would survive.

288. **Hydraulic Lifter bridge execution must mirror Boost use.** `Prime_TC_Punt*` uses a custom two-click bridge path (`Board:AddEffect(skill:GetFinalEffect(...))`) rather than `pawn:FireWeapon`, so the bridge must manually apply the +1 Boost damage and then update `status.boosted`: consume ordinary/Opener Boost, preserve Kai's full-HP Arrogant Boost, and grant Morgan's Finisher Boost on a killed enemy. If a Pitcher action verifies with `damage_amount|Prime_TC_Punt|attack` or `status.boosted predicted false actual true`, patch/reload the bridge before continuing instead of teaching Rust the bridge's underpowered stale-Boost behavior. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Detritus `Mission_Bomb` turns 1-2, where boosted Gana's Hydraulic Lifter left `Burnbug2` 1 HP too high and failed to consume Opener Boost.

289. **Enemy queued-target origins survive displacement and flips.** Do not derive every enemy attack direction from the pre-action board position alone after player weapons retarget a queued attack. Preserve the queued target's origin through player-phase displacement; when `DIR_FLIP` effects such as Seismic Capacitor flip a moved enemy, reverse the original attack vector around the enemy's current tile and reset that origin. Enemy Charge attacks must also move the charger along the queued charge path before applying impact damage. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, `Mission_BeetleBoss` turn 2, where a displaced Seismic-flipped Beetle Leader should charge through C6 into B6, but pre-v178 Rust either aimed from a stale diagonal or left the charger in place and underpredicted grid loss.

290. **Hydraulic Lifter forest landings ignite surviving targets.** `Prime_TC_Punt*` moves the grabbed unit to the landing tile, then applies landing damage. If that landing tile is Forest, live consumes the Forest into burning Ground and the surviving thrown unit immediately gains Fire; this is stricter than ordinary delayed forest fire bookkeeping. Regression anchor: Cataclysm Unfair stress run `20260521_120049_468`, Archive `Mission_Volatile` turn 1, where upgraded `Prime_TC_Punt_AB` threw `Firefly2` from G3 to E3 Forest and live set `status.fire=true` while pre-v179 Rust only ignited the tile.

322. **Webbed Leap Mech cannot fire Hydraulic Legs.** `Prime_Leap` / Hydraulic Legs is an attack-shaped self-move, but live no-ops it while the Leap Mech is webbed: the mech stays in place, remains webbed, takes no self-damage, and landing-adjacent enemies survive. Keep this distinct from Aerial Bombs / Bombing Run, which have live evidence for web-breaking leap movement. Regression anchor: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Barrels` turn 2, where webbed Leap at C4 was predicted to leap to B3 and kill the C3 Leaper, but live left Leap webbed and the Leaper alive.

323. **Hydraulic Legs target area is cardinal-line only.** `Prime_Leap` inherits `Leap_Attack:GetTargetArea`, which enumerates `DIR_VECTORS[i] * k`: long jumps are legal, but only along the same row or column. A non-cardinal bridge `ATTACK` can ACK while producing no engine effect, so treat any off-axis Hydraulic Legs plan as a simulator bug, not a tactical option. Regression anchor: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Barrels` turn 3, where Leap at E3 was predicted to jump to G4 and destroy the H4 A.C.I.D. Vat, but live left Leap and the vat unchanged.

324. **Patch Leap_Attack target areas in the bridge context.** Vanilla `Leap_Attack:GetTargetArea` calls global `Pawn:GetPathProf()`, which is set during normal UI targeting but can be nil during file-bridge `pawn:FireWeapon(...)`. If bridge execution of `Prime_Leap` / Hydraulic Legs raises the black error screen `Something went wrong in Prime_Leap:GetTargetArea`, do not classify it as a normal click miss or continue the turn; patch/reinstall `src/bridge/modloader.lua` so Leap_Attack-family skills use a bridge-safe target-area wrapper that derives the path profile from the source pawn, then restart/restore before retrying. Regression anchor: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Barrels` turn 3, where a legal E4→G4 Hydraulic Legs bridge fire crashed the Lua target-area context.

326. **Viscera Nanobots heal after attack kills, including self-damage recoil.** `Passive_Leech` / Viscera Nanobots is an active-board passive from the Nano Mech: when a player mech deals a killing blow, heal that attacking mech by 1 HP, or 2 HP for `Passive_Leech_A`, after the attack's weapon damage and Hazardous self-damage have resolved. A Hazardous mech that reaches 0 HP from its own killing attack can be revived by this heal. The passive remains active even if the Nano Mech is disabled/dead during the battle, so do not HP-gate the passive carrier while parsing the bridge board. Do not classify a higher-than-predicted Unstable/Leap HP verify diff after a kill as a click miss. Regression anchors: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Civilians` turn 2, where `Brute_Unstable` killed a Vek and live healed UnstableTank from recoil back to full while pre-v186 Rust predicted 2/3 HP; Corporate HQ turn 4, where dead Nano's passive healed UnstableTank's recoil after killing Leaper1, simulator v191.

327. **Acid Projector pushes already-ACID targets unless the endpoint no-ops.** `Science_AcidShot` / Acid Projector is a zero-damage projectile with live forward push when the push destination is legal; do not suppress the direct push solely because the hit unit already has ACID. If the hit unit is on the map-edge endpoint in the fire direction, the zero-damage ACID payload can be suppressed entirely and the unit also stays put. Regression anchors: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Civilians` turn 4, where NanoMech B2 fired at Scarab1 on A2 and live left it alive/acid=false; Corporate HQ turn 2, where NanoMech G7 fired at F7 and pushed a clean Beetle Leader from E7 to D7 while applying ACID; and run `20260522_193613_471`, Detritus `Mission_AcidTank` turn 3, where NanoMech D5 fired toward D6 and pushed an already-ACID Burnbug1 from D7 to D8.

328. **Teleporter pad pairs are mission-scoped.** `teleporter_pairs` are valid only for `Mission_Teleporter`; if the bridge emits non-empty pairs on any other mission, treat them as stale and ignore them in the simulator. Otherwise ordinary move/recoil landings can be projected as fake pad swaps. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Detritus `Mission_AcidTank` turn 1, where stale pairs made the sim move UnstableTank C6→E6 and NanoMech C3→E2, while live left both units on their landing tiles.

329. **Viscera Nanobots do not heal bump-only kills.** `Passive_Leech` / Viscera Nanobots heals Hazardous mechs for direct weapon-damage kills, not for enemies that only die to a follow-up push/bump/edge collision. Keep total `enemies_killed` and mission kill credit, but do not count bump-only deaths toward Nanobots healing. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Detritus `Mission_AcidTank` turn 2, where Hydraulic Legs pushed an A.C.I.D.-tagged Burnbug into the board edge; live killed the Vek but Leap Mech stayed at `1/3` instead of receiving the predicted heal to `2/3`.

331. **Hydraulic Legs landing effects happen after recoil.** `Prime_Leap` / Hydraulic Legs queues the Leap move, adjacent damage, then self-damage; live applies landing-tile effects after the recoil damage has stripped shield. If a shielded Leap Mech lands on an ACID pool, the shield can absorb self-damage first and the mech can still become ACID. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Detritus Corporate HQ turn 1, where Bethany's shielded Leap Mech jumped to E3, recoil removed shield, and the landing ACID pool applied while pre-v196 Rust consumed the pool before recoil and predicted no ACID status.

332. **Hydraulic Legs kill-pushes move ACID corpse pools.** When `Prime_Leap` / Hydraulic Legs kills an ACID target with landing-adjacent weapon damage and the same SpaceDamage pushes the corpse, live leaves the new ACID pool at the corpse's pushed destination, not on the original damage tile. Preserve any pre-existing ACID on the original tile, but move only the newly-created death pool. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Detritus Corporate HQ turn 2, where Leap jumped F3->F1, killed ACID Mosquito1 on F2, and live put the pool on F3 while pre-v197 Rust left it on F2.

333. **Instant-killed web sources still release grapples.** Web ownership is tied to the source enemy's life, not to normal weapon-damage death only. If an instant-kill path such as Terraformer, water, chasm, lava, or mine kills the Scorpion/Leaper/Webb Egg that owns `web_source_uid`, clear the webbed unit immediately. Terraformer also clears grass on ACID ground tiles without converting that ACID tile to Sand. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Terraform` turn 1, where Terraformer killed the webbing Scorpion on D4 and live unwebbed the Terraformer while an ACID ground tile on C3 lost grass but stayed Ground.

335. **ACID death pools on Sand become Ground; Unstable pushback bump kills can heal.** When an ACID unit dies and leaves an ACID pool on a Sand tile, live reports the tile as Ground with ACID, not Sand with ACID. For `Brute_Unstable`, the `PushBack` self SpaceDamage can bump-kill the blocker behind the Unstable Mech; that bump kill is credited as part of the Unstable Cannon action for Viscera Nanobots, unlike ordinary enemy edge-bump kills from Hydraulic Legs. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Terraform` turn 4, where Unstable fired E5->F5, killed the ACID Totem behind it via pushback bump, stayed at `3/3` after two Nanobots heal credits, and live left D4 as Ground+ACID while pre-v199 Rust predicted `2/3` and Sand+ACID.

337. **Occupied cracked Ground does not collapse from pawn-only weapon damage.** If ordinary weapon `SpaceDamage` kills or damages a pawn standing on a cracked Ground tile, the pawn absorbs the hit and live keeps the tile as cracked Ground. Only direct damage to the empty cracked tile opens a chasm. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Bomb` turn 1, where Unstable Cannon killed `Totem1` on cracked E5 and live left E5 cracked Ground while pre-v200 Rust predicted a chasm.

339. **Hydraulic Legs self-damage can collapse occupied cracked Ground.** The occupied-crack exception is only for ordinary weapon damage absorbed by a pawn on the tile. Self-damage applies to the mech's own tile, so `Prime_Leap` / Hydraulic Legs landing recoil on cracked Ground opens a chasm under the grounded Leap Mech and kills it. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Bomb` turn 4, where Leap jumped onto cracked E5 and live converted E5 to chasm while pre-v201 Rust left Leap alive on Ground.

340. **Unstable Cannon dead recoil still bumps live blockers before Nanobots heal.** `Brute_Unstable` / Unstable Cannon can self-damage the attacker to 0 HP before `Passive_Leech` / Viscera Nanobots revives it, but the queued recoil `PushBack` still resolves and can bump a live unit behind the tank. Model the rear blocker damage even when the attacker is temporarily disabled, then apply the direct-kill Nanobots heal to the attacker. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Crack` turn 4, where UnstableTank at C2 killed Scorpion1 on B2, live healed UnstableTank to `2/3`, and simultaneously disabled LeapMech on D2 while pre-v202 Rust left Leap alive.

341. **Nanobots cannot revive a mech that fell into a fresh chasm.** `Prime_Leap` / Hydraulic Legs can damage occupied cracked Ground adjacent to the landing tile and can also collapse cracked Ground under the Leap Mech with its self-damage. If the resulting terrain kill drops the mech into a chasm/water/lava tile, do not let `Passive_Leech` / Viscera Nanobots revive it even if the same attack killed a Vek; terrain death wins over the heal. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Survive` turn 2, where Leap jumped H3->F3, killed the Totem on cracked F2, live converted both F2/F3 to chasms and disabled Leap while pre-v203 Rust revived Leap to `2/3`.

342. **Bump damage does not open occupied cracked Ground.** A pawn standing on cracked Ground absorbs blocked-push / bump collision damage the same way it absorbs ordinary weapon damage for crack purposes: the pawn loses HP, but the occupied tile stays cracked Ground. Keep the `Prime_Leap` landing-adjacent exception separate. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Survive` turn 3, where NanoMech's Acid Projector bumped Shaman1 into Bouncer1 on cracked B3; live left Bouncer at `2/3` on cracked Ground while pre-v204 Rust opened a chasm and killed it.

343. **Unstable Cannon direct kills do not leave corpse ACID pools.** `Brute_Unstable` / Unstable Cannon still uses normal weapon damage math and Nanobots kill credit against ACID targets, but a direct weapon kill does not synthesize a new ACID pool on the target tile. Keep ordinary ACID death pools and pushed-corpse pool movement intact for other weapons. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Survive` turn 3, where UnstableTank at C4 killed ACID Shaman1 on C3 and live left C3 as plain Ground while pre-v205 Rust predicted Ground+ACID.

345. **Hydraulic Legs Nanobots revive before landing tile fire/ACID pickup.** `Prime_Leap` / Hydraulic Legs queues the Leap move, adjacent damage, and self-damage; if the kill triggers `Passive_Leech` / Viscera Nanobots, live can revive the temporarily disabled Leap Mech before the destination tile's fire/ACID landing effects apply. Keep the terrain-death block: if self-damage opens a chasm/water/lava under the mech, Nanobots still cannot revive it. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive `Mission_Artillery` turn 3, where Leap at G3 jumped to burning E3, killed Firefly1, revived from `0` to `2/5`, and then caught fire while pre-v207 Rust predicted `fire=false`.

346. **Nanobots revive clears carried negative statuses before landing reapplication.** If `Passive_Leech` / Viscera Nanobots revives a mech that was temporarily disabled by its own killing attack, live has already cleared the mech's carried death statuses such as Fire/ACID/Web before the heal returns it to play. Landing effects still apply afterward, so a revived Hydraulic Legs mech can immediately catch Fire/ACID from its destination tile. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive `Mission_Artillery` turn 4, where burning Leap at E3 jumped to non-burning E1, killed Spider2/WebbEgg1, revived to `2/5`, and live cleared Fire while pre-v208 Rust kept it burning.

347. **Unstable Cannon edge recoil does not self-bump.** `Brute_Unstable` / Unstable Cannon still applies its normal Hazardous self-damage and can still recoil-bump a live rear blocker, even when the attacker is temporarily disabled before Nanobots heal. If the recoil direction points off the board, live does not add an extra edge-bump damage event to the Unstable Mech. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive `Mission_Volatile` turn 2, where UnstableTank fired from C8 toward C7, killed Firefly1, and live ended at `5/5` after one self-damage plus boosted Nanobots while pre-v209 Rust predicted `4/5`.

348. **Dam flood extinguishes burning flooded tiles.** On `Mission_Dam`, the Old Earth Dam flood can fire during the same attack that ignites a Forest tile. If the flood converts that tile to Water, clear tile Fire; do not leave impossible `water+fire` terrain status. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive `Mission_Dam` turn 2, where Hydraulic Legs ignited E4 and destroyed the dam in the same action, but live flooded E4 as Water with `fire=false` while pre-v210 Rust predicted `fire=true`.

350. **Unstable Cannon killed targets can corpse-bump live blockers.** `Brute_Unstable` / Unstable Cannon's direct projectile damage and forward push are simultaneous. If the direct target dies from the cannon hit, its corpse still resolves the forward push into a live blocker behind it, damaging the blocker for 1 HP. Keep this separate from recoil: recoil still uses its no-edge-bump policy, but the target push uses ordinary edge bumps plus live-blocker corpse bumps. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive Corporate HQ `Mission_BlobberBoss` turn 2, where UnstableTank killed BlobB on D4 and live bumped NanoMech on D3 from `4/4` to `3/4` while pre-v211 Rust left Nano undamaged.

351. **Occupied ice absorbs weapon/bump tile breaks.** Weapon/bump-class `SpaceDamage` can damage, kill, and push a pawn standing on Ice, but live does not crack intact occupied Ice or melt occupied cracked Ice under that pawn. Keep this narrower than direct damage to empty Ice and Hydraulic Legs self-damage on the landing tile. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_Survive` turn 1, where Unstable Cannon and then Hydraulic Legs damaged/killed Mosquito1 on E5 while live kept E5 as Ice and pre-v212 Rust predicted Water.

352. **Unstable Cannon ice-origin flooding is not universal.** `Brute_Unstable` / Unstable Cannon self-damage still charges the firing mech before Nanobots healing, but do not convert every intact Ice firing tile to Water after recoil. If an Unstable origin Ice tile floods, first suspect cracked-state or another tile effect; a later intact Ice firing tile staying Ice is a counterexample to the broad rule. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_Survive` turn 3, where v213 incorrectly predicted UnstableTank's E4 firing tile would become Water, but live left E4 as Ice after recoil to F4.

353. **Hydraulic Legs breaks occupied Ice for grounded targets, not flying targets.** `Prime_Leap` / Hydraulic Legs landing-adjacent damage to a grounded pawn on Ice can leave the original occupied tile as Water after the pawn is pushed away; a flying pawn on Ice absorbs the same occupied-tile hit without cracking or melting the tile. Keep the flood after the push so the target can move off before drowning checks. Regression anchors: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_Survive` turn 3, where Beetle2 on F5 was pushed to E5 and F5 became Water; turn 1, where flying Mosquito1 on E5 was damaged/killed while E5 stayed Ice.

354. **Blocked Hydraulic Legs pushes leave occupied Ice intact.** The occupied-Ice break in rule 353 requires actual displacement from the original tile. If the grounded target dies or bumps into a blocker and stays on its starting Ice tile, live leaves that Ice intact even though the blocker takes bump damage. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_Factory` turn 4, where Leaper1 died on C3 but blocked against UnstableTank on C2, and C3 stayed Ice while pre-v215 Rust predicted Water.

356. **Hydraulic Legs friendly pushes can enter existing dead-unit tiles.** When `Prime_Leap` / Hydraulic Legs damages a friendly landing-adjacent mech and pushes it into an already-dead unit/wreck tile, live can move the friendly mech into that tile and omit the normal wreck bump. Keep this special to Hydraulic Legs friendly pushers; enemy corpse bumps into live blockers still deal normal blocker damage without moving the corpse. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_FreezeMines` turn 2, where Leaper1 was already dead on F2 and UnstableTank was later damaged on E2, shoved into F2, and ended at `2/5` while pre-v216 Rust kept Unstable at E2 `1/5`.

357. **Live broad-regression hangs can yield to focused proof.** During a parked live combat turn, if `bash scripts/regression.sh` finishes compiling but the release replay test sits for several minutes at 0% CPU with no output, stop that hung run and proceed only if focused Rust tests plus the exact failing replay on the installed wheel pass. Record in the final/status note that the broad regression was interrupted. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_FreezeMines` turn 2 v216 fix, where `regression_all_boards` slept at 0% CPU for about seven minutes after compile while the live End Turn was parked.

358. **Hydraulic Legs has no pass-over transit damage.** `Prime_Leap` / Hydraulic Legs uses vanilla `Leap_Attack:GetSkillEffect`: leap movement, landing-adjacent damage/push, then recoil. Do not synthesize Aerial Bombs/Bombing Run-style transit damage for tiles between takeoff and landing. If a long jump appears to damage a middle unit, first check landing-adjacent BombRock blasts or push collisions before adding pass-over damage. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Corporate HQ turn 1, where LeapMech jumped G3→C3, pushed Scarab2 into Blood Psion at E3, and live left the Blood Psion at `1/2`; pre-v220 Rust added phantom pass-over damage and predicted it dead.

360. **Hydraulic Legs BombRock blasts exclude the landing Leap Mech.** If `Prime_Leap` / Hydraulic Legs destroys a landing-adjacent BombRock, the BombRock still explodes and damages other adjacent units/buildings, but the Leap Mech on the landing tile does not take that blast damage. Keep ordinary BombRock explosions for other weapons unchanged. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle `Mission_SnowStorm` turn 2, where LeapMech landed on E6, detonated BombRock on E5, Dung2 on E4 took the blast, and Leap stayed at `4/5` instead of pre-v217 Rust's `3/5`.

361. **Unstable Cannon direct edge pushes do not edge-bump.** `Brute_Unstable` / Unstable Cannon still applies direct projectile damage, forward push into live blockers, killed-target corpse bumps into live blockers, and recoil/pushback rules from earlier entries, but a live target at the board edge does not take an extra off-board bump from the direct forward push. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Corporate HQ turn 1, where acid Mosquito Leader at D8 took 4 Unstable Cannon damage and survived at `1/5`; pre-v220 Rust added an edge bump and predicted it dead.

362. **Hydraulic Legs terrain-push kills can heal and leave origin Ice intact.** When `Prime_Leap` / Hydraulic Legs damages and pushes a grounded target off an Ice tile into lethal water/chasm/lava, live can kill the target at the destination, credit Viscera Nanobots to the Leap Mech, and leave the original occupied Ice tile intact. Keep rule 353 for displaced grounded targets that survive; this exception is for targets that die after displacement. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Corporate HQ turn 4, where Leap pushed Beetle1 from B2 into water, healed from `4/5` to `5/5`, and B2 stayed Ice.

364. **Unstable Cannon Nanobots heals from zero and preserves carried statuses.** If `Brute_Unstable` or its powered variants self-damage the attacker below 0 HP while the same action earns Viscera Nanobots kill credit, live treats the mech's HP as 0 before applying the heal. Unlike the Hydraulic Legs revive path, Unstable Cannon Nanobots recovery preserves carried statuses such as Fire. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Volcanic Hive turn 4, where burning UnstableTank at C5 fired `Brute_Unstable_AB` into Jelly_Lava1 on D5 and live ended at `2/5` still burning while pre-v223 Rust predicted `1/5` and Fire cleared.

365. **Acid Projector can push live enemies into dead enemy wrecks.** `Science_AcidShot` / Acid Projector applies ACID and pushes a live enemy into an existing dead enemy/wreck tile without applying the usual wreck bump damage. Keep this scoped to Acid Projector's live target push; disabled mech wrecks and other weapon pushes still use their documented blocker rules. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Final Cave turn 3, where NanoMech pushed Hornet1 into dead Scarab1's tile and live left Hornet1 alive at `1/1` while pre-v224 Rust predicted a bump kill.

366. **Rock Launcher empty rock spawns preserve Forest.** `Ranged_Rockthrow` / Rock Launcher spawns a neutral `RockThrown` on an empty, unblocked target tile without applying center terrain damage first. If the empty target is Forest, live leaves the underlying tile as unburned Forest rather than converting it to burning Ground. Keep occupied and preblocked target behavior on the ordinary center-hit path. Regression anchor: Lightning War run `20260610_184414_692`, Archive `Mission_Mines` turn 1, where RockartMech fired at empty E3 Forest, spawned `RockThrown`, and live kept E3 as Forest with `fire=false`; fixed in simulator v260.

367. **Bomb Dispenser is a line-artillery deploy, and Powered Blast credits the second AP hit.** `Ranged_DeployBomb` / Bomb Dispenser uses LineArtillery-style cardinal targeting with minimum range 2, empty ground destinations, and range_max `0` treated as board-range. It spawns `DeployUnit_Bomby`, whose modeled default weapon is `DeployUnit_SelfDamage`; unused Walking Bombs are temporary deployables that should not be counted as permanent mech deaths when they dismantle. `Ranged_DeployBomb_A` / 2 Bombs is a true two-click deploy: the first and second clicks are empty ground line-artillery tiles in different directions from the Bombling Mech, and the bridge must execute it through `TWO_CLICK_ATTACK`. `Brute_PierceShot` / AP Cannon pushes through the first target without damaging it, then damages/pushes the second target; for **Powered Blast**, the first target must be a live Walking Bomb and the second hit must kill an enemy. Simulator v263 added the base deployable and trigger support; v264 added `achievement_powered_blast` event/scoring; v311 added `Ranged_DeployBomb_A` save overlay, target enumeration, bridge execution, and double-spawn simulation. Regression anchors: Bombermechs Easy run `20260611_202233_019`, Archive `Mission_Artillery` turn 4, where the bridge spawned a Walking Bomb at C2, a re-solve moved Pierce Mech to D2, and AP Cannon at C2 killed Scarab1 at B2 to unlock **Powered Blast**; Bombermechs No Survivors run `20260630_181556_177`, R.S.T. island 3 after powering 2 Bombs.

368. **Enemy attacks use live bridge order, not UID sort.** `attack_order` must preserve the live unit-list order emitted by the bridge. Do not sort queued enemies by UID before enemy-phase replay: Pinnacle `Mission_Factory` can have a `Snowlaser1` fire before a lower-UID `Burnbug1` kills it, damaging a mech/building that UID order would incorrectly spare. Regression anchor: Bombermechs Complete Victory run `20260623_035936_734`, `Mission_Factory` turn 2; fixed in simulator v273.

369. **Enemy-phase pre-attack deaths clear before later enemy-phase steps.** Vek killed by start-of-enemy-turn fire, smoke storm, environment, conveyor, or wind effects should not remain as wreck blockers for subsequent enemy-phase pushes or attacks. Keep this separate from player weapon resolution, where simultaneous killed-target corpse pushes and wreck blockers have many weapon-specific rules. Regression anchor: Stay With Me run `20260616_083357_196`, R.S.T. `Mission_Wind` turn 3, where a burning Scorpion at C3 died before attacks; live then let Wind Storm push Firefly1 into a line that destroyed C5, while pre-v276 Rust left the fire-killed Scorpion corpse blocking the gust.

370. **AP Cannon second-target corpses can bump and acid the first target's landing tile.** `Brute_PierceShot` / AP Cannon still skips edge-bump damage for its direct second-target push, but if the second target dies and a live unit stands directly behind it, the killed second target's corpse applies a 1-damage bump to that blocker. If that killed second target had ACID, the death pool remains on the wreck tile and a friendly first target pushed into that tile picks up and consumes the pool. Regression anchor: Hold the Door run `20260629_190949_968`, Detritus `Mission_Acid` / Downtown turn 4, where AP Cannon fired through BomlingMech into an ACID Burnbug; live killed ExchangeMech via corpse bump and left BomlingMech ACID after entering the Burnbug tile.

371. **AP Cannon friendly first targets can collide with adjacent live second targets.** When `Brute_PierceShot` / AP Cannon fires through a friendly first target and the second target is immediately behind it, if the second target survives AP damage, live resolves the friendly first-target push into the still-occupied second tile before the second target vacates. The collision deals normal bump damage to both units and can kill a damaged friendly first target. Keep killed-second-target behavior under rule 370. Regression anchor: Hold the Door run `20260629_190949_968`, Detritus `Mission_Survive` / Disposal Site C turn 3, where PierceMech fired F5->E5 through 1 HP ExchangeMech into a 3 HP Burnbug at D5; live killed ExchangeMech and the Burnbug while pre-v289 Rust predicted Exchange survived.

372. **Mission_Filler Earth Mover fails if webbed.** `Mission_Filler` / Earth Mover protection is not just "Filler_Pawn survives"; a live but webbed Earth Mover can fail the visible `Defend the Earth Mover` objective because it cannot finish filling the chasm. Track `protected_objective_units_webbed` for `Mission_Filler` and treat any predicted live-webbed Earth Mover as an objective-loss safety block. Regression anchor: Cataclysm Core of the Earth run `20260630_143648_199`, R.S.T. `Mission_Filler` / Tectonic Site 3.1, where Filler_Pawn stayed alive at `2/2` but webbed through the final turns and the Region Secured screen reported `Defend the Earth Mover (Failed)`.

373. **Spawned Arachnoid Bite killed targets can corpse-bump live blockers.** `DeployUnit_AracnoidAtk` / Arachnoid Bite is a 1-damage melee hit with forward push. If the bite kills the target, live still resolves that killed target's forward corpse push into a live blocker behind it; if that blocker is a `BombRock`, the boulder detonates and damages adjacent units/buildings. Keep this scoped to spawned Arachnoid Bite policy rather than changing default melee corpse absorption. Regression anchor: Arachnophiles Efficient Explosives run `20260702_162407_779`, Pinnacle `Mission_SnowStorm` turn 1, where an Arachnoid at E3 bit the D3 egg sack, live bumped/detonated the C3 BombRock, damaged Dung2 at C2 and building C4, while pre-v320 Rust left the BombRock alive and C4 undamaged.

374. **Quick-Fire Rockets are true two-click projectiles; bridge execution is board-only.** `Brute_TC_DoubleShot*` selects two cardinal targets in different directions from the Quick-Fire Mech. Each projectile starts at the source, stops at its first blocker, applies `Damage` to that blocker, and applies `Push` for the `_A` / `_AB` variants; Boost raises each projectile's direct damage by 1 and must be consumed/preserved/granted like other custom bridge paths. Do not execute Quick-Fire through `Board:AddEffect(skill:GetFinalEffect(...))` in the file-bridge context: it can ACK and spend the action while leaving both targets unchanged. For ordinary combat, the bridge should synthesize two `DamageSpace` calls from `GetProjectileEnd`, then manually update Boost state. That synthetic path reproduces board damage but bypasses the engine's single-activation achievement bookkeeping, so it cannot be used for the achievement-bearing **Maximum Firepower** volley. When that target is active and the solver predicts at least 8 actual enemy HP removed, execute and verify any setup actions, then perform the final move, weapon arm, and both target clicks through the calibrated visible UI as one native activation. Overkill on low-HP enemies does not count toward the required 8 damage. Regression anchors: Heat Sinkers Maximum Firepower run `20260703_125151_272`, R.S.T. `Mission_Train` turn 1, where native `GetFinalEffect` for `Brute_TC_DoubleShot` no-opped and desynced until simulator v324 plus the synthetic bridge executor; run `20260705_021942_077`, Archive `Mission_Repair` turn 4, where the synthetic 8-damage event did not unlock the achievement, but restoring the turn and firing the same solver line through the visible two-click UI produced the popup and persisted `Squad_Heat_3` in the local profile.

375. **Fire ticks on occupied Ice do not preempt queued enemy attacks.** Start-of-enemy-turn Fire damage to a pawn standing on Ice damages the pawn, but the occupied tile does not melt before that pawn's queued attack resolves. If a burning Bouncer survives the Fire HP tick and only dies from its later recoil into water, it still hits the queued target first. Regression anchor: Heat Sinkers Maximum Firepower run `20260703_125151_272`, Pinnacle `Mission_Shields` turn 3, where a 2 HP burning Alpha Bouncer on B3 still destroyed the B4 Defense Lab before drowning from recoil into B2 water; pre-v325 Rust melted occupied Ice during the fire tick and falsely predicted the building safe.
376. **Shell Psion armor persists through both Quick-Fire Rockets.** Quick-Fire Rockets are one two-click activation; if the first rocket kills `Jelly_Armor1`, Shell Psion armor still reduces the second rocket during that same activation, then the aura clears afterward. Regression anchor: Heat Sinkers Maximum Firepower run `20260703_125151_272`, Pinnacle HQ `Mission_ShamanBoss` turn 2, where the first Quick-Fire rocket killed `Jelly_Armor1` but the second rocket dealt only 1 damage to `Centipede2`; pre-v326 Rust cleared the aura between rockets and overpredicted damage.

403. **Health Psion HP persists through both Quick-Fire Rockets.** Quick-Fire Rockets are one two-click activation; if the first rocket kills `Jelly_Health1` or `Jelly_Boss`, affected Vek keep the +1 HP aura through the second rocket and only tear down after that second rocket resolves. This can change whether the second target survives direct damage long enough to bump a blocker before dying from the final aura collapse. Regression anchor: Heat Sinkers Maximum Firepower run `20260703_125151_272`, Detritus `Mission_Crack` turn 4, where boosted Add Push Quick-Fire killed `Jelly_Health1` with the first rocket, and live `Mosquito1` still bumped Napalm Mech during the second rocket before dying; pre-v327 Rust collapsed the aura between rockets and missed the mech damage.
