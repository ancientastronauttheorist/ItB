# Agent Live Runbook

This is the operational runbook for running Into the Breach through the bridge and Codex UI tooling. The top-level `AGENTS.md` keeps the rules that must be remembered every turn; this file carries phase flow, command semantics, UI caveats, and live-process guardrails.

## Execution Model

**Default combat mode: `auto_turn`.** A single `game_loop.py auto_turn` call reads board, solves, executes every mech action via bridge (move → verify → attack → verify, re-solving on desync), and emits an End Turn click plan. It *polls the bridge internally* for the enemy→player transition (up to 45s by default) at entry, waiting for both `phase == combat_player` **and** `active_mechs > 0` — the animation window after End Turn flips phase early but leaves mechs inactive, so checking phase alone falsely proceeds into "No active mechs" errors.

**Typical turn = 2 agent rounds:** `auto_turn` → click End Turn → `auto_turn` (blocks in Python until next player turn) → click End Turn → ...

**Fallback manual play** (`click_action <i>` / `verify_action <i>` / `click_end_turn`) exists for when the bridge is unavailable — don't use it otherwise.

**Pause-menu heartbeat rule.** The Lua bridge heartbeat is written from mission
`BaseUpdate`. A real pause menu can suspend `BaseUpdate`, so the heartbeat will
go stale after about five seconds while paused. This is normal and safe when the
pause menu is visibly open; do not treat it as a dead bridge by itself. The
live-burst boundary is the important part: after clicking Continue or toggling
Escape out of pause, wait until the heartbeat is fresh again before `read`,
`solve`, `deploy_recommended`, `auto_turn`, or any bridge combat command. If it
does not become fresh after unpause, or if a bridge command reports stale
heartbeat while the game should be ticking, recover from a fresh read plus
solve and do not reuse any old plan.

**Coordinate mapping:**
- Bridge `(x, y)` → visual: `Row = 8 - x`, `Col = chr(72 - y)`. Example: bridge `(3, 5)` = `C5`. Always use A1–H8 visual notation in communication.
- MCP pixel coords: `grid_to_mcp(x, y)` (in `src/control/executor.py`) or `python3 tile_hover.py <TILE>`. Both auto-detect window position via Quartz on macOS or Win32 APIs on Windows.
- Island-select click coords: `python3 island_select.py`.
- **End Turn button: `python3 game_loop.py click_end_turn`** — emits both legacy screen/global coords and `codex_computer_use_batch` window-local coords for Codex Computer Use. In Codex Computer Use, dispatch `codex_computer_use_batch` (or the per-click `window_x`/`window_y`), not the legacy `x`/`y`. The calibrated End Turn offset is `(126, 120)` window-relative.
- If Codex Computer Use is unavailable, use the emitted legacy screen coordinates only after activating the Into the Breach window and doing a fresh `read`. On macOS, a minimal fallback is `osascript -e 'tell application "Into the Breach" to activate'`; on Windows, bring the HWND titled `Into the Breach` foreground with Win32 APIs. Then use a short `pyautogui` click on the current `click_end_turn` screen coordinate, wait, and read again. Do not repeat a fallback End Turn click just because the save parser says `mission_ending`; compare the visible game, bridge `phase`, active mechs, and countdown first.

## Phase Protocols

Each phase: read → act → verify. Detailed command semantics are in **Game Loop Command Reference** below.

- **NEW_GAME_SETUP** (very start of a new run, before ISLAND_SELECT): Run `python3 game_loop.py achievements --sync` if Steam env keys are present, then `python3 game_loop.py recommend_squad --tags achievement` or initialize with `python3 game_loop.py new_run auto --tags achievement`. On the new-game screen, set the target difficulty (**Hard** / `--difficulty 2` for Hard Victory; **Easy** / default for ordinary squad achievements), ensure **Advanced Edition** content is toggled ON, then run `python3 game_loop.py verify_setup --difficulty <target>` and require `status: PASS` before pressing the setup **Start** button. Select the recommended actual squad card. Use **Balanced Roll** only when `recommend_squad` / `new_run --mode solver_eval` / a random-squad achievement explicitly says to. See `feedback_playstyle.md`.
- **ISLAND_SELECT** (after the CEO intro): `python3 island_select.py` picks one of 4 corporations. Click its coord. Click through CEO intro. → ISLAND_MAP.
- **ISLAND_MAP:** `game_loop.py read` → pick mission (prefer bonus objectives for the achievement target) → click mission on map → mission briefing → click preview or start button → DEPLOYMENT. On the island map, red regions are the selectable frontier missions and green regions are already secured/completed; choose from red regions, not green ones, even if the bridge/save metadata is stale or sparse.
- **DEPLOYMENT:** `game_loop.py deploy_recommended` → verify PASS for all placed mechs → click visible CONFIRM. If bridge deployment is unavailable, use `game_loop.py read` MCP coords and click 3 recommended tiles manually, then CONFIRM. → COMBAT_PLAYER_TURN.
- **COMBAT_PLAYER_TURN:** `auto_turn --time-limit 10` → dispatch the emitted End Turn click plan or run `click_end_turn` → loop. The next `auto_turn` blocks in Python until the next player turn or mission end. On game_over or empty solution, falls back to screenshot-based reasoning.
- **MISSION_END:** screenshot reward screen → click reward → `snapshot <label>` → ISLAND_MAP (or ISLAND_COMPLETE if all missions done). If the screen visibly shows **Region Secured**, a promotion popup, a reward panel, or the island map while `auto_turn` / `read` keeps returning the previous `combat_player` / `combat_enemy` board from a later turn, treat the bridge as stale and follow this MISSION_END flow; do not keep polling or try another combat command. `cmd_read` cross-checks `saveData.lua` and will now ignore that stale combat bridge board once the save phase is `between_missions` or `mission_ending`; the Lua bridge also dumps `phase="unknown"` immediately on `MissionEnd` so future mission endings don't leave combat JSON behind.
- **SHOP:** appears *only after winning a whole island* (4 missions + finale), not mid-island. Do NOT expect a shop between missions — grid repair opportunities come from time-pod rewards and island completions, full stop. Screenshot when it appears (neither save nor bridge distinguish shop from map). **GRID-FIRST RULE: if Grid Power is below max, buy Grid Power until it reaches 7/7 before considering anything else.** If you accidentally buy a core/weapon/pilot while Grid Power is still below max, click **Undo All** immediately and repurchase Grid Power first. Verify the post-buy state from `game_loop.py status` or another reliable non-visual read before leaving with unspent reputation; do not rely on visually counting the bar pips. If reputation remains and grid is still below max, buy another Grid Power before cores/weapons or leaving the island. Once the grid is full, the same slot becomes Overpower / +Grid Defense; that is optional, not mandatory, unless the current achievement target explicitly wants Grid Defense or full-grid overpower purchases. With a full grid, shop normally: prefer reactor cores, solver-supported weapons/passives, useful pilots, and achievement-relevant buys. Rationale: `feedback_grid_buy_priority.md`. → ISLAND_MAP (next island) or RUN_END.
- **RUN_END:** `snapshot run_end`. If defeat, analyze critical turns from decision log. Check achievement progress. Start new run with next target.

### Error Recovery

- **Unexpected screen:** screenshot, log to decision log, diagnose visually.
- **State not updating:** `refresh_bridge_state`. If bridge dead, retry save-file verify up to 5× with 1.5s delay.
- **Stale heartbeat while paused:** expected. Keep using pause as the safe
  thinking surface. Before live bridge work, unpause and wait for the heartbeat
  to refresh; only escalate if it stays stale after unpause.
- **Windows state-file fallback:** the ModLoader can leave the freshest valid bridge payload in `itb_state.json.tmp` while `itb_state.json` is stale. `src/bridge/protocol.py` reads the newest valid state candidate. When the visible screen contradicts a combat read on Windows, inspect the screen and avoid resuming an old solution until a fresh bridge/read agrees.
- **Solve/auto_turn stall:** inspect the latest `recordings/<run_id>/mNN_turn_NN_solve_input.json` first. That file captures the exact Rust payload before solving starts, so reproduce with `itb_solver.solve` / `solve_top_k` / `solve_beam` from it. Do not rely on the next `*_board.json` after a stall; live recovery may have already executed actions or advanced animations, making that board post-action or stale.
- **Grid power = 0:** log, snapshot, analyze.
- **Crash/timeout:** fresh `cmd_read` + `cmd_solve`. Never resume an old solution.
- **Lightning startup helper crash:** `lightning_autonomous` reports
  `title_new_game_exception`, `classify_after_title_new_game_exception`,
  `setup_start_exception`, `setup_verification_exception`, or
  `setup_click_exception`, or `lightning_start_run_exception` instead of
  continuing from uncertain setup state. Preserve the traceback/window
  evidence, regain a visible title/setup screen, and rerun visual setup proof
  before starting or accepting a timeline.
- **Lightning runner helper crash:** `pause_guard_initial_exception`,
  `achievement_sync_exception`, `preflight_exception`, or
  `lightning_segment_exception` is a named stop, not permission to continue.
  Inspect the traceback plus any `command_span` exception event; after segment
  crashes, recover only from a fresh read plus solve or fresh setup proof.
- **Lightning panel evidence crash:** `screen_classification_exception`,
  `visible_panel_handle_exception`,
  `mission_preview_dialogue_clear_exception`,
  `mission_preview_dialogue_post_classify_exception`, or nested
  `resume_paused_segment_panel_exception` / `ensure_pause_exception` means
  CV/UI evidence failed around a panel click. Preserve the traceback/window
  evidence and re-establish a verified pause, panel, or map state before
  continuing.
- **Lightning shop/leave helper crash:** nested `*_exception` shop reasons keep
  `exception_evidence`, grid state, and ordered shop `steps`. Do not click
  Leave, Confirm, or route again until the shop/handoff evidence is understood.
- **Lightning session read crash:** `session_load_exception` means the runner
  could not read active session metadata. Inspect the `stage` and traceback;
  do not trust stale island progress, pace gates, or post-leave handoff state
  until the session read succeeds again.
- **Lightning evidence startup crash:** `telemetry_start_exception` or
  `screenshot_start_exception` means the runner stopped before taking live
  actions because its evidence trail could not be initialized. Fix the recorder
  path/capture problem, or rerun with a consciously weaker evidence setup.
- **Lightning telemetry rehome crash:** `telemetry_rehome_exception` means a new
  Lightning timeline began but recordings could not move to the new run
  directory. The runner keeps the previous telemetry active; reconcile
  screenshots/manifests from that event before using the logs as timing proof.

## Game Loop Command Reference

All commands are `game_loop.py <name> [args]`. Each is stateless: read state, compute, output, exit. Session state persists in `sessions/active_session.json`.

**State reading:**
- `read` — Bridge state (primary) or save file (fallback). Prints phase, board, threats, active mechs, deployment zone with MCP coords, env hazards. Auto-records to `recordings/<run_id>/`.
- `status` — Quick summary: turn, grid, mech HP, threats, objectives; also surfaces any persistent `post_enemy_block`.
- `verify_action <index>` — Per-action diff: refreshes bridge, diffs actual vs the predicted snapshot captured during `replay_solution`, classifies (click_miss / death / damage_amount / push_dir / grid_power / status / terrain / tile_status / repair_platform / spawn / pod), writes desync to `recordings/<run_id>/failure_db.jsonl`. Never re-solves.
- `verify [index]` — Legacy save-parser-based path (retries 5×, 1.5s each). Superseded by `verify_action` in bridge mode.

**Solving & recording:**
- `solve [--candidate-rank N]` — Runs the solver, stores solution in session, prints action sequence. Recording includes structured actions, per-action `ActionResult`, per-action `predicted_states` (input to `verify_action`), predicted post-enemy board, score breakdown, search stats. If the selected plan is safety-blocked, the JSON includes the exact `dirty_consent_id` needed for a reviewed dirty run.
- Recordings from `read`/`solve` land in `recordings/<run_id>/m<NN>_turn_<NN>_<label>.json`. Dedup guard prevents duplicate `(mission, turn)` pairs.

**Combat execution:**
- `auto_turn [--time-limit N] [--no-wait] [--max-wait S] [--candidate-rank N] [--allow-dirty-plan --dirty-consent-id ID]` — Full turn via bridge with per-sub-action verification. Polls at entry for `combat_player` phase **and** `active_mechs > 0` (up to `--max-wait` seconds, default 45; disable with `--no-wait`). Returns an MCP click plan for End Turn only after safety gates and the pre-End-Turn threat audit pass. On desync, re-solves from actual board with partial mech states (DONE = inactive, MID_ACTION = can_move=false, ACTIVE = full search).
- `click_action <i>` — Pure planner for manual play. Emits a `computer_batch`-ready sequence for ONE mech action (select-tile, optional move, weapon icon, target). Handles dash weapons (skip move click), Repair (click Repair button), passives (no-op).
- `click_end_turn` — Pure planner. Emits a single click on End Turn.
- `recommend_squad [squad|auto] [--achieve ...] [--mode achievement_hunt|solver_eval|random_squad|custom]` — Pure strategist. Uses `data/achievements_detailed.json` to choose a named squad for achievement hunting, or Balanced Roll for solver-eval/random-squad runs.
- `verify_setup [--difficulty N]` — Visual setup guard for the Difficulty Setup overlay. Captures the game window, verifies the selected difficulty and Advanced Content target, reports `window_focus_verified`, and emits window-local clicks for any mismatched rows before the final setup **Start**. If the Into the Breach window bounds or frontmost status cannot be proven, it fails closed with no click plan.
- In Lightning automation, `setup_start` opens the Difficulty Setup overlay; the
  final `setup_modal_start` that begins the timeline is clicked only inside
  `lightning_start_run` after `verify_setup` passes.
- `click_balanced_roll` — Pure planner. Emits a single click on the Balanced Roll button on the squad-select screen. Dispatch before clicking Start only when the setup recommendation says Balanced Roll.
- `deploy_recommended` — Bridge deployment helper. Sends `DEPLOY uid x y` for the ranked recommended tiles, verifies placed mech coordinates, and leaves only the visible CONFIRM click for Computer Use.
- `execute <index>` / `end_turn` — Bridge-mode action commands. Used internally by `auto_turn`. In manual play, use `click_action` / `click_end_turn` instead. These refuse to run while a persistent post-enemy block exists.
- `research_peek [--limit N]` — Read-only view of the pending research queue. Use this instead of the nonexistent `research_queue_peek` command.
- `resolve_post_enemy_block --reason "<specific cause/fix>"` — Clears a persistent post-enemy investigation block after the miss has been understood and recorded.

**Full-mission automation:**
- `auto_mission [--max-turns N]` — Full mission: auto-deploy → combat loop → mission end. Final turn is force-flushed to failure_db on exit. Falls back to the agent for reward/shop/map screens.
- `lightning_autonomous [--mode baseline|speed] [--target-islands 2] [--max-attempts N]` — Top-level Lightning War runner. Baseline mode starts from verified title/setup/in-run states, verifies setup before Start, routes automatically, keeps combat inside `lightning_segment`, intercepts island-complete panels for grid-first shopping, and stops on unresolved safety/research/desync/terminal gates. The runner pauses on rest/UI screens, but combat bursts run unpaused to keep the Lua heartbeat fresh. Baseline ignores speed-only route vetoes after mission-id proof so the two-island reliability path can finish slow-but-playable missions; speed mode keeps the vetoes. Pre-combat route-auto-start refusals, including preview-only vetoes after a route click but before Start Mission, can abandon back to verified setup and retry up to `--max-attempts`; generic combat safety gates still stop with evidence. Unlabeled visual route candidates may be probed once through the live bridge preview, but Start Mission is clicked only when the preview mission id is verified. Visible preview OCR may identify known vetoes such as `Defend the Tanks`; speed mode treats OCR as veto-only evidence, while baseline may use matching non-vetoed OCR as Start authority only with an expected mission id or save-backed selected region plus post-start mission proof. Speed mode uses the same state machine with the Lightning War speed-loss policy and a 2-second default combat budget. See `docs/agent/lightning-war-runner.md`.
- Treat the Lightning War runner as live-unproven until a baseline command
  reaches `target_islands_completed` from the main menu or verified setup state
  with the final OCR-backed success proof. The proof must show a known reward,
  island-complete, or verified island-map screen; if pause covers the game, the
  runner uses an OCR `lightning_peek` before accepting success. Ambiguous
  `island_map_or_unknown` evidence still blocks. Repo tests do not replace that
  live two-island evidence.
- `lightning_segment [--time-limit 2] [--max-turns N] [--max-steps N]` — Lightning War start-to-start conductor. It loops `lightning_attempt`, runs preflight only once, keeps exact dirty consent pending until an attempt actually enters combat, clears safe reward/pod panels and mission-preview dialogue text, and stops on the next route-ready island map or hard/unclear gate. Defaults are optimized for the achievement: resume-if-paused, auto-clear-safe-panels, quiet nested output, pause-on-stop, and Lightning War speed-loss policy. Use this from a paused mission preview, deployment, combat, reward, or map tail when the goal is "get to the next Start Mission click" with minimal UI handling.
- `lightning_loop [--time-limit 2] [--max-turns N] [--speed-loss-policy]` — Lightning War hot path: repeatedly runs `auto_turn`, uses the verified local End Turn click, and immediately lets the next `auto_turn` wait through enemy animations. If the bridge does not observe the End Turn transition quickly, it stops instead of burning the full wait window. It refuses unresolved research queues, persistent post-enemy blocks, Hold the Line targeting, and all normal safety/investigation gates. Use from a clean unpaused player-turn state; pause only for route decisions, rewards/shops, dirty review, research, or unexpected visuals. The optional speed-loss policy is target-scoped to Lightning War and still blocks timeline collapse, mech losses, and uncertain boards.
- `lightning_attempt [--time-limit 2] [--max-turns N] [--speed-loss-policy]` — Lightning War conductor. It does preflight, reads a compact bridge snapshot, deploys with `deploy_recommended` plus the calibrated local CONFIRM click when on deployment, then hands combat to `lightning_loop`. On island-map screens it runs `recommend_mission --routing lightning_war` and stops for visual route selection; on unknown screens it stops instead of guessing. A visible Start Mission preview board is clicked only when it matches an expected route mission from the route-start path; no-bridge previews and deployment-looking previews without that expected mission block with `mission_preview_requires_route_validation`. CLI use defaults to resume-if-paused, auto-clear-safe-panels, quiet nested output, and `pause_on_stop`: when the pause menu is visible it clicks Continue first, reward/promotion/perfect/bottom Continue panels are cleared automatically, chained Region Secured/Pod Recovered panels are reclassified after each click, then it conservatively tries `lightning_ui ensure_pause` so the pause menu becomes the resting/thinking state before the large result prints. Its budget guard reads the save/profile `["current"]["time"]` game clock in milliseconds and stops at 30:00; ignore profile `["timer"]`, which is not the achievement clock. Add `--speed-loss-policy` when using this lower-level command for the same optional-objective/grid/building speed tradeoff that `lightning_segment` enables by default, `--no-auto-clear-panels` when auditing panel handling, `--no-resume-if-paused` only when inspecting a paused screen, `--verbose` only for diagnosis, `--no-pause-on-stop` only when the next action must stay live, `--dry-run` to see the next action without clicking, `--no-click` to emit only the action plan, and `--max-wall-seconds N` as an extra conservative restart guard. It does not request bridge fast mode from preflight; fast mode is set only when entering combat so reward/map panels do not pay a bridge ACK timeout.
- `lightning_segment [--route-visual-region-index N] [--route-start-mode MODE]` — Higher-level start-to-next-decision conductor. With a route index selected from `lightning_attempt` / `lightning_route_start` / `lightning_map_regions` output, it can start that red region once, continue through deployment/combat/post-mission tails, and stop at the next route decision. With `--route-auto-start`, an unlabeled visual index is first treated as a preview probe: if the live bridge preview is missing, unverified, or vetoed, the segment stops at `route_auto_start_not_allowed` before the Start click. `--baseline-route-policy` ignores speed-only route vetoes after mission-id proof; the default `--speed-route-vetoes` keeps them. When the bridge is absent but visible preview OCR matches a known veto phrase, the segment records `visible_preview_ocr` and treats it as a veto-only pre-Start block. Baseline may accept matching, non-vetoed visible OCR when an expected mission id was supplied, and may accept silent/unknown OCR only when a save-backed selected visual region supplies a non-vetoed expected mission; either path must still prove a fresh deployment/combat transition after the commit. A safe bridge-preview mission id with multiple visible red regions is still validated through `lightning_route_start` as an exact expected mission before Start. Preview-only same-island retries from an already-open mission preview must block and return to the outer restart path; do not click through the preview overlay to probe another region. Route-ready results expose `primary_next_command` for the fastest handoff; route candidate JSON uses this as the primary `command`; `route_start_command` is the narrower start-only fallback. Use `--route-start-mode dialogue-region-repeat-preview-board-twice` for R.S.T.-style dialogue/preview loops when the screenshot evidence shows that pattern; the index is consumed once and is not reused for the following map.
- `lightning_autonomous` defaults to 8 safe timeline attempts. This budget exists
  for RNG maps where every visible candidate is proven unsafe before Start
  Mission, for example OCR-vetoed `Mission_Tanks` previews; it is not permission
  to restart after combat, desync, or any non-preview stop sign.
- `lightning_autonomous` stops with `route_gate_restart_failed` if a safe
  pre-combat route-gate retry cannot abandon back to setup, restart, or
  re-verify the new timeline. Inspect nested `restart` evidence first; an
  `abandon_to_setup_exception` means the window/setup state is uncertain and
  must be visually re-established before any more clicks.
- `lightning_autonomous` stops with `stale_bridge_heartbeat` when a combat burst
  reports `Bridge heartbeat stale` / `Lua stopped ticking`, especially after a
  partial action. Inspect `heartbeat_evidence.path`, recover the bridge, then
  resume only from a fresh read plus solve; do not reuse the old partial-turn
  plan.
- `lightning_autonomous` stops with `combat_desync` when nested combat evidence
  reports `DESYNC`. It preserves `stop_evidence.path` and does not pause-click
  or route-restart from that uncertain board; recover from a fresh read plus
  solve before any more combat commands or End Turn clicks.
- `lightning_autonomous` stops with `reload_or_main_menu_visible` if title/setup
  appears during an active run. Treat this as reload/crash/main-menu evidence:
  do not trust stale session progress, resume existing combat only after a fresh
  read plus solve, and restart only after fresh setup verification.
- `lightning_autonomous` stops with
  `post_leave_handoff_ambiguous_before_target` when an island-exit handoff is
  only `island_map_or_unknown` and more islands remain. Inspect the screenshot
  and regain a verified next-island map, bottom-continue, or pause state before
  routing.
- `lightning_autonomous` promotes stop-sign tokens to named results:
  `research_required`, `post_enemy_blocked`, `threat_audit_blocked`,
  `safety_blocked`, or `investigation_required`. Inspect `stop_evidence.path`
  / `preflight.stop_evidence.path` when present and follow that protocol before
  any more combat commands or End Turn clicks.
- `lightning_autonomous` stops with `segment_failed` if `lightning_segment`
  returns a tokenless top-level `ERROR`/`BLOCKED` result. Inspect
  `segment_failure` and the compact segment evidence; do not let the same helper
  failure repeat into a no-progress loop.
- `lightning_autonomous` stops with `external_system_prompt_visible` when macOS
  displays a privacy/system prompt over the game. Do not click `Allow` or dismiss
  it without explicit user authorization; inspect `external_prompt_evidence.path`
  when present, and after the prompt is handled, resume from the current visible
  game screen.
- For suspicious reward/continue/leave panels, use
  `python3 game_loop.py lightning_ui classify --include-ocr`. The autonomous
  runner requests this OCR-backed evidence at terminal-prone panel, handoff, and
  success-proof classify points and blocks on KIA, timeline-lost, or failed
  objective text. `lightning_ui handle_screen` and `clear_tail_pause` also
  refuse to clear panels when explicit terminal/OCR evidence is present.
- `lightning_abandon_to_setup [--reason text]` — Calibrated Lightning War recovery from a verified pause-safe state. It clicks Abandon Timeline, confirms, selects the carry-forward pilot, requires the final visible UI to classify as `new_game_setup`, and clears stale bridge state/command files from the abandoned timeline.
- `lightning_start_run` uses a single first-island click after the setup modal.
  Never double-click the same island coordinate as a confirmation; after the
  corp map opens that coordinate can select a mission region. Its initial
  segment is baseline-safe by default: Easy difficulty, Advanced Content OFF,
  no speed-loss policy, and no objective-loss consent. Use
  `--speed-loss-policy` only for the explicit Lightning War speed-loss mode,
  and `--allow-objective-loss` only for reviewed exact dirty objective-loss
  consent.
- `lightning_ui [control]` — Calibrated local held-clicks for known Lightning War UI buttons: `pause`, `menu_continue`, `title_continue`, `reward_continue`, `bottom_continue`, `pod_open_door`, `dialogue_textbox`, `mission_preview_board`, `deploy_confirm`, `deploy_slot_0`, `deploy_slot_1`, `deploy_slot_2`, `modal_understood`, `panel_continue`, `perfect_reward_grid`, `spend_reputation`, `shop_grid_power`, `shop_continue`, `leave_island`, `leave_confirm_yes`, `island_archive`, `island_rst`, `island_pinnacle`, `island_detritus`, and `end_turn`. Use `--dry-run` to verify the window-local coordinate first, `--list` to print the full table, or `lightning_ui classify` to screenshot-classify visible non-combat panels before trusting bridge `island_map`. Special speed controls: `lightning_ui ensure_pause` clicks pause only when no blocking panel is covering the screen; `lightning_ui handle_screen` clears obvious reward/promotion/perfect/bottom-continue/pod panels and dismisses mission-preview dialogue text boxes, but Start Mission boards still require route proof; `lightning_ui start_visible_mission` clicks the detected yellow Start Mission text when the preview board shifted away from the static coordinate; `lightning_ui start_visible_dialogue` dismisses CEO dialogue first, then uses the visual detector; `lightning_ui guard_status` reads the last pause-resting-state note. Named bursts include `to_archive`, `to_rst`, `leave_confirmed`, `start_from_dialogue`, `resume_start_mission`, and first-island handoffs `first_island_to_rst_pause`, `first_island_perfect_grid_to_rst_pause`, and `first_island_perfect_full_to_rst_pause`; explicit ad-hoc hurry sequences can still join controls with `+` or `,`, for example `lightning_ui menu_continue+rst`. `title_continue` resumes from the title screen after a game restart; `bottom_continue` is the bottom-right panel button used by CEO/island intro screens; `pod_open_door` opens a recovered Time Pod before the contents Continue appears; `dialogue_textbox` dismisses no-button advisor text boxes; `mission_preview_board` starts the selected mission from the large preview board when it is in the calibrated position.
- `lightning_capture <label> --game-timer M --clock-state ticks|paused --note ...` — Capture the game window and append a timing note under `run_notes/lightning_war_smoke_<date>/`. Use this during smoke tests and after surprising clock movement.
- `lightning_autonomous --mode speed` writes `speed_phase_timing` telemetry rows
  for phase/island/mission timing and compact combat-turn totals. Use those rows
  for speed tuning after a safe baseline, not as permission to bypass stop
  signs. Expected phase labels are `between_missions`, `route`, `mission`,
  `reward_shop`, and `target_complete`.
- `lightning_peek [label] [--include-ocr]` — Visual-only micro-peek for ambiguous paused states. It requires the pause menu by default, precomputes the screenshot path/window bounds while paused, clicks `menu_continue`, captures one persistent screenshot, immediately clicks `pause`, classifies the captured evidence, and returns the screenshot path for paused analysis. With `--include-ocr`, terminal-prone revealed panels also carry OCR text snippets. It never clicks End Turn, route/start/shop controls, combat actions, or bridge commands. If the final pause menu is not verified, treat the result as blocked before any follow-up command.
- `lightning_map_regions --screenshot-path PATH [--start-mode MODE] [--target-name NAME]` — Extract red island-map region centers from a paused peek screenshot and emit route candidate commands. The primary candidate `command` is `lightning_segment --route-visual-region-index ...`; `route_start_command` and `coordinate_command` are fallbacks. Use `--start-mode dialogue-region-repeat-preview-board-twice` on R.S.T.-style preview/dialogue loops when the screenshot evidence suggests the ordinary preview-board start will not commit. When `recommend_mission` identifies a save-region name, pass it as `--target-name` so each candidate carries a visual matching hint.
- `lightning_preflight [--set-bridge-fast]` — Read-only speed-run blocker check except for the optional bridge fast-mode request. Checks Blitzkrieg/Easy targeting, pending research/diagnosis, persistent post-enemy blocks, timer setting, settings speed, and the save/profile `current.time` game clock against the 30:00 Lightning War limit. If the pause menu is visible, it OCRs `Timeline Playtime` and uses that visible timer when it is newer than stale save/profile files.
- `bridge_speed [fast|visual]` — Requests the Lua bridge animation/execution mode. For timed attempts, prefer `fast`.
- Archive Air Support / `Mission_Airstrike` previews can require the isometric preview-board start click while advisor dialogue is still visible. Do not dismiss the dialogue first if the board is visible; click the preview board and let deployment load.
- During Lightning War, trust the visible screen over `island_map` if reward, promotion, Perfect Island, or bottom-right Continue panels are still open: the bridge can expose the map behind the panel. Clear visible panels first with `lightning_ui handle_screen` or the exact calibrated control, then run `lightning_ui ensure_pause` before routing/thinking and rerun `lightning_attempt --dry-run` before committing to a route.
- If `lightning_attempt` sees `phase=combat_player` with `active_mechs=0`, it should stop or clear an obvious safe panel instead of entering the full combat wait. That state is usually a held End Turn, a just-clicked transition gap, or a post-mission panel; spending `max_wait` there burns Lightning War clock without making tactical progress.
- For Lightning War pace, measure the live segment as **mission Start click to the next mission Start click**, not just combat time. The 3-minute target includes region click/preview/dialogue, deployment, all combat actions and End Turn waits, Region Secured, hidden-pod popups, reward/XP panels, map return, next region selection, and the next preview-board Start click. Think only from pause between these live segments.
- `recommend_mission --routing lightning_war` is tuned for start-to-start speed: it boosts metadata `turn_limit == 3`, Tidal aliases including `Mission_Tides` / `Mission_Terratide`, Cataclysm/Seismic, Sandstorm, and Train, while pushing Time Pod / asset rewards and Detritus barrels/vats (`Mission_Barrels`, `Mission_AcidTank`, `Mission_Disposal`) below plain fast alternatives.
- For active Lightning War targets, Time Pods are expendable. The speed loop may ignore `pod_lost` / `pod_unrecovered_final` as a pod-only safety loss. With `lightning_segment`'s default speed-loss policy, or `--speed-loss-policy` on the lower-level commands, the loop may also accept predicted nonlethal grid/building damage and optional objective/protected-unit loss for speed. This does not permit timeline collapse, mech death/HP/status loss, pylon/bomb/final-cave loss, unknown loss kinds, stale boards, or unresolved research/desync gates.
- On deployment screens, the bridge can report `phase=combat_enemy` while `deployment_zone_count > 0`, and bridge deploy ACKs can time out after the mech was actually placed. Some deployment screens do not run the Lua command poll before CONFIRM; `deploy_recommended` now uses a short bridge timeout, clears stale pending `/tmp/itb_cmd.txt` data, and falls back to calibrated `deploy_slot_N` + ranked tile UI clicks when the bridge is not ticking. `lightning_attempt` treats `turn=0`, visible deployment zones, and `mech_count < 3` as deployment, calls `deploy_recommended` to finish only missing mechs, clicks CONFIRM, then lets `lightning_loop` wait into the first player turn. `deploy_recommended` skips already placed mechs and treats an ACK timeout as recovered only when a fresh bridge read shows that UID placed; UI fallback returns `WARN` because verification happens after CONFIRM.
- If the bridge reports `phase=unknown`, `turn=0`, and deployment zones, trust the screen. A visible mission preview is started with the preview-board control and then re-read; a visible island map or `island_map_or_unknown` screen routes through the save/CV route planner instead of deploying. An active deployment state can still finish missing mechs only when the bridge heartbeat is fresh; stale active-mission/deployment snapshots are evidence, not permission to deploy. During the 2026-05-28 smoke test, preview/deployment ambiguity, stale island-map/deployment state, and partial deployment burned more than a minute before this recovery path existed.
- After End Turn or environmental cleanup, the bridge can briefly report `phase=combat_player` with `active_mechs == 0`. Treat that as a wait/retry state, not a solveable turn.

**Analysis & tuning:**
- `replay <run_id> <turn> [--time-limit N]` — Reconstruct Board from a recorded JSON and re-run the solver. Compares new solution with original.
- `analyze [--min-samples N]` — Read failure_db.jsonl, report patterns by trigger / severity / squad / island.
- `validate <old.json> <new.json> [--failures-only] [--time-limit N]` — Compare two weight versions across recorded boards.
- `tune [--iterations N] [--min-boards N] [--time-limit N]` — Auto-tune EvalWeights. Hybrid objective: `mean_fixed_score − 100 * fired_failure_count`.

**Run management:**
- `new_run [squad|auto] [--achieve X Y] [--difficulty N] [--mode achievement_hunt|solver_eval|random_squad|custom] [--tags audit ...]` — Initialize new session. Omit the squad or pass `auto` to let `recommend_squad` choose the achievement-aware setup. Use `--tags audit` / `--mode solver_eval` for environment-audit playthroughs (those failures stay out of the tuner corpus).
- `recommend_mission` has no achievement-target flag; do not invent `--target` / `--tags` arguments mid-run. It does support `--routing lightning_war` for Blitzkrieg speed routing. If a mission-pick command shape is uncertain, inspect the parser/help in a standalone command first, then call only supported flags. Regression anchor: Rusting Hulks There Is No Try run `20260519_133059_179`, Archive island 2 selection, where `recommend_mission --target there_is_no_try` failed during map triage.
- On mission-preview screens, `Escape` opens the pause menu instead of safely closing the preview. To back out of a rejected preview, click clearly empty water / non-region map space and verify the preview closed before selecting another region. Regression anchor: Rusting Hulks There Is No Try run `20260519_133059_179`, Archive island 2 mission scouting, where `Escape` opened the menu over Storage Vaults.
- `snapshot <label>` — Save current state for regression.
- `log <message>` — Append reasoning to the decision log.
- `island_select.py --lightning-war [--completed archive]` — Non-session helper for Lightning War island coordinates. It prints Archive first, then R.S.T. after Archive is listed as completed.
- When a `game_loop.py log` message contains shell metacharacters such as `;`,
  `&`, `|`, `(`, `)`, `<`, `>`, or quotes, wrap the whole message in single
  quotes (escaping internal single quotes) or avoid those characters. Do not
  let a reasoning note become accidental shell chaining during a live turn.

## Detailed Live-Ops Rules

3. Click tile centers, not sprites — sprites render 100–170 px above tile center. `click_action` / `grid_to_mcp` already do this.

4. After every failed run, analyze the critical turn. Save a snapshot first.

5. To select a mech in manual play, click its tile on the board. No portraits, no Tab, no keyboard.

7. **Default combat:** `python3 game_loop.py auto_turn --time-limit 10` each turn. It polls for player phase at entry, solves, executes all mech actions via bridge, emits an End Turn click plan. In Codex Computer Use, dispatch `codex_computer_use_batch` (window-local); in legacy batch tools, dispatch `batch` (screen/global). Manual-play fallback (`click_action`/`verify_action`/`click_end_turn`) is only for when the bridge is unavailable. Bridge sub-action commands (`move_mech`, `attack_mech`, `skip_mech`, `repair_mech`) are used internally by `auto_turn` — never call directly.

10. Bridge-to-visual coordinate rule (see Execution Model). Use A1–H8 in all communication.

11. **No keyboard during combat.** End Turn is the only MCP click needed per turn. `click_end_turn` / `auto_turn` emit legacy `batch` coords in screen/global space plus `codex_computer_use_batch` coords in Codex window-local space. After `get_app_state`, use `codex_computer_use_batch` or the visible End Turn button center, then verify the screen switches to ENEMY ACTIVITY / ENEMY TURN.

16. **Deployment:** prefer `python3 game_loop.py deploy_recommended`, which sends bridge `DEPLOY uid x y` commands for the ranked FORWARD/MID/SUPPORT tiles and verifies the placed mech coords. Click the visible CONFIRM button afterward. If the bridge helper is unavailable, fall back to `game_loop.py read` MCP coords and click the 3 recommended tiles manually, then CONFIRM.

17. **Hover-verify-click — for novel UI only.** Before `left_click`-ing an unfamiliar UI element (shop items, reward cards, new menus), do: `mouse_move` → `screenshot` → `left_click`. **Exempt:** the End Turn button (use `codex_computer_use_batch` in Codex Computer Use; calibrated offset `(126, 120)` relative to the window) and any combat tile click computed by `grid_to_mcp()` (calibrated <2px — `click_action` handles internally).

18. **Prefer `game_loop.py read` over screenshots for combat state.** Screenshots are 100–200 kB each and accumulate fast. Reserve screenshots for novel UI screens (defeat, rewards, shop, CEO cutscenes) and for verifying unexpected state.

22. **Simulator version discipline.** `SIMULATOR_VERSION` in `src/solver/verify.py` pins the semantic behavior of the Rust + Python simulators. Bump it whenever a simulator change alters predictions on a given board (e.g., Storm Generator implemented, Psion regen applied, Spartan Shield fixed, PushDir::Flip semantics changed). Weight-tuning changes do NOT bump it. Every new solve recording and failure_db entry is stamped with the current version. Before bumping: `cp recordings/failure_db.jsonl recordings/failure_db_snapshot_sim_v<old>.jsonl` to archive the pre-bump corpus. `cmd_tune` refuses to run on a mixed-version corpus unless `--accept-version-change` is passed.

25. **New-run setup is achievement-aware: difficulty + Advanced Edition ON + the target squad.** Updated 2026-05-13 for the active Hard Victory push. Balanced Roll remains correct for `--mode solver_eval` and random-squad achievements, but normal achievement hunting should first run `python3 game_loop.py achievements --sync` when `.env` has `STEAM_API_KEY` and `STEAM_ID`, then pick an actual unfinished squad via `python3 game_loop.py recommend_squad --tags achievement` / `python3 game_loop.py new_run auto --tags achievement`. For ordinary squad achievements, use **Easy** (`--difficulty 0` or omit the flag). **Exception:** Hold the Line needs four simultaneous emerging Vek markers, so start that Blitzkrieg achievement on **Normal** (`--difficulty 1`) unless the user explicitly asks to continue an existing Easy timeline. For **Hard Victory**, explicitly use **Hard** (`--difficulty 2`), Advanced Edition ON, and the chosen target squad (currently Rusting Hulks). Don't fall back to another difficulty after defeats unless the run target changes. If starting a nonstandard difficulty run from the squad/start screen, watch `new_run` output for `[new_run] difficulty synced from save file`; if it rewrites the requested difficulty from an old timeline save, do not press Start until the visible difficulty screen has been re-opened, `verify_setup --difficulty <target>` passes, and the active session metadata is corrected. Rationale: `feedback_playstyle.md` plus squad-locked achievement targeting.

27. **No repo-wide Rust formatting during tactical fixes.** This Rust tree is not globally `cargo fmt`-clean; running `cargo fmt` rewrites many unrelated files and hides the simulator change under formatting noise. For live/debug fixes, keep Rust edits hand-scoped to touched hunks. Only run a repo-wide formatter as its own explicit cleanup task.

33. **Deployment roster supplementation is built in.** `deploy_recommended` merges live bridge mechs, the last active solution, and save-file `GameData.current.mechs` into stable UIDs 0/1/2. If the bridge lists only two unplaced mechs, do not manually deploy the third first — run `deploy_recommended` and let the bridge `DEPLOY uid x y` verification tell us whether a UID is truly missing.

43. **Bridge-executed combat does not guarantee Reset Turn is usable.** After `auto_turn` mutates the board through bridge commands, the visible Reset Turn button may hover but refuse to open a confirmation dialog. If a bad bridge plan executes, snapshot immediately, fresh-read/solve from the actual board, and only rely on Reset Turn if the UI visibly opens the confirm prompt.

70. **Save-file upgrade overlays must read pawn mod pips, not only `current.weapons`.** `GameData.current.weapons` can preserve base active weapon IDs after newly powered upgrades, while each `SquadData.pawnN` has the authoritative `primary_mod*` / `secondary_mod*` power lists. The Lua parser must preserve bare numeric arrays like `{3, 3}` as lists, because `{}` here erases powered upgrade evidence. When bridge SkillLists show base weapons, derive modeled upgraded IDs such as `Ranged_Rocket_A` and `Science_Repulse_A` from fully powered pawn mod lists before solving. Do not overlay unmodeled passives such as `Passive_Electric_A` if Rust intentionally keys the base passive ID. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle Thermal Dampeners deployment after Mission_Stasis: Rocket +1 Damage and Pulse Shield Self were powered in pawn mods, but `current.weapons` stayed `Ranged_Rocket` / `Science_Repulse`; fixed in simulator v78.

79. **Minor Vek do not receive Psion aura bonuses.** Units marked `Minor = true` in Lua (Blobs, Spiderlings/WebbEggs, Shaman Totems/Spores, etc.) are still enemy units for attacks, collision, and threats, but they are not affected by Psion bonuses: Blast/Boss death explosions, Soldier HP, Shell armor, Blood regen, Fire immunity/on-death fire, Boost damage, or Spider Psion eggs. Do not infer Minor from old local tables that marked `Leaper1`; shipped `Leaper1` is a normal non-minor Vek. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Dam` / Central Museums turn 2: flood-drowned `Totem2` at E4 did not emit a Blast Psion burst, so Pulse Mech only lost shield, not HP; fixed in simulator v84.

84. **Mission preview boards can absorb map clicks as Start Mission.** Once a red-region preview is open, the displayed mini-board is an active Start Mission target. Do not click through the board area while trying to compare other red missions; first exit/close the preview with a known-safe UI action or click a clearly separate map region outside the preview board. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, R.S.T. island after Tesla Fields: a comparison click on the open Blast Garden preview started deployment.

91. **Mission preview mini-board hitboxes are wider than they look.** If a preview is open and you want to switch to another red region, do not click through any area visually overlapped by the preview mini-board or its projected diamond, even if the target label seems separate; that can start the current preview mission. Use a clearly outside-map/UI close path or accept the current mission intentionally. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, after Pumping Station, a click meant for Venting Center started Reprocessing.

106. **Post-pod pilot assignment is drag-and-drop.** On the island map/hangar screen, a newly recovered pilot in Storage must be dragged from the storage portrait onto the empty mech pilot bay; clicking the portrait only opens the pilot tooltip and does not assign them. Regression anchor: Hard Rusting Hulks run `20260512_104120_903` after Razor Shore pod: Kai Miller in Storage needed to be dragged into JetMech's empty pilot slot after Morgan Lejeune died.

111. **Arachnid Psion death eggs appear during player actions.** `Jelly_Spider1` makes every other non-minor Vek death create a live `SpiderlingEgg1` on the death tile immediately, before the next mech acts. Player-phase replay snapshots and partial re-solves must materialize that egg with the engine-style next pawn UID; enemy-phase deaths still defer egg placement until after the hatch loop so fresh eggs do not hatch in the same enemy phase. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Corporate HQ turn 4: Rocket D8 hit Firefly at D4, the corpse bumped Arachnid Psion at D3, and live spawned `SpiderlingEgg1` uid 84 at D4; fixed in simulator v105.

117. **Do not let Region Secured snapshots stall the live run.** On reward / Region Secured screens, `game_loop.py snapshot <label>` can hang while bridge/save state is in a transition. If a snapshot produces no output within roughly 20 seconds, terminate that snapshot process, note it in the run log later, click through the visible reward/continue flow, and use `mission_end ... --no-commit` or a later stable snapshot once the island map/save phase has settled. Do not run another session-touching `game_loop.py` command until the hung snapshot process is gone.

130. **Mission preview clicks must target the intended region or visible Start Mission text.** On the island map, mission preview cards, board previews, and CEO dialogue overlays can overlap other regions; a stray click inside the preview or text overlay can select the underlying mission instead of starting the intended one. After a region is chosen, use the CEO portrait or another non-map edge area to dismiss briefing text when needed; do not click the text strip itself, because the dismiss click can pass through to an adjacent region after the dialogue clears. Then click only the visible **Start Mission** label on that region's preview board. If the selected mission changes, reselect the intended red region and reconfirm its title/objectives before starting.

143. **Mission-start save lag is not post-mission stale bridge.** Right after confirming deployment, `saveData.lua` can still parse as `between_missions` while the fresh Lua bridge is already in `combat_enemy` / `combat_player` for turn 0 or turn 1 of the new mission. If the bridge says `in_active_mission=true`, has a `mission_id`, and the combat turn is 0/1, trust the bridge instead of the save phase. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Pinnacle Shielded Vaults opened with `Mission_SnowStorm` turn 1 visible while `cmd_read` incorrectly ignored the bridge as stale.

148. **Mission-panel clicks can commit the selected mission.** After a mission detail panel is open on the island map, do not click through the visible map to inspect another region; a click in the panel/preview area can advance into deployment for the already selected mission. To compare missions, dismiss/back out to the plain island map or rely on `recommend_mission` / screenshots before opening a panel. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, after The Pasture, an attempted Chronology Hall inspection from the Old Earth Park panel started Old Earth Park deployment instead.

154. **Mission preview overlays can eat adjacent-region clicks.** After selecting an island-map region, the tilted preview board and large **Start Mission** text sit over nearby red regions; clicking what looks like another region through that overlay can immediately start the currently selected mission. To inspect a different mission, first click a clearly exposed non-start part of that red region outside the preview board, or use save/visual evidence and decide without cycling previews. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility preview started when a Scrapheap-looking click landed on the active Start Mission overlay.

170. **Status checks are session commands too.** Treat `python3 game_loop.py status` like `read`, `solve`, `auto_turn`, and other session-touching commands: run it alone, never inside `multi_tool_use.parallel` with file reads, helper scripts, screenshots, or UI inspection. A quick status/read check can still take the active session lock and should stay auditable as its own step. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Archive Artifact Vaults turn 2, where a status check was accidentally bundled with a skill file read. Follow-up anchor: Blitzkrieg Lightning War run `20260603`, where a fresh `read` during route-mismatch triage was incorrectly bundled with an `rg` search.

180. **Mission preview clicks must be title-verified before starting.** After inspecting a rejected mission, do not click the board preview or any large preview panel until the visible mission title matches the intended target. First click an exposed part of the desired red region, wait for the preview title/objectives to update, then start only if that title/objective set is acceptable. Regression anchor: Perfect Strategy farming run `20260517_184341_140`, after rejecting `Accord Repository` ("Do not kill the Volatile Vek" + "Protect the Clinic"), a click intended for `Historic County` landed on the active `Accord Repository` preview and started the rejected fire-squad no-kill mission.

184. **A visible `Start Mission` overlay is a live launch button.** Once a mission preview board is active, any click inside the yellow-outlined preview board can start that mission, even if the intent is to inspect a different region behind it. To switch targets, click only clearly exposed map/label pixels outside the preview board and outside the yellow outline; if the desired region is obscured, dismiss or choose a visible safe region instead. Regression anchor: Flame Behemoths Easy run `20260517_191517_652`, after rejecting Downtown (`Defend the Train`), a click intended for Pumping Station landed on the active Downtown preview board and started the train mission.

187. **Preview-revealed unit objectives must be mapped before launch.** If a mission preview shows a unit objective that is not already in `data/mission_unit_objectives.json`, add the exact Lua pawn type from the game scripts and a focused resolver test before starting that mission. Detritus examples: `Mission_Civilians` protects `VIP_Truck`; `Mission_Power` with "Destroy 2 Vek Egg Sacks" destroys `BonusDebris`; generic `Mission_Survive` can also pair bonus id `7` with live `BonusDebris` egg sacks, so require the injected destroy-objective list before trusting the solve. This lets `auto_turn` safety compare `protected_objective_units_alive` / objective unit destroy value instead of treating those pawns as ordinary units.

190. **Verify Advanced Content by colored icons, not empty checkboxes.** The Difficulty Setup screen's Advanced Content squares do not draw a normal persistent tick in captures; enabled rows are confirmed by the colored row icons, while disabled rows are grayscale. Before pressing the setup **Start** button for achievement runs, run `python3 game_loop.py verify_setup --difficulty 0` (or the target difficulty) and require `status: PASS`. If it reports missing rows, click the emitted window-local `click_plan`, move the cursor away from the rows, and rerun `verify_setup`. Regression anchor: Perfect Strategy restart on 2026-05-17 where several row clicks only produced hover outlines and the old process had no machine check for Advanced Content state.

191. **Combat click plans must be visually calibrated in Codex Computer Use.** `click_action` may emit `codex_computer_use` window-local coordinates, but if the first select click lands on a visible terrain tile instead of the intended mech, stop immediately and switch to visible-tile manual calibration; do not dispatch the rest of the batch. Split manual recovery into one sub-action at a time: select the mech, click the intended highlighted move tile, run `game_loop.py read`, and only arm/fire after the bridge confirms the mech reached the expected visual tile. If a wrong tile move happens, try visible **Undo Move** once after reselecting the mech; if it does not revert, solve from the actual board and do not trust stale planned movement. Regression anchor: Perfect Strategy run `20260517_212541_626`, Detritus `Mission_Power` turn 3, `click_action`'s window-local select landed on an acid tile and a manual C2 attempt moved Swap Mech to B3 before verification caught it.

195. **When rejecting a mission preview, do not click through its board overlay.** On the island map, selecting a mission opens a large isometric preview whose center text is an active `Start Mission` target. If the objective text is bad for Perfect Strategy, click a clearly visible unselected region label or use the game menu to restart; do not click inside the preview board while trying to inspect a different region. After selecting an alternative, verify the mission name and objective text changed before clicking Start Mission. Regression anchor: Perfect Strategy run `20260517_224254_157`, Archive `Old Town`, where a rejected "Kill 4 or fewer Enemies" preview absorbed the next region-inspection click and started deployment for the bad mission.

199. **Mission-end `unknown` phase after final enemy cleanup means inspect the reward screen.** On some missions, especially when the final queued enemy dies before it can act, `auto_turn` can wait through the last enemy activity, see the bridge flip from `combat_player` to `unknown`, and return `Not in combat_player phase: unknown` even though the visible screen is already `Region Secured`. Treat this as a mission-end transition, not a combat recovery point: call Computer Use `get_app_state`, verify the visible objective stars/reward screen, snapshot success or failure, and do not issue another combat command from the stale board. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_085625_707`, R.S.T. `Mission_Lightning` / Razor Shore after turn 4, where the Hornet died during final cleanup and the reward screen showed `Protect the Old Earth Bar` completed.

201. **Mid-mission `podReward` alone does not prove a Time Pod objective survived.** A save-file `player.podReward` can be non-empty while the visible mission objective row is crossed, and the final reward screen can still report `Protect the Time Pod (Failed)`. For Perfect Strategy, treat a crossed visible Time Pod row as blocking unless the current or final reward UI explicitly says `Pod Recovered`; do not rely on mid-mission `podReward` by itself. If the row is crossed before final End Turn and Reset Turn is unavailable, snapshot and restart/abandon after mission cleanup. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_085625_707`, R.S.T. `Mission_Survive` / The Haze turn 2-4, where saveData contained `podReward = Science_FreezeBeam + 1 core` but the Region Secured screen showed `Protect the Time Pod (Failed)`.

203. **Destroy-unit objectives need final-turn safety, not just scoring.** Mission previews like `Destroy the ACID Storm Controller` must map their unit pawn into `data/mission_unit_objectives.json` and plan safety must compare `destroy_objective_units_alive` on the final turn. A clean grid/building/pod plan that leaves the target alive is a failed Perfect Island. Regression anchor: Rusting Hulks Easy Perfect Strategy run `20260518_175913_794`, Detritus `Mission_AcidStorm` / Venting Center, where the Time Pod was recovered and buildings were safe but `Storm_Generator` stayed alive, producing `Destroy the ACID Storm Controller (Failed)`.

203. **Perfect Strategy avoids fire-tile counter missions until progress is hard-gated.** `Mission_ForestFire` / "End with 8 spaces on fire" is a custom mission objective (`use_bonus=false`), so the normal `bonus_objective_ids` risk logic does not see it. The solver can finish the mission with clean grid/building safety while the reward screen still fails at `3/8` fire tiles. During Perfect Island farming, reject this mission preview when any alternative exists, even with Flame Behemoths, unless the solver payload and visible objective progress prove the counter will reach 8 before the final End Turn. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_100313_205`, Archive `Mission_ForestFire` / Colonial Park, where the reward screen showed `End with 8 spaces on fire (3 on fire)` failed after all combat safety gates passed.

205. **`recommend_mission` takes profiles, not tags.** On the island map, use `python3 game_loop.py recommend_mission` for the default achievement-aware scoring, or `python3 game_loop.py recommend_mission --profile <name>` when a specific strategy profile is implemented. Do not pass `--tags achievement`; that flag belongs to `new_run` / `recommend_squad`, and argparse will error before scoring the map. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_105028_125` after Colonial Park, where a stale memory of `--tags achievement` interrupted mission selection.

206. **Use BSD/macOS-compatible file listing commands.** On this Mac workspace, `find -printf` is not available. For quick recording inventories, prefer `ls -lt`, `find ... -print`, or `stat -f` instead of GNU-only `find -printf`, especially during live mission selection where process noise can obscure the game-loop signal. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_105028_125` after Colonial Park, where a parallel file-inspection batch emitted `find: -printf: unknown primary or operator`.

208. **`game_loop.py log` takes a positional message.** Do not pass `--message`; argparse will reject it. Use `python3 game_loop.py log "..."` (or multiple positional words) as its own session-touching command when recording a live tactical acceptance. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_105028_125`, `Mission_Repair` turn 4 recovery, where a stale `--message` flag interrupted the pre-End-Turn decision log.

209. **Mission preview overlays are sticky; don't click through them to compare neighbors.** After selecting a district preview, a click on a nearby red region can instead commit the currently previewed mission and jump to deployment. To compare alternatives, first dismiss the preview with a clearly safe outside click or use `recommend_mission` plus visible label confirmation; do not click ambiguous coordinates that lie over the preview/map inset. Regression anchor: Flame Behemoths Easy Perfect Strategy run `20260518_105028_125`, after `Mission_Repair`, where trying to inspect Research Center while Artifact Vaults was previewed launched the Tanks mission.

216. **Mac save/config path is `IntoTheBreach`, not `Subset Games/Into The Breach`.** Live profile files are under `~/Library/Application Support/IntoTheBreach/profile_Alpha/`, and setup toggles live in `~/Library/Application Support/IntoTheBreach/settings.lua`. Do not waste live-run time probing `~/Library/Application Support/Subset Games/Into The Breach`. Regression anchor: simulator v147 restart verification after `Mission_Tanks`, where the old path produced `No such file or directory`.

219. **If Computer Use rejects a click after a fresh app-name state read, refresh by bundle id.** Sometimes `get_app_state(app="Into the Breach")` succeeds but the immediate `click(app="Into the Breach", ...)` still returns "Computer Use is not active". When that happens, call `get_app_state` again with the reported bundle id `subset.Into-the-Breach`, then send the click with that same app id. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_121221_583`, Retrospect Park turn 2 End Turn click.

224. **Island-selection clicks commit the next island immediately.** On the four-island selection screen, clicking an unsecured corporate island enters that island and triggers the CEO intro; it is not a harmless preview. Decide from current screenshot, local island data, and any available strategist/agent advice before clicking. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_121221_583`, where a data-gathering click on R.S.T. selected it as island 2.

226. **Empty survival turns can be valid End Turn states.** On survival-style missions, enemy cleanup can leave the visible board with no live enemies, `Victory in N turns`, End Turn available, and all mechs still `DONE`; `auto_turn` may then wait 45s and return `No active mechs after polling` even though this is a legitimate empty player turn. If `read`/the visible screen show grid/objectives clean, no enemies, and End Turn lit, log the anomaly and click End Turn instead of repeatedly calling `auto_turn`. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_121221_583`, R.S.T. QA Division turn 2 after accepted turn-1 threat cleanup.

227. **Quote shell patterns that contain backticks.** When using `rg`, `sed`, or any shell command with literal Markdown/code spans in the pattern, wrap the whole pattern in single quotes or avoid the backticks. Otherwise zsh treats backticks as command substitution before `rg` runs. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_121221_583`, where `rg -n "Quote `game_loop.py log`..." AGENTS.md` tried to execute `game_loop.py`.

232. **Spawn-block objectives are mandatory, not generic spawn hygiene.** `Mission_Survive` / `BONUS_BLOCK` can be safety-clean while failing Perfect Island because the current tactical solver only gives generic value for occupying spawn tiles and does not know `BlockedSpawns / 3` is a bonus objective. During Perfect Strategy farming, avoid block-spawn missions unless no safer mission exists. If forced into one, inspect the visible/save objective counter before every End Turn, and do not advance unless either enough spawn tiles are occupied this turn or a top-k/manual audit proves the remaining turns can still reach 3/3. If `auto_turn` leaves a required spawn unblocked, stop before End Turn, snapshot, run top-k candidate triage for plans with `spawns_blocked.count > 0`, and manually execute the spawn-blocking candidate. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_133912_109`, Detritus Landfill turn 2, where a clean safety plan left the only spawn unblocked at `BlockedSpawns=0`, making the 3-block objective mathematically impossible later.

234. **Mission preview clicks can launch; return to map before scouting another region.** On the island map, once a mission briefing/preview is open, clicks over the preview/map area can advance to deployment instead of selecting a different territory. During Perfect Strategy scouting, inspect the visible objectives, then dismiss/return to the territory map or click only known safe UI controls before probing another mission. If the next action could be launch/deployment, decide whether the current mission is acceptable first; if it is not acceptable, do not click another territory until the preview is visibly closed or the target territory label is unobscured. Regression anchors: Flame Behemoths Perfect Strategy run `20260518_142606_302`, Archive Retrospect Park, where a scouting click meant for Colonial Park advanced into a Mite + Power Generator deployment; Flame Behemoths Quantum/This Is Fine run `20260519_224158_398`, Archive Secondary Archives, where a scouting click meant for Preserved Farms landed on the open dam preview and launched deployment.

235. **Find helper paths before opening guessed files during live play.** When investigating harness internals mid-run, use `rg --files` / `rg -n` to locate the real module before `sed`/`cat`; do not open guessed paths while a mission is waiting on a tactical decision. A missing-file lookup is harmless but adds noise to the live audit trail. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_142606_302`, Retrospect Park turn 1, where `src/capture/research_queue.py` was guessed during stale research-queue cleanup.

238. **Island-select helper coords are only for the four-island screen.** Once a corporation zooms into its territory map, never reuse the `island_select.py` click coordinate or a visual island-center coordinate; those now lie over mission regions and can launch a mission before the strategist ranks the slate. After the zoom, run `game_loop.py read` / `recommend_mission` once, and if the bridge has no island map, inspect visible mission previews deliberately rather than clicking the old island-select point again. Regression anchor: Flame Behemoths Perfect Strategy run `20260518_160125_397`, where a second Pinnacle helper-coordinate click launched `Mission_BoomBots` before mission-picker triage.

241. **Mission-preview scouting must have a no-launch escape plan.** If a preview opens for a risky counter mission such as `BONUS_BLOCK`, do not click inside the preview/map inset while trying to inspect another sector; that same click can be interpreted as Start Mission. Either commit deliberately, dismiss via a known safe outside/UI target, or use `recommend_mission`/visible labels without more exploratory clicks. Regression anchor: Rusting Hulks Easy Perfect Strategy run `20260518_192325_609`, R.S.T. The Haze, where an inspection click committed to `Mission_Survive` with `Block Vek Spawning 3 times`.

261. **Quote decision-log messages that contain punctuation.** `game_loop.py log` accepts free-form words, but the shell will still treat punctuation such as semicolons as command separators unless the full message is quoted. For any log message with `;`, `&&`, `|`, parentheses, quotes, or other shell-significant characters, wrap the message in single quotes and escape embedded single quotes before running the command. Regression anchor: Flame Behemoths Quantum/This Is Fine run `20260520_134643_900`, Pinnacle `Mission_DungBoss` turn 4, where an unquoted semicolon in a dirty-line note split the log command after the first clause.

265. **Do not run `mission_end` after a map read has auto-advanced the session.** `cmd_read` can detect the reward-to-map boundary and advance `session.mission_index` to the next mission slot. If that already happened, `game_loop.py mission_end win` will record the just-finished mission under the next mission id and bump the session one extra step. For ordinary region rewards, prefer snapshot + visible confirmation + fresh map scouting; use `mission_end` only before any auto-advance or for true run-end bookkeeping. Regression anchor: Flame Behemoths Quantum/This Is Fine run `20260520_134643_900`, where Containment Zone J (`m07`) was briefly recorded as `m08 Mission_AcidStorm` after a post-reward `read`.

266. **Close mission previews before scouting another region.** A click on the board/preview while a mission card is open can start the currently previewed mission, even if the visible intention is to inspect a different region behind the overlay. When rejecting a preview, first click clearly empty water / non-region map space, verify the preview is closed with Computer Use, then click the next red region. Regression anchor: Flame Behemoths Quantum/This Is Fine run `20260520_134643_900`, where Disposal Site C was started while attempting to scout Containment Zone D from an open preview.

280. **Stage helper definitions with their call sites.** Before committing a live-loop code fix, compare `git diff --name-only` with `git diff --cached --name-only` for the touched module family, and make sure any new Rust/Python helper definitions are staged with every call site that references them. A targeted test run from a dirty worktree can pass with unstaged helper code while the committed tree is broken. Regression anchor: commit `08f1710` initially staged `solver.rs` call sites for `Unit::is_player_action_unit()` but missed the new helper in `rust_solver/src/board.rs`.

292. **Quote live-loop log messages as one shell argument.** `python3 game_loop.py log` is session-touching and must run alone; when the message contains punctuation such as semicolons, quote the entire message so zsh cannot split it into a second command. Prefer a single-quoted message with no shell metacharacters when recording dirty-line/audit decisions.

295. **Island mission previews have a large Start Mission hitbox.** When inspecting mission choices manually, click the province label or outer red province area first and keep subsequent comparison clicks away from the yellow diamond/board preview; clicking near the preview can immediately start the highlighted mission before comparing alternatives. If a safe-but-unconfirmed mission starts accidentally and deployment has not begun, treat it as a process miss, update this guard, and continue only if the mission is still acceptable for the achievement target. Regression anchor: Frozen Titans Untouchable run `20260521_214036_725`, Archive `Retrospect Park`, where a comparison click inside the preview started `Mission_Satellite` before `Safeguard Valley` was re-inspected.

298. **Do not let a silent cargo regression block a live run indefinitely.** When `bash scripts/regression.sh` reaches `cargo test --release --test regression --no-default-features` and stays silent for more than two minutes with the cargo process sleeping, no `rustc`/test child, and negligible CPU time, terminate that hung harness run, record the interruption, and continue with focused pytest/script coverage plus any replay/solve check that exercises the fix. Re-run the broad harness later from a clean terminal before treating a Rust semantic change as fully certified. Regression anchor: Frozen Titans Untouchable satellite-flying fix, where the broad harness stalled in cargo before spawning children despite no Rust source diff.

305. **Cargo test takes one filter argument.** When running multiple focused Rust tests, either use a single shared substring filter or run separate `cargo test` commands; passing two test names before `--` is a CLI error and wastes live-loop time. Regression anchor: Rusting Hulks Untouchable conveyor-direction fix, where `cargo test ... test_a test_b -- --nocapture` failed with `unexpected argument` before any test ran.

309. **Avoid Bad Repairs for active Untouchable.** `Mission_Repair` / "Bad Repairs" can deploy mechs below max HP before the first player turn, which is incompatible with a no-mech-damage island attempt even if repair platforms can heal or overheal afterward. If a mission preview says "Use 3 Repair Platforms" with the Bad Repairs warning, do not start it for `Untouchable`; if it starts by accident, read the bridge HP, snapshot, and restart on any `hp < max_hp`. Regression anchor: Frozen Titans Untouchable run `20260522_000333_403`, Archive `Artifact Vaults`, where MirrorMech deployed at `1/3` HP before combat.

310. **First-island comparison clicks can commit the island immediately.** On the first Corporate Island selection screen, an island-body click can zoom straight into that island's mission map instead of merely switching threat-scanner previews. When comparing first-island options for `Untouchable`, do not rely on exploratory clicks after setup; use the current `get_app_state` screenshot, known island risk rules, or a non-clicking helper first, then click only the island you are willing to commit. If a click commits an island before comparison is complete, snapshot immediately and restart if the island is strategically unacceptable. Regression anchor: Frozen Titans Untouchable run `20260522_102747_192`, where an Archive click committed Archive before scanner comparison was complete.

314. **If Cargo/maturin sleeps before spawning rustc, switch to a clean target dir.** On this Mac workspace, the default `rust_solver/target` can leave `cargo test` / `maturin build` sleeping at 0% CPU with no `rustc` child after a live interruption. Kill the sleeping Cargo process, rerun focused tests with `CARGO_TARGET_DIR=/tmp/itb_solver_target` and `DYLD_FRAMEWORK_PATH=/Library/Developer/CommandLineTools/Library/Frameworks`, then build the wheel with `CARGO_TARGET_DIR=/tmp/itb_solver_maturin_target python3 -m maturin build --release` and install that wheel. Do not wait indefinitely while a live run is parked. Regression anchor: Frozen Titans Untouchable run `20260522_113513_095`, Spartan Shield simulator v182/v183 fix, where the default target slept before any compiler child but the clean target dir built and tested normally.

318. **Spartan Shield is not a Burnbug/Gastropod attack cancel.** `Prime_ShieldBash` damages and flips attack direction, but live bridge checks can leave projectile-grapple enemies such as `Burnbug1` still queued on the original mech target after the bash. For active `Untouchable`, do not approve a manual line whose no-HP proof depends on Shield Bash "moving" or canceling a Burnbug/Gastropod; after any Shield Bash used as a manual threat answer, fresh-read the exact `queued_target` before spending the next action. If the target still points at a mech and Reset Turn is unavailable, snapshot and restart/restore instead of finishing the turn. Regression anchor: Frozen Titans Untouchable run `20260522_133722_695`, Archive `Mission_Dam` turn 4, where a manual Guard bash on F3 left Burnbug1 at F3 targeting GuardMech E3 and forced a restore attempt.

321. **Rollback triage still obeys the session lock.** When recovering from a live mistake, do not bundle `game_loop.py read`, `status`, `solve`, or any other session-touching command into `multi_tool_use.parallel` beside `rg`, `ls`, `find`, or snapshot-file inspection. Run the game-loop command alone first or after the file reads finish; emergency mode is exactly when accidental concurrent session reads make the audit trail harder to trust. Regression anchor: Hazardous Mechs Healing run `20260522_154935_555`, Detritus `Mission_Barrels` turn 3 recovery, where a rollback-helper search was incorrectly parallelized with `game_loop.py read`.

325. **Do not click through an open mission-preview board.** Once a province preview is open, the large yellow-outlined board itself is the Start Mission hitbox; labels for neighboring provinces may be visually behind or beside that overlay. To compare another mission, first click a clearly exposed province body/label outside the preview board, or dismiss/reposition using a safe map area if available. Never click on top of the preview board to reach a hidden label. Regression anchor: Hazardous Mechs Healing run `20260522_154935_555`, Detritus island map after `Mission_Barrels`, where a click intended for Nanite Farms landed on Reclamation Zone's preview board and started the VIP/Coal Plant mission.

334. **Replay `None` actions as bridge skips.** In recorded solver plans, `weapon_id=None` / `WId::None` is not a half-action waiting for later UI input; `auto_turn` moves the pawn if needed and then sends the bridge skip, spending that unit. Offline replay snapshots must mark that unit inactive after the `None` action so active-flag-only drift does not hide a real Terraformer or status diff. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, R.S.T. `Mission_Terraform` turn 1, where LeapMech's move-only F5->D2 action was skipped live but pre-v198 replay still showed it active.

336. **Parallel file searches must not mention `game_loop.py`.** The session-lock rule applies to command strings, not just runtime behavior: do not put `game_loop.py` in a `multi_tool_use.parallel` command even as a filename argument to `rg`, `sed`, `nl`, or another read-only tool. Search other paths in parallel first, then inspect `game_loop.py` in its own terminal call if needed. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, after R.S.T. `Mission_Terraform`, where a harmless-looking parallel `rg ... game_loop.py` violated the audit rule while no session command was intended.

349. **Open mission-preview boards swallow far-away map clicks.** Once a province preview is open, even clicking a seemingly separate label or lower-right region can land inside the preview board/start hitbox and immediately begin the previewed mission. To compare alternatives, first dismiss the open preview or click a clearly exposed province body outside the entire preview board bounds; never click through/behind the open preview. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Archive map after `Mission_Volatile`, where a click intended for `Archivist Hall` while Colonial Park's preview was open started `Mission_Dam` / Colonial Park deployment.

355. **Do not click anywhere covered by a mission-preview board, even outside the text panel.** The yellow-outlined map preview itself is the Start Mission hitbox and can extend over neighboring province bodies above or beside the text box. To switch missions after opening a preview, first dismiss the preview by clicking clearly empty ocean/map background outside the entire rendered board, then click the next province label/body. Regression anchor: Hazardous Mechs Healing run `20260522_193613_471`, Pinnacle map after `Mission_Factory`, where clicking the apparent upper body of `Sub-Zero Range` was still inside The Tundra's preview board and started `Mission_FreezeMines`.

367. **Setup verifier screenshots must be visibly game-focused.** Before trusting `verify_setup` output, confirm the screenshot path it reports shows Into the Breach, not Codex or another frontmost window. If it captured the wrong app, discard the suggested setup clicks, refocus Into the Breach with Computer Use, and re-run or manually verify from the visible game screen before pressing Start. Regression anchor: Hold the Line setup on 2026-05-24, where `verify_setup --difficulty 0` captured the Codex thread and falsely reported all Advanced Content rows off.

369. **Live `game_loop.py log` commands need literal quoting even for short notes.** Treat every free-form log message as data: wrap it in single quotes before running the shell command, including status notes that contain colons, semicolons, commas, arrows, or achievement commentary. If a log command accidentally splits, do not keep composing unquoted prose in the shell; rerun only the corrected quoted note and continue the parked turn. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, Archive `Mission_Artillery` turn 1, where an unquoted simulator-fix note split at a semicolon and tried to execute `continuing` as a shell command.

371. **Hold the Line precheck must happen after enemy-phase waits too.** If a deployment confirm or prior End Turn leaves the bridge in `combat_enemy`, do not call `auto_turn` as the first player-phase observer on active `Hold the Line` attempts. Wait/read until `phase == combat_player` and `active_mechs > 0`, then apply rule 370 before any solver execution. `auto_turn` can internally wait through the enemy animation and discover `spawn_points >= 2`, but by then it will immediately solve/execute and the spawn-banking sweep is skipped. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. `Mission_Force` turn 1, where `auto_turn` waited out deployment enemy phase, found 2 spawn markers, and executed top-1 before a widened spawn-block ranking could run.

372. **Log-command quoting is mandatory even after a snapshot.** A safety snapshot does not make an unquoted `game_loop.py log` harmless; the log command itself can still split at a semicolon and execute trailing prose. Before running any `python3 game_loop.py log ...`, scan the exact shell command for a single quoted payload after `log`, or replace punctuation such as `;` with plain words. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. `Mission_Solar` turn 1, where `... threat; accepting ...` logged only the prefix and then zsh tried to run `accepting`.

374. **Keep live-turn searches out of `recordings/` unless the corpus is the target.** During a parked combat turn, do not run broad `rg` scans over `recordings/` just to find CLI examples; the corpus is large enough to stall the harness while the End Turn plan is waiting. Search `game_loop.py`, `src/`, and `docs/` first, or add `--glob '!recordings/**'` when the recordings corpus is not the subject. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. `Mission_Solar` turn 3, where `rg research_submit ... recordings | head` had to be killed while a clean End Turn plan was parked.

376. **Confirm Rust source filenames before targeted `rg`.** This solver tree does not have a generic `search.rs`; target generation currently lives in `rust_solver/src/solver.rs`. When investigating a live sim miss, use `rg --files rust_solver/src` or a broad directory search before naming a file explicitly, so a missing-file `rg` exit does not add noise to the parked-turn investigation. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. `Mission_Solar` turn 3, where `rg ... rust_solver/src/search.rs` failed during Brute_Grapple triage.

378. **Cargo test accepts one filter string.** To run multiple focused Rust tests, use a shared substring filter with the PyO3 harness flags (for example `cargo test --no-default-features --lib test_brute_grapple_destroyed_building`) or run separate `cargo test` invocations; do not pass two bare test names before `--`, because Cargo treats the second as an unexpected argument. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. `Mission_Solar` turn 3 v229 fix, where a two-name focused test command failed before compiling.

381. **Probe bridge JSON fields before ad-hoc `jq` tile filters.** `/tmp/itb_state.json` tile payloads are not guaranteed to be object lists; read explicit top-level fields such as `.spawning_tiles` first, or inspect one sample element before using `.tiles[] | select(.spawning)`. Regression anchor: Blitzkrieg Hold the Line run `20260524_154211_411`, R.S.T. Corporate HQ turn 1, where a spawn precheck `jq` assumed object-shaped tiles and errored while the turn was parked.

383. **Hold the Line enemy-phase waits must be read-only.** On active `Hold the Line` runs, after deployment confirm or any End Turn click, do not use `auto_turn` as the wait primitive while the bridge may still be in `combat_enemy`. Poll with standalone `read` calls until the result is `phase == combat_player` and `active_mechs > 0`, then inspect `spawn_points` and run the rule-370 top-k spawn-block sweep before any solver execution. Regression anchor: Blitzkrieg Hold the Line run `20260524_195401_412`, Detritus `Mission_Acid` turn 1, where `auto_turn` waited through deployment, discovered 2 spawn markers, and executed top-1 before the spawn-banking sweep.

384. **Live-turn `rg rust_solver` must exclude build artifacts.** During parked combat, never run `rg ... rust_solver` without restricting to `rust_solver/src` or adding `--glob '!rust_solver/target/**'`; generated `.o` files under `rust_solver/target` can time out on macOS and stall the live loop. Regression anchor: Blitzkrieg Hold the Line run `20260524_195401_412`, Detritus `Mission_Acid` turn 2, where a search for top-k helpers scanned `rust_solver/target/debug/deps/*.o` and had to be killed.

385. **Live-turn repo-wide `rg` must exclude generated run corpora.** During parked combat, avoid `rg ... .` unless the generated run folders are the target; add `--glob '!recordings/**' --glob '!snapshots/**' --glob '!sessions/**' --glob '!rust_solver/target/**'` or search explicit source/docs paths first. Regression anchor: Blitzkrieg Hold the Line run `20260524_195401_412`, Detritus `Mission_Power` turn 1, where a broad Reset Turn helper search stalled while a threat-audit block was waiting.

386. **Live UI-coordinate searches should stay out of bulky data folders.** When a visible menu is open and the goal is just to identify a click target, do not run broad `rg` over `data/` or other large reference folders; inspect the visible UI or search the specific command implementation file instead. Regression anchor: Blitzkrieg Hold the Line run `20260524_195401_412`, Detritus island map after `Mission_Power`, where a broad Start Mission text search stalled while a mission panel was already visible.

387. **Hold the Line HQ and boss deployments use the same read-only wait.** Boss fights often show spawn markers immediately after deployment, so do not treat Corporate HQ as an exception to rules 370/371/383. After pressing CONFIRM on any active `Hold the Line` mission, including HQ, poll with standalone `read` until `phase == combat_player` and `active_mechs > 0`; if `spawn_points >= 2`, run the offline spawn-block sweep before calling `auto_turn`. Regression anchor: Blitzkrieg Hold the Line run `20260524_195401_412`, Detritus `Mission_BlobberBoss` turn 1, where `auto_turn` waited through HQ deployment, found 3 spawn markers, and executed top-1 before the four-block precheck could run.

394. **Computer Use fallback clicks must be refocused and re-read.** After a reboot or Codex Desktop interruption, the in-app Computer Use tools may disappear while bridge commands still work. If using `pyautogui` as a fallback for the End Turn button, first activate Into the Breach, use the latest `click_end_turn` screen coordinate, wait for the enemy phase, and run a standalone `read` before any next click. If the save parser reports `mission_ending` but the visible game still shows an End Turn button, action banners, or a final-cave countdown, treat that as save-phase lag and rely on the fresh bridge/visual state. Regression anchor: Easy Custom Squad run `20260526_204256_831`, final cave, where raw bridge state and the visible screen stayed authoritative while saveData intermittently claimed `mission_ending`.
