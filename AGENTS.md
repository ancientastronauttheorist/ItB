# Into the Breach Achievement Bot Agent Guide

This file is adapted from `CLAUDE.md` so Codex and other agent runners can pick up the same project instructions.

## Project Goal

Earn all 70 achievements in Into the Breach autonomously. The game runs natively on macOS. State is extracted via a Lua bridge (file-based IPC through `/tmp/`). Game actions go through the bridge for combat, and through MCP mouse clicks for UI (deployment, menus, shop, rewards, island nav). The user watches in real-time.

## Important Context

- Turn-based, 8×8 grid, deterministic, perfect information — every enemy telegraphs its attack.
- ITB-ModLoader (Lua) works on Mac via `modloader.lua`. Bridge writes `/tmp/itb_state.json`, reads `/tmp/itb_cmd.txt`.
- Steam App ID: 590380.
- `scripts/install_modloader.sh` copies the bridge Lua into the Steam app bundle — run after editing `src/bridge/modloader.lua`, then restart the game.

## Architecture

**Five layers:**

- **Layer 0 — Game loop:** `game_loop.py` CLI + `src/loop/` (session, logger, commands). The agent is the control loop; Python commands are stateless.
- **Layer 1 — State extraction:** `src/bridge/` (primary) and `src/capture/save_parser.py` (fallback). The bridge delivers richer data: targeted tiles, per-enemy attack data, environment hazards with kill flag, deployment zones, objective buildings, attack order.
- **Layer 2 — Game state:** `src/model/` (Board, Unit, WeaponDef). Single source of truth for solver.
- **Layer 3 — Solver:** `rust_solver/` (the only solver AND the only simulator, pyo3 extension `itb_solver` with entry points `solve` / `solve_top_k` / `solve_beam` / `score_plan` / `project_plan` / `replay_solution`). Search is constraint-based threat response + bounded search. `EvalWeights` tune scoring without changing search logic. On the Python side: `src/solver/solver.py` is now a 121-line wrapper holding `MechAction` / `Solution` dataclasses and `replay_solution()` (a thin shim around `itb_solver.replay_solution` that adds Python's `evaluate_breakdown` for audit-only `score_breakdown`). `src/solver/evaluate.py` owns weight/threat/psion machinery for that breakdown — never touched by `solve()`. **`src/solver/simulate.py` is gone** (deleted in the simulate.py-removal PR series); every diagnose-loop fix targets `rust_solver/src/*.rs`, validator + agent prompt enforce `target_language=rust`.
- **Layer 4 — Strategist:** `src/strategy/` picks `EvalWeights` per achievement target; manages squad/island/shop choices.

**Rebuild the solver** after editing any `rust_solver/src/*.rs`:
```bash
cd rust_solver && maturin build --release && \
  pip3 install --user --force-reinstall target/wheels/itb_solver-0.1.0-cp39-cp39-macosx_11_0_arm64.whl
```
If `maturin` is not on `PATH`, use `python3 -m maturin build --release`
from `rust_solver/` with the same `pip3 install` command.

If `cargo test` hits a rustc incremental-compilation ICE, rerun with
`CARGO_INCREMENTAL=0` before treating it as a code/test failure. The regression
script already sets this guard.

## Execution Model

**Default combat mode: `auto_turn`.** A single `game_loop.py auto_turn` call reads board, solves, executes every mech action via bridge (move → verify → attack → verify, re-solving on desync), and emits an End Turn click plan. It *polls the bridge internally* for the enemy→player transition (up to 45s by default) at entry, waiting for both `phase == combat_player` **and** `active_mechs > 0` — the animation window after End Turn flips phase early but leaves mechs inactive, so checking phase alone falsely proceeds into "No active mechs" errors.

**Typical turn = 2 agent rounds:** `auto_turn` → click End Turn → `auto_turn` (blocks in Python until next player turn) → click End Turn → ...

**Fallback manual play** (`click_action <i>` / `verify_action <i>` / `click_end_turn`) exists for when the bridge is unavailable — don't use it otherwise.

**Coordinate mapping:**
- Bridge `(x, y)` → visual: `Row = 8 - x`, `Col = chr(72 - y)`. Example: bridge `(3, 5)` = `C5`. Always use A1–H8 visual notation in communication.
- MCP pixel coords: `grid_to_mcp(x, y)` (in `src/control/executor.py`) or `python3 tile_hover.py <TILE>`. Both auto-detect window position via Quartz.
- Island-select click coords: `python3 island_select.py`.
- **End Turn button: `python3 game_loop.py click_end_turn`** — emits both legacy screen/global coords and `codex_computer_use_batch` window-local coords for Codex Computer Use. In Codex Computer Use, dispatch `codex_computer_use_batch` (or the per-click `window_x`/`window_y`), not the legacy `x`/`y`. The calibrated End Turn offset is `(126, 120)` window-relative.

## Core Game Rules (Solver-Critical)

The solver enforces these; use them when reviewing solver output or writing tests.

**Terrain kills:** Water and Chasm kill non-flying ground units. Lava kills like water but sets flying units on Fire. Pushing enemies into these is a primary kill method.

**Push:** 1 tile in a direction. If blocked by unit/mountain/edge, the pushed unit takes 1 bump damage instead. If blocked by a building, *both* pushed unit and building take 1 bump. Chain pushing (A pushed into B) doesn't move B — both take bump. Push/bump damage ignores Armor and ACID.

**Damage types:**
- Weapon damage: −1 from Armor, ×2 from ACID
- Push / bump / fire / blocking damage: ignores Armor and ACID
- Blocking damage fires when a Vek tries to emerge on an occupied tile

**Status effects:**
- **Fire:** 1 dmg/turn start. Removed by repair, water, or freezing.
- **Frozen:** invincible + immobilized. Any damage unfreezes (0 dmg dealt).
- **ACID:** doubles weapon damage. Persists until unit dies.
- **Smoke:** prevents attack AND repair. Cancels Vek attacks when on smoke tile at execution.
- **Shield:** blocks one instance of damage + negative effects. Removed by direct damage.
- **Armor:** −1 weapon damage (floor 0). No effect on push/fire/bump.
- **Webbed:** can't move, can still attack. Breaks only when the unit actually changes tiles, or when the webber moves/dies. A blocked push/bump leaves the unit webbed.

**Terrain HP:**
- **Mountain:** 2 HP (2=full, 1=damaged, 0=rubble/walkable). Any weapon damage reduces by 1.
- **Building:** 1 HP (objective buildings 2). Any damage destroys all on the tile. Reduces grid power by old HP.
- **Ice:** intact → cracked → water (non-flying drowns).

**Repair platforms:** Mission_Repair places `Item_Repair_Mine` tiles. Any live
unit that lands on one triggers the item, heals by the engine's `SpaceDamage(-10)`
(live captures show damaged 3-max-HP mechs can become `5/3`; 2-max-HP mechs cap
at `4/2`, so model this as `max_hp + 2`, not a flat 5 HP), consumes the platform,
and increments `repair_platforms_used` toward the 3-platform objective. If the
unit is already at or above max HP when it lands, the platform still consumes and
counts but does not add overheal. Raw progress can exceed the objective target
(for example `4/3`); clamp only in UI scoring/presentation. It is not the mech
Repair action; do not assume it clears Fire/ACID/Frozen unless a capture proves
the engine does so.

**Repair:** any mech can repair instead of attacking. +1 HP, clears Fire and ACID. Can't repair on smoke.

Extended rules: `data/ref_game_mechanics.md`.

## Operational Rules

1. Always verify state after each mech action (the bridge does this in `auto_turn` automatically). Save file only updates at turn boundaries; bridge updates per sub-action.
2. Never execute mech N+1 before the verify for mech N returns PASS, unless you consciously decide to log+continue past a desync.
3. Click tile centers, not sprites — sprites render 100–170 px above tile center. `click_action` / `grid_to_mcp` already do this.
4. After every failed run, analyze the critical turn. Save a snapshot first.
5. To select a mech in manual play, click its tile on the board. No portraits, no Tab, no keyboard.
6. Priority order: buildings > threats > kills > spawns.
7. **Default combat:** `python3 game_loop.py auto_turn --time-limit 10` each turn. It polls for player phase at entry, solves, executes all mech actions via bridge, emits an End Turn click plan. In Codex Computer Use, dispatch `codex_computer_use_batch` (window-local); in legacy batch tools, dispatch `batch` (screen/global). Manual-play fallback (`click_action`/`verify_action`/`click_end_turn`) is only for when the bridge is unavailable. Bridge sub-action commands (`move_mech`, `attack_mech`, `skip_mech`, `repair_mech`) are used internally by `auto_turn` — never call directly.
8. Never move onto ACID tiles voluntarily — doubles damage, disables armor.
9. **Self-improvement:** every process error triggers an immediate AGENTS.md update with a guard or fix. Every mistake makes the process permanently better.
10. Bridge-to-visual coordinate rule (see Execution Model). Use A1–H8 in all communication.
11. **No keyboard during combat.** End Turn is the only MCP click needed per turn. `click_end_turn` / `auto_turn` emit legacy `batch` coords in screen/global space plus `codex_computer_use_batch` coords in Codex window-local space. After `get_app_state`, use `codex_computer_use_batch` or the visible End Turn button center, then verify the screen switches to ENEMY ACTIVITY / ENEMY TURN.
12. Use all mech actions every turn — even suboptimal moves beat skipping.
13. The solver handles environment hazards via `environment_danger_v2`: each entry is `[x, y, damage, kill_int]`. `kill_int=1` = lethal (Air Strike, Lightning, Cataclysm→chasm, Seismic→chasm, Tidal→water; bypasses shield/frozen/armor/ACID). `kill_int=0` = non-lethal (Wind/Sand/Snow). Env ticks fire BEFORE Vek attacks in enemy phase.
14. On crash/timeout recovery, always start with `cmd_read` + `cmd_solve`. Never resume a previous solution — the board may have changed.
15. Save file updates only at turn boundaries. Bridge does not have this limit — `verify_action` reads fresh bridge state.
16. **Deployment:** prefer `python3 game_loop.py deploy_recommended`, which sends bridge `DEPLOY uid x y` commands for the ranked FORWARD/MID/SUPPORT tiles and verifies the placed mech coords. Click the visible CONFIRM button afterward. If the bridge helper is unavailable, fall back to `game_loop.py read` MCP coords and click the 3 recommended tiles manually, then CONFIRM.
17. **Hover-verify-click — for novel UI only.** Before `left_click`-ing an unfamiliar UI element (shop items, reward cards, new menus), do: `mouse_move` → `screenshot` → `left_click`. **Exempt:** the End Turn button (use `codex_computer_use_batch` in Codex Computer Use; calibrated offset `(126, 120)` relative to the window) and any combat tile click computed by `grid_to_mcp()` (calibrated <2px — `click_action` handles internally).
18. **Prefer `game_loop.py read` over screenshots for combat state.** Screenshots are 100–200 kB each and accumulate fast. Reserve screenshots for novel UI screens (defeat, rewards, shop, CEO cutscenes) and for verifying unexpected state.
19. **Always follow the solver — never override.** The solver encodes push directions, bump damage, projectile re-aiming, terrain interactions, env_danger ordering — your manual analysis will be wrong or incomplete. Execute what `game_loop.py solve` outputs. When `verify_action` reports a desync, the failure_db record is the useful signal (it feeds the auto-tuner). Overriding the solver suppresses that data. Only exception: solver returns empty (timeout) — play manually via screenshot reasoning. Rationale: `feedback_trust_solver.md`.
20. **Research gate — never solve past an unknown.** When `game_loop.py read` flags `requires_research: true`, or when `solve` / `auto_turn` return `error: RESEARCH_REQUIRED`, the self-healing loop has detected a pawn type, terrain, weapon, or screen we've never catalogued. Solving past it produces confidently-wrong plays. Do this before resuming combat: (a) `game_loop.py snapshot research_gate_<turn>` for rollback, (b) `game_loop.py research_next` to get the MCP capture plan, (c) dispatch `plan.batch` via `mcp__computer-use__computer_batch`, zoom each `plan.crops[i].region`, run Vision with each bundled `prompts[crop_name]` to produce JSON per crop, (d) `game_loop.py research_submit <research_id> '<json>'`. If the submit result includes `community_queries.queries`, (e) `WebFetch` each URL (Steam discussions + r/IntoTheBreach), summarize each into `{url, excerpt, confidence}` and (f) `game_loop.py research_attach_community <research_id> '<notes_json>'` to persist the corroborating community notes. After a display-name submit (e.g. "Centipede") for a tiered bridge type (e.g. `Centipede1`), ensure the exact bridge type(s) are present in `data/known_types.json`; otherwise `research_next` can say `NO_WORK` while `auto_turn` still gates on the tiered type. Repeat until `research_next` returns `status: NO_WORK`. Only then `auto_turn`. Rationale: `docs/self_healing_loop_design.md` §Missing wire.
21. **Codex Computer Use coordinate caveat.** `research_next`, legacy `grid_to_mcp` plans, and `tile_hover.py` emit MCP/global coordinates for the old batch tool. The Codex Desktop `mcp__computer_use__.click` tool expects screenshot/window-local coordinates, so don't paste those MCP coords directly into it — use the emitted `codex_computer_use_batch` when present, the visible tile center from the screenshot, or a helper that explicitly says its coords are Codex window-local. This came from a 2026-05-04 research miss where an MCP coord intended for `Snowtank1` selected the wrong visible unit, and recurred on 2026-05-09 when a `tile_hover.py G1` coordinate selected a building instead of `Starfish2`.
22. **Simulator version discipline.** `SIMULATOR_VERSION` in `src/solver/verify.py` pins the semantic behavior of the Rust + Python simulators. Bump it whenever a simulator change alters predictions on a given board (e.g., Storm Generator implemented, Psion regen applied, Spartan Shield fixed, PushDir::Flip semantics changed). Weight-tuning changes do NOT bump it. Every new solve recording and failure_db entry is stamped with the current version. Before bumping: `cp recordings/failure_db.jsonl recordings/failure_db_snapshot_sim_v<old>.jsonl` to archive the pre-bump corpus. `cmd_tune` refuses to run on a mixed-version corpus unless `--accept-version-change` is passed.
23. **Grid-drop investigation gate — fire now, not later.** When `auto_turn` returns `status: "INVESTIGATE"`, the solver's prediction lost grid power in a way the sim didn't model. This is exactly how Prime_ShieldBash and Ramming Engines' empty-charge self-damage bugs surfaced — we missed the signal for multiple runs. Never click `pending_end_turn_batch` before resolving. For each entry in `investigations`: (a) read the `snapshot_path` (has `predicted.json`, `actual_board.json`, `context.json` with `grid_power_diff` + `building_hp_diffs` + `failure_db_id`), (b) spawn an Explore agent with those three files and the prompt "trace this unpredicted grid loss to a weapon-def, simulator, or push-chain bug — propose a fix with file+line and confidence level", (c) if the agent returns a concrete fix with high confidence, apply it, rebuild Rust if `rust_solver/src/*.rs` changed, run `bash scripts/regression.sh` to verify nothing else regressed, (d) if fix applied, `replay <run_id> <turn>` on the new solver to confirm it now predicts the actual grid power. Only then dispatch `pending_end_turn_batch` via `computer_batch`. If the agent cannot propose a fix (low confidence / needs more data), log that in the decision log, dispatch the batch, and continue — not every mystery is fixable mid-turn.

24. **Diagnosis loop — "drain the queue", "run the loop", "diagnose this run" all mean the same protocol.** When `verify_action --diagnose` is on (or env `ITB_AUTO_DIAGNOSE=1`), desyncs accrue in `session.diagnosis_queue`. The harness — that's you — drains the queue **between turns**, NEVER inside `auto_turn` (45s enemy-animation budget; design doc §13 #17). Per-call protocol:

    a. **Drain one entry.** `python3 game_loop.py diagnose_next` pops one pending failure, runs Layer 2 (rejections → known_gaps → rules.yaml). Outputs `rule_match` / `needs_agent` / `insufficient_data` / `rejected` / `EMPTY`. On `EMPTY`, stop and tell the user — don't spin polling.
    b. **`needs_agent` → dispatch.** Read the `## Agent prompt` block from `recordings/<run_id>/diagnoses/<failure_id>.md`, dispatch via the available agent/delegation tool with an exploration-oriented subagent. The validator tolerates prose-wrapped + multi-`{...}` agent output (last block wins, braces inside string literals are skipped) but enforces `target_language=rust`, every `suspect_files[*].path` resolves, every cited line exists, `confidence ∈ {high,medium,low}`, `fix_snippet.before` and `.after` both non-empty. Pipe the JSON via `python3 game_loop.py diagnose_apply_agent <id> <json|path>`. On validator failure: read the error list, fix the JSON OR `reject_diagnosis` if the proposal is bad on its merits.
    c. **`rule_match` → leave alone.** Rule fixes are conceptual prose (e.g. "guard set_active(false) on weapon_id != WId::None"), not Edit-applicable. Surface the rule_id + suspect_files to the user; `apply_diagnosis` will refuse a rule_match anyway.
    d. **`agent_proposed` → ASK before applying.** Do NOT auto-fire `apply_diagnosis`. It mutates source files, atomically bumps SIMULATOR_VERSION in BOTH `rust_solver/src/lib.rs` and `src/solver/verify.py`, archives `failure_db.jsonl`, runs `bash scripts/regression.sh`, and reverts on failure. Always `apply_diagnosis <id> --dry-run` first to surface the plan, then ask the user before the real apply. Multi-location fixes (function signature changes that need both def + call sites edited) currently can't be expressed in one `fix_snippet` — design doc §15.1 known limitation; reject those with a reason citing it.
    e. **`apply_diagnosis` refused on dirty workdir → surface, don't auto-resolve.** Refusal is by design (don't clobber user work). Print the dirty paths to the user; do NOT `git stash` or `git commit` on their behalf to "make it apply". Per AGENTS.md "Executing actions with care".
    f. **`apply_diagnosis` succeeded → don't auto-commit.** The memory rule about auto-pushing routine commits doesn't apply to auto-applied sim changes — those need eyeballs. Show `git diff`, ask the user to commit. The commit message should include the failure_id and the agent's confidence.
    g. **`reject_diagnosis <id> --reason "..."` is the feedback channel.** Use it whenever a proposal is wrong (incomplete fix, wrong direction, would not compile, regresses corpus). The (diff_signature × sim_version × proposed_fix_sig) gets recorded in `diagnoses/rejections.jsonl` so the same wrong proposal can't recur. `--force` on diagnose bypasses, for when a rejection turns out to have been premature.
    h. **One drain per call** unless the user says "drain all" / "clear the queue" — agent dispatches cost real tokens. Report queue depth and ask before continuing. Exception: if every pending entry resolves to `rule_match` or `insufficient_data` (no agent dispatch), drain them all in one pass — they're cheap.

    Full spec: `docs/diagnosis_loop_design.md`. Live-run hardening retrospective: §15.1.

25. **New-run setup is achievement-aware: difficulty + Advanced Edition ON + the target squad.** Updated 2026-05-13 for the active Hard Victory push. Balanced Roll remains correct for `--mode solver_eval` and random-squad achievements, but normal achievement hunting should first run `python3 game_loop.py achievements --sync` when `.env` has `STEAM_API_KEY` and `STEAM_ID`, then pick an actual unfinished squad via `python3 game_loop.py recommend_squad --tags achievement` / `python3 game_loop.py new_run auto --tags achievement`. For ordinary squad achievements, use **Easy** (`--difficulty 0` or omit the flag). For **Hard Victory**, explicitly use **Hard** (`--difficulty 2`), Advanced Edition ON, and the chosen target squad (currently Rusting Hulks). Don't fall back to another difficulty after defeats unless the run target changes. Rationale: `feedback_playstyle.md` plus squad-locked achievement targeting.
26. **Session lock discipline:** never run `game_loop.py read`, `status`, `solve`, `deploy_recommended`, `auto_turn`, `diagnose_next`, `diagnose_queue`, `research_next`, `research_submit`, `research_resolve`, or other session-touching game-loop commands in parallel. They share `sessions/active_session.json.lock`; run them one at a time, then parallelize only independent file reads or analysis commands. Do not tuck even a harmless-looking `status` / `diagnose_queue` call into `multi_tool_use.parallel` beside `rg` / `sed` / `git status`, beside helper scripts such as `island_select.py`, `tile_hover.py`, or file reads used during research targeting, or beside Computer Use `get_app_state` / screenshot reads; issue the game-loop command separately, then do UI inspection or file/helper reads after it exits. During research-gate cleanup, run `diagnose_queue` as its own terminal call before or after `rg` / `jq` checks, never in the same parallel batch. If a remembered command such as `research_queue_peek` is not listed by `game_loop.py -h`, do not probe it inside a parallel batch; check or run the intended real command alone. This also applies during documentation/commit cleanup: if you need `status`, run it alone first, then fan out file reads afterward.
27. **No repo-wide Rust formatting during tactical fixes.** This Rust tree is not globally `cargo fmt`-clean; running `cargo fmt` rewrites many unrelated files and hides the simulator change under formatting noise. For live/debug fixes, keep Rust edits hand-scoped to touched hunks. Only run a repo-wide formatter as its own explicit cleanup task.
28. **Do not chain session-mutating commands.** Even with `&&`, commands like `py_compile && game_loop.py read` make live logs harder to audit and can hide which step changed session state. Run formatting/compile checks and game-loop commands as separate terminal calls. This includes one-off research cleanup checks: run any `rg` / file validation first, then run `game_loop.py research_resolve` in its own command.
29. **Final-island save phase anomaly.** During `Mission_Final` and `Mission_Final_Cave`, `saveData.lua` can report `mission_ending` while the Lua bridge and visible screen are still in live combat. If `cmd_read` / `auto_turn` says `mission_ending` but the screen shows active final combat, trust the fresh bridge when `mission_id` is `Mission_Final` or `Mission_Final_Cave` and at least one mech is active. Conversely, if `auto_turn` waits out with `active_mechs=0`, it now returns `status="TERMINAL_OR_MISSION_END"` and clears the stale active solution; check the visible screen for Victory / Region Secured / rewards / defeat before issuing more combat commands.
30. **Safety block emergency protocol.** On a safety block, `cmd_solve` now automatically widens to `solve_top_k(..., 100)` and, if the session has temporary soft-disabled weapons, runs a second emergency top-k pass with `disabled_actions` removed from the payload. This does **not** mutate `session.disabled_actions`; it only lets the current candidate search recover a clean plan. If it still returns `SAFETY_BLOCKED`, use manual analysis / `--allow-dirty-plan` only with exact dirty-token acceptance (see rule 37). On the final bomb turn, a dirty plan is acceptable only when the predicted grid remains above 0, the Renfield Bomb survives, and no clean candidate is found. A final-cave pylon loss is allowed only as an emergency exception after a deep search finds no pylon-preserving candidate, all mechs survive without HP/status/danger debt, no other objective loss is predicted, and the exact dirty token is used.
31. **Research queue cleanup.** If `research_next` reports stale pending known-type entries, use `python3 game_loop.py research_resolve <TYPE> --kind behavior_novelty --reason "<why>"` rather than hand-editing `sessions/active_session.json`. The command validates `<TYPE>` against `data/known_types.json` and records a `manual_resolved` result with attempts/diff metadata.
32. **Research queue peek still matters.** `research_queue_peek` is a JSON result field, not a command. For a standalone read-only check use `python3 game_loop.py research_peek`; to process work use `research_next` / `research_submit`. If `auto_turn` completes but its `research_queue_peek` contains a current-board unknown such as `BigBomb`, snapshot and resolve that queue item before advancing another turn, even if no hard `RESEARCH_REQUIRED` gate fired.
33. **Deployment roster supplementation is built in.** `deploy_recommended` merges live bridge mechs, the last active solution, and save-file `GameData.current.mechs` into stable UIDs 0/1/2. If the bridge lists only two unplaced mechs, do not manually deploy the third first — run `deploy_recommended` and let the bridge `DEPLOY uid x y` verification tell us whether a UID is truly missing.
34. **Projectile-grapple attacks are line attacks, not melee.** Burnbug/Gastropod proboscis weapons (`BurnbugAtk1`, `BurnbugAtk2`, `BurnbugAtkB`, `GastropodAtk1`, `GastropodAtk2`) trace from the attacker's current tile along the original queued direction until the first projectile blocker, then damage that blocker and grapple: hit pawns are pulled toward the attacker, objects pull the attacker toward the object. A vacated first target tile does NOT cancel the shot. If an unexpected grid loss follows a moved Burnbug/Gastropod, first check for a building farther down that line. Regression anchor: Normal run `20260504_210332_088`, mission 1 turn 1, F7 `(1,2)` building loss fixed in simulator v44.
35. **Trust engine terrain ids over stale terrain names.** Shipped map files and Lua constants have terrain id `5` = Ice, not Lava. Older bridge builds mislabeled id 5 as `"lava"`, causing false terrain-death predictions on Pinnacle ice and the Coal Plant loss chain in Normal run `20260504_210332_088`, mission 1 turn 2. Parser code must prefer `terrain_id` normalization when present; bridge Lua must derive `TERRAIN_NAMES` from engine globals (`TERRAIN_ICE`, `TERRAIN_LAVA`, etc.) instead of hand-maintaining numeric tables.
36. **Enemy attack intent must be save-gated when available.** `GetSelectedWeapon() > 0` can stay nonzero on non-attacking Vek after movement/re-aim animation, causing Rust's missing-target phantom attack guard to invent false building damage. When `saveData.lua` exposes `iQueuedSkill`, bridge code must derive `has_queued_attack` from `iQueuedSkill >= 0` and only fall back to `GetSelectedWeapon()` when the save field is missing. Regression anchor: Normal run `20260504_210332_088`, mission 7 turn 3, Firefly1 uid `33` falsely threatened B5 and produced a false safety block; fixed in bridge + simulator v47.
37. **Dirty-plan acceptance is exact and single-use.** `--allow-dirty-plan` by itself is not enough for a blocking plan. `solve` / `auto_turn` emits a `dirty_consent_id` tied to run, mission, turn, candidate rank, input fingerprint, loss profile, and exact action list. Only rerun with `--allow-dirty-plan --dirty-consent-id <id>` after that exact line is reviewed; the token is consumed once. If a verify desync forces `auto_turn` to partially re-solve and the new plan predicts grid loss, building loss, mech death/status debt, pylon loss, or objective loss, stop with `SAFETY_BLOCKED` and ask again before executing the new line. Predicted timeline collapse is not dirty-consentable; final-cave pylon loss is dirty-consentable only under the emergency exception in rule 30.
38. **Buildings Immune weapon upgrades must be explicit.** The Lua bridge reports the pawn's base SkillList, so powered upgrades that change semantics need save-file overlays plus Rust `WId` support. `Ranged_Artillerymech_A` is Artemis Artillery with direct Grid Building damage set to zero; push/bump collision damage is still physical and can still hurt buildings. If a save loadout contains a new `_A` / `_B` / `_AB` weapon and the overlay drops it, add the upgraded ID before trusting the solve.
39. **Controllable mission allies count if they have weapons.** Friendly non-mech units such as `Archive_Tank` can be READY and player-controlled even when `mech=false` in bridge JSON. If `read` shows a READY friendly with a weapon and the solver omits it, first check whether that weapon has a Rust `WId` mapping. `Deploy_TankShot` / Stock Cannon is a 0-damage projectile with forward push; ignoring it can turn a salvageable tank mission into a false grid-zero safety block.
40. **Dirty frontier before dirty consent.** When a solve safety-blocks and reports `dirty_frontier`, inspect the tradeoff classes before asking to continue. The top raw-score dirty plan may not represent the best strategic compromise; compare options such as `grid_loss`, `mech_loss`, `building_loss`, and `objective_loss` explicitly, especially on Normal where a single dirty Turn 1 can cascade into timeline loss.
41. **Lookahead frontier is diagnostic until promoted.** `project_plan_scenarios` returns bounded plausible next-turn enemy-intent scenarios: base `heuristic_requeue` plus a few high-value building retargets. `lookahead_frontier` in solve output previews worst projected recovery across those scenarios, and `robust_frontier` ranks tradeoffs by current score + worst next-turn score, but neither selects live actions yet. Treat both as evidence for dirty-plan triage and future robust-beam tuning, not as permission to bypass safety.
42. **Rocket Artillery center-kill pushes are weapon-specific corpse bumps.** `Ranged_Rocket` center damage and forward push are simultaneous: if the center target dies, its corpse still resolves the Rocket center push. A dead pushable center target can bump a live blocker; a dead non-pushable center target can still bump static blockers such as buildings/mountains before disappearing. Do not generalize this to every push: Cluster Artillery outer corpse absorption still has a regression test where a dead train corpse does not damage a live adjacent unit. Regression anchors: Easy run `20260506_114649_974`, Storage Vaults turn 1 Pulse mech death and turn 4 Jelly Armor building bump, simulator v55.
43. **Bridge-executed combat does not guarantee Reset Turn is usable.** After `auto_turn` mutates the board through bridge commands, the visible Reset Turn button may hover but refuse to open a confirmation dialog. If a bad bridge plan executes, snapshot immediately, fresh-read/solve from the actual board, and only rely on Reset Turn if the UI visibly opens the confirm prompt.
44. **Time pod recovery UI beats the crossed objective line.** Mission objective rows can look failed after a pod is collected, while the reward flow still shows `Pod Recovered` and the save has `podReward`. Treat the reward screen / save reward state as authoritative. In fallback parsing, numeric save pod state `1` means live/uncollected; recovered state `3` must not be treated as a live board pod.
45. **Manual plan scoring can be invalid-action dirty.** `score_plan` / `replay_solution` are diagnostic tools, not action validators. If a hand-written plan produces an `illegal_*` replay event such as `illegal_leap_landing:x:y:unit`, the plan is impossible in-game and must not be used as dirty-plan consent evidence. Regression anchor: Easy run `20260506_114649_974`, Disposal Vaults turn 4 false clean where Aerial Bombs was hand-scored onto live Spider2 at D2; fixed in simulator v57 by no-oping illegal Leap landings.
46. **Detritus Contraption barrages must target a non-source unit.** `Missiles_Shield` / `Missiles_OneDmg` inherit `Support_Missiles:GetTargetArea`, which excludes the firing Contraption tile even though the effect is global. Clicking the Contraption's own tile can spend the action without emitting any missiles. The solver must pick a live non-source unit as the representative target, and the simulator must exclude the source from Shield/Missile Barrage effects. Regression anchor: Easy run `20260506_114649_974`, Mission_Missiles turn 2 source-click no-op, simulator v59.
47. **Soft-disable masks must cover every WId.** New weapon IDs can exceed 127; if the disabled-action bitset only has one `u128`, a desynced high-ID weapon will not actually be suppressed on the next solve. Keep disabled masks wide enough for all `WEAPON_COUNT` entries and add/keep a regression whenever a newly catalogued weapon is above 127. Regression anchor: `Missiles_OneDmg` WId 135 stayed selectable after a turn-1 desync until simulator v59 widened the mask.
48. **Normal/Alpha Spiders are pushable.** `Spider1` / `Spider2` throw eggs but are still normal pushable Vek; do not mark them like Burrowers or Psions. If Repulse or another push leaves a Spider unmoved in prediction while the engine moves it, check `src/model/pawn_stats.py` and solve payload `pushable` enrichment before changing Repulse logic. Regression anchor: Easy run `20260506_114649_974`, Mission_Missiles turn 4 `Science_Repulse_A` pushed Spider1 from E5 to E6; fixed in simulator v60.
49. **Teleporter move-then-attack targets use the post-swap tile.** `pawn:Move()` fires Mission_Teleporter pads before the bridge sends `ATTACK`; if the solver keeps a self-AoE target on the pre-swap pad, `pawn:FireWeapon()` can silently spend the action as an invalid target and produce no Repulse/shield effect. Enumerate attacks, smoke checks, and no-op filtering from the post-teleport board while keeping `move_to` as the pad tile. Invalid diagonal SelfAoe replay events are no-ops, not centered attacks. Regression anchor: Easy run `20260506_114649_974`, Mission_Teleporter turn 4 `Science_Repulse_A` stale target D4 after teleport to E3; fixed in simulator v61.
50. **Every move-then-attack target is relative to the post-move tile.** The teleporter fix must not special-case only pad moves and accidentally leave normal moves targeting from the old tile. If `auto_turn` shows `click_miss|Brute_Jetmech|attack` where Aerial Bombs was legal from the pre-move tile but not the moved tile, check `enumerate_actions`: attack target enumeration, smoke checks, and effect filtering all need the post-move attack origin. Regression anchor: Easy run `20260506_114649_974`, Mission_Teleporter turn 4 dirty recovery emitted JetMech G1→D3 then Aerial Bombs at G3, which was only legal from G1; fixed in simulator v62.
51. **Post-action desyncs are an automatic investigation gate.** Any `verify_action` / `auto_turn` desync after a live bridge action (`click_miss`, `death`, `push_dir`, status/tile mismatch, etc.) must be investigated before resuming gamer-mode or selecting the next mission, even if the visible outcome is Region Secured or otherwise successful. Do not dismiss it as harmless because the mission survived. Minimum protocol: inspect the saved `*_action_*_verify.json`, the newest `recordings/failure_db.jsonl` entry, and the corresponding `*_solve_input.json`; reproduce with `itb_solver.solve` / `solve_top_k` when target legality or search behavior is suspect; decide whether it is a known benign bucket, stale UI/bridge timing, or a concrete solver/simulator/control bug. If a concrete bug is found, fix it immediately, add/keep a regression, rebuild the Rust wheel when `rust_solver/src/*.rs` changed, run the regression harness, and update AGENTS.md with the new guard before continuing.
52. **Partial re-solves must preserve upgraded weapon overlays.** `cmd_solve` patches bridge mech loadouts from save-file weapons because Lua reports base SkillList IDs, but `_re_solve_partial` must do the same after a desync. If an upgraded weapon such as `Science_Repulse_A` predicts base behavior after a re-solve (for example Pulse does get Shield Self in-game but predicted `status.shield=false`), check that `_enrich_bridge_mech_weapons_from_save` runs in the partial re-solve path before Rust solve/replay. Regression anchor: Easy run `20260506_114649_974`, Corporate HQ turn 1 after RocketMech status desync, Pulse Repulse predicted no shield while actual shield appeared; fixed in simulator v63.
53. **ACID pools are non-stoppable for player movement.** The operational "never move onto ACID voluntarily" rule is enforced in Rust movement enumeration, not merely by scoring penalties. If a plan moves a mech onto a tile with `tile.acid=true`, treat it as a movement enumeration bug before considering weapon logic; ACID-pool status reporting can be bridge-ambiguous after the move, so the safest policy is to keep those destinations out of search for player-controlled units. Regression anchor: Easy run `20260506_114649_974`, Corporate HQ turn 2 moved RocketMech D3→C5 onto an ACID pool and produced `status|Ranged_Rocket|move`; fixed in simulator v64.
54. **Smoke on a queued web source releases the web immediately.** Scorpion/Leaper-style current-turn webs are tied to the queued attack; when a player action places smoke on that enemy, the engine cancels the queued attack and `IsGrappled()` clears before the next mech acts. The simulator must break `web_source_uid` links when smoke is placed on a live enemy tile, including Rocket Artillery's behind-shooter smoke. Regression anchor: Easy run `20260506_114649_974`, Corporate HQ turn 3 RocketMech E3→B3 smoked ScorpionBoss at F3 and freed PulseMech at F4; fixed in simulator v65.
55. **Rocket Artillery damage upgrades need save overlays and Rocket semantics.** `Ranged_Rocket_A` / `Ranged_Rocket_B` are 3-damage Rocket Artillery, and `Ranged_Rocket_AB` is 4-damage Rocket Artillery. They must preserve the base weapon's smoke-behind-shooter and center corpse-push behavior. If live Rocket kills a target that sim leaves at 1 HP after a blocked push/bump, inspect save-file `state.weapons` and add the upgraded ID to both `_MODELED_UPGRADED_WEAPONS` and Rust `WId` support before advancing. Regression anchor: Easy run `20260506_114649_974`, Volcanic Hive final turn 2 `Ranged_Rocket_A` killed Scorpion2 at C6 with 3 damage plus blocked bump; fixed in simulator v66.
56. **Perfect Battle turns treat mech HP loss as blocking.** While explicitly chasing Perfect Battle, any `plan_safety` / dirty-frontier `mech_hp_loss` must stop before the End Turn click, even if the solver labels it `WARN` or the HP display looks over-max from pilot bonuses. Spawn-blocking damage, bump damage, fire, and any other mech HP drop all void the achievement. Do not rationalize the warning away; either find a no-mech/no-building-damage plan or explicitly abandon the Perfect Battle attempt before advancing. Regression anchor: Easy Rusting Hulks run `20260508_122657_124`, Mission_FreezeBots turn 1 predicted `mech_hp_loss` before End Turn and then actual Rocket/Pulse damage on enemy phase.
57. **Cannon-Bot Mark I is a projectile with fire.** `SnowtankAtk1` / Cannon 8R Mark I inherits `FireflyAtk1` in `scripts/weapons_snow.lua` and sets `Fire=1`; it is not melee. The shot travels down the queued cardinal line to the first projectile blocker and sets the hit unit on fire. If Mission_FreezeBots or Mission_Hacking shows a Cannon-Bot damaging a mech behind the first empty target tile, check this weapon definition before blaming movement or Ralph/max-HP display noise. Regression anchor: Easy Rusting Hulks run `20260508_122657_124`, Mission_FreezeBots turn 1 Cannon-Bot at G3 fired through G4/G5 into PulseMech at G6, leaving Pulse at 2 HP and on fire; fixed in simulator v67.
58. **Retire completed achievement blockers immediately.** `achievement_targets` can outlive the unlock inside an active session. After `achievements --sync` marks a target complete (including Steam client-cache promoted fresh unlocks), chase-specific safety blockers for that target must stop applying so the same timeline can pivot to the next objective. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Scrapyard turn 2 was safety-blocked by Perfect Battle mech-HP rules after `Ach_Rust_A_3` had already unlocked.
59. **AE Moth/Bouncer recoil is a real queued self-push.** `MothAtk1` / `MothAtk2` and `BouncerAtk1` / `BouncerAtk2` queue a backward self-push before their target hit, then push the target forward. A blocked recoil bumps both the attacker and the blocker, so a mech behind a Moth can lose HP even when the artillery target is elsewhere. If post-enemy HP is one lower than predicted around Moths/Bouncers, check self-push ordering before blaming spawn-blocking or HP display noise. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Scrapyard turn 2 Moth at E4 fired at E7 while PulseMech stood behind it at E3; live Pulse took recoil bump plus spawn-block damage; fixed in simulator v68.
60. **Mission_Trapped Coal Plant tiles may need inferred unique-building accounting.** The bridge can expose 2-HP Coal Plant structures as ordinary building tiles without `unique_building=true`, but live grid power still drops when one HP is lost to push/bump damage. On `Mission_Trapped`, infer objective-style grid accounting for 2-HP building tiles before solving; do not globally change non-unique 2-HP residential bump accounting without a fresh capture. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Scrapyard turn 3 Bouncer pushed a Moth into the D4 Coal Plant tile, reducing building HP 2→1 and grid 6→5; fixed in simulator v69.
61. **Dirty-plan selection should minimize same-class collateral.** If every candidate is safety-blocked and the dirty frontier has the same irreversible loss class (for example all lose exactly 1 grid / 1 building HP), do not blindly preserve the highest raw-score plan when it also causes avoidable mech HP loss. Widen the dirty frontier enough to inspect lower-ranked candidates and choose the same blocking-loss profile with the least non-fatal mech HP collateral before asking for dirty-plan consent. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Scrapyard turn 4 top dirty candidates nicked PulseMech, while rank 110 kept all mech HP intact with the same unavoidable grid/building loss.
62. **Leap weapons cannot land on chasm tiles.** Aerial Bombs / leap target enumeration must reject chasm landing tiles, and replay/score diagnostics must no-op illegal chasm landings instead of damaging transit tiles. Flying does not make a Cataclysm chasm a valid Aerial Bombs landing. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Mission_Cataclysm turn 3 JetMech B5→B3 click_miss where B3 was already chasm; fixed in simulator v70.
63. **Rocket Artillery center push does not edge-bump.** When `Ranged_Rocket` hits a live center target on the map edge, the engine applies Rocket damage but does not add a blocked-push bump into the off-board edge; the target stays in place. Keep Rocket's mountain/building/blocker corpse-bump semantics from rule 42, but do not count off-board edge bump as extra kill damage. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 2 RocketMech D7 fired at Moth1 on D1; live Moth stayed at D1 with 1/3 HP instead of dying to phantom edge bump, fixed in simulator v71.
64. **ACID status is not an instant pool under live targets.** ACID weapons such as `Acid_Tank_Attack` / A.C.I.D. Cannon acidify a live unit on the hit tile, but the ground beneath that unit does not become an ACID pool until an ACID unit dies there. Empty ground/rubble/water ACID hits can still create pools/tiles, and shielded or still-frozen targets can still use the "falls to feet" tile-acid rule when the unit cannot receive ACID. If a later move verify reports `tile_status` where `predicted acid=true` and `actual=false` on an occupied target tile, check for over-eager `apply_weapon_status` tile acid before blaming the moving mech. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Reclamation Zone turn 4 Acid Tank hit Scarab1 at E4, then JetMech D1→E5 surfaced the stale predicted pool; fixed in simulator v72.
65. **Starfish attacks are self-centered diagonal appendages, not melee.** `StarfishAtk1` / `StarfishAtk2` self-target the Starfish tile and damage the four diagonal tiles only. `StarfishAtkB1` / Starfish Leader does the same 3-damage diagonal pattern and additionally applies zero-damage outward pushes to the four cardinal adjacent tiles. If the bridge shows a Starfish queued target equal to its own tile, do not treat that as a no-op melee attack. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 1 StarfishBoss at E3 queued `StarfishAtkB1` on itself with eight telegraphed tiles; fixed in simulator v73.
66. **Aerial Bombs cannot land on water or lava.** Even though Jet Mech is flying, `Brute_Jetmech` / Aerial Bombs uses the weapon target area for its landing tile; forced water/lava landing clicks spend the attack as a no-op and do not damage or smoke transit tiles. Reject water/lava landings in target enumeration and replay diagnostics. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 2 JetMech D7 tried to land on B7 water over Blob1 at C7; live Jet stayed at D7 and Blob1 survived; fixed in simulator v74.
67. **Repulse edge pushes do not edge-bump.** `Science_Repulse` / Repulse is a zero-damage adjacent push. When an adjacent unit would be pushed off-board, the engine leaves it in place without adding the generic 1 bump damage. On-board Repulse pushes and blocked collisions still matter, but do not count off-board edge bump as kill damage for Repulse. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Corporate HQ turn 3 PulseMech D2 repulsed a 1-HP Blobber at D1; live Blobber stayed alive at D1 instead of dying to phantom edge bump, fixed in simulator v75.
68. **Web Egg adjacency webs are tile-based, but do not steal active grapples.** A live `WebbEgg1` webs every living unit in the four cardinal adjacent tiles, including units that move next to it after the egg already exists. Bridge `IsGrappled()` / grappler source can miss or misattribute this, so solver inputs and Rust landing/move simulation must infer adjacency and set `web_source_uid` to the egg when the unit has no live non-egg source currently targeting it. If a Scorpion/Leaper-style source is alive and its queued target is that unit's tile, preserve that source; killing the adjacent egg should not free the other grapple. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_FreezeBldg` turn 2 JetMech moved H2→F2 next to a F3 egg and became webbed, while Pulse remained webbed by the E2 Scorpion after the egg died; fixed in simulator v76.
69. **Burrower Slam hits the perpendicular three-tile row.** `BurrowerAtk1` / `BurrowerAtk2` are melee attacks with `path_size=3`: the queued direction selects the center target tile, and the engine also damages the two perpendicular flank tiles around that center. Do not simulate Burrowers as single-tile melee. If an objective/building loss follows a Burrower attack, first check for a missed flank tile in the slam row. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Stasis` turn 1 Coal Plant at C4 was lost by a Burrower1 flank that the sim missed; fixed in simulator v77.
70. **Save-file upgrade overlays must read pawn mod pips, not only `current.weapons`.** `GameData.current.weapons` can preserve base active weapon IDs after newly powered upgrades, while each `SquadData.pawnN` has the authoritative `primary_mod*` / `secondary_mod*` power lists. The Lua parser must preserve bare numeric arrays like `{3, 3}` as lists, because `{}` here erases powered upgrade evidence. When bridge SkillLists show base weapons, derive modeled upgraded IDs such as `Ranged_Rocket_A` and `Science_Repulse_A` from fully powered pawn mod lists before solving. Do not overlay unmodeled passives such as `Passive_Electric_A` if Rust intentionally keys the base passive ID. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle Thermal Dampeners deployment after Mission_Stasis: Rocket +1 Damage and Pulse Shield Self were powered in pawn mods, but `current.weapons` stayed `Ranged_Rocket` / `Science_Repulse`; fixed in simulator v78.
71. **Player artillery target areas are cardinal, not diagonal.** Standard player artillery weapons such as Artemis Artillery, Rocket Artillery, and Rock Accelerator fire along a row or column from the shooter. A bridge `FireWeapon` call can return OK on an off-axis point but spend the action as an engine no-op, so do not broaden `get_weapon_targets(... WeaponType::Artillery ...)` to board-wide targets. Regression anchors: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Reactivation` turn 2, RocketMech E6 fired `Ranged_Rocket_A` off-axis at C4 and live Burnbug1 survived untouched; Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, ArtiMech D6 fired Artemis at F2 and live Crab1/Firefly2 were unchanged. Fixed in simulator v80/v134.
72. **Diagnostic replay/score must reject impossible player actions.** `score_plan` / `replay_solution` are useful only when they obey the same legality gates as live search: movement destinations must be reachable and unblocked, smoke on the attack origin prevents attacks/repair, and weapon targets must be in that weapon's legal target area. If a hand-written plan looks clean but `solve` omits it, first check for `illegal_move:*`, `illegal_attack_smoke:*`, or `illegal_weapon_target:*` events before calling it a solver miss. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Pinnacle `Mission_Reactivation` turn 2: JetMech A4→A5 then Aerial Bombs at C5 was blocked by smoke, while RocketMech E6 off-axis C4 was outside Rocket Artillery's target area; diagnostic guards hardened in simulator v80.
73. **Bridge tile arrays are records, not coordinate-indexed grids.** Recorded `bridge_state["tiles"]` can be a flat list whose order is not `x*8+y`; each tile carries authoritative `x` / `y` fields. When inspecting raw solve inputs, build a lookup from `(tile["x"], tile["y"])` instead of indexing the list directly, or you can mislabel terrain/buildings and chase a false legality bug.
74. **Aerial Bombs bridge transit damage is adaptive.** `pawn:FireWeapon()` may or may not apply Aerial Bombs / Bombing Run transit terrain damage before the bridge workaround runs. The Lua bridge must snapshot transit terrain HP/state before `FireWeapon` and synthesize damage only when the engine left that tile unchanged; otherwise a 2-HP mountain/objective can be double-damaged. A mountain-only `building_hp` diff is a terrain desync, not a grid-drop gate. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Repair` turn 1: JetMech C3→C1 over C2 mountain became rubble after bridge double-applied `Brute_Jetmech` transit damage.
75. **Normal Psions are pushable.** The shipped `pawns.lua` `Jelly_Health1` family inherits default `Pushable=true`; do not inject `pushable=false` for `Jelly_Health1`, `Jelly_Armor1`, `Jelly_Regen1`, `Jelly_Explode1`, `Jelly_Lava1`, or AE Psions. If Repulse/Rocket predictions miss bump damage through a Psion into a building, check pawn stats enrichment before changing push physics. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Repair` turn 2: PulseMech Repulse at C4 bumped Blast Psion from C5 into the 2-HP C6 building, damaging both; fixed in simulator v81.
76. **Blast Psion explosions chain through eligible non-minor Vek.** `Jelly_Explode1` grants "all other Vek explode on death"; if a Blast Psion aura burst kills another ordinary non-minor Vek, that secondary death emits its own 1-damage cardinal burst. Minor pawns still do not receive the aura, so old Totem/Spore/Blob cases remain non-chaining, and intrinsic Volatile Vek decay stays separate. Regression anchors: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Repair` turn 3, Totem D2 did not explode because it was Minor; Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, R.S.T. Perimeter turn 2, Dash Punch killed Burnbug C4, whose burst killed Moth B4 / Leaper C3 and secondary bursts destroyed B3; corrected in simulator v125.
77. **Dam flood drown deaths still fire death side effects.** Old Earth Dam flood converts eligible tiles to water, and grounded non-massive Vek drowned by that water must go through the normal instant-death bookkeeping: kill credit, Psion cleanup, Blast Psion aura burst, and Volatile decay. Do not raw-set `hp = 0` in flood helpers. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Dam` / Central Museums turn 2: Rocket destroyed the dam, flood drowned Shaman2 at D4, and live Blast Psion aura damaged the adjacent 2-HP Robotics Lab at D3; fixed in simulator v83.
78. **Direct building damage drains grid per HP; bump exceptions stay narrow.** Direct weapon/explosion building damage reduces current Grid Power by each building HP lost, even when a 2-HP building survives at 1 HP. Non-unique multi-HP push/bump collision damage remains the narrow exception: it preserves grid on partial damage and drains only on destruction, while unique/inferred objective buildings drain per HP on bump. Beware the animation gap: a first bridge read can show `phase=combat_player` with `active_mechs=0` and stale grid before the delayed grid drop settles. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Dam` / Central Museums turn 2: Blast Psion aura damaged a 2-HP D3 building 2→1, and the grid dropped 5→4 only after the player phase fully activated; corrected in simulator v85 plus post-enemy recording guard.
79. **Minor Vek do not receive Psion aura bonuses.** Units marked `Minor = true` in Lua (Blobs, Spiderlings/WebbEggs, Shaman Totems/Spores, etc.) are still enemy units for attacks, collision, and threats, but they are not affected by Psion bonuses: Blast/Boss death explosions, Soldier HP, Shell armor, Blood regen, Fire immunity/on-death fire, Boost damage, or Spider Psion eggs. Do not infer Minor from old local tables that marked `Leaper1`; shipped `Leaper1` is a normal non-minor Vek. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive `Mission_Dam` / Central Museums turn 2: flood-drowned `Totem2` at E4 did not emit a Blast Psion burst, so Pulse Mech only lost shield, not HP; fixed in simulator v84.
80. **Blobber Leader is not an Alpha Blobber alias.** `BlobberBoss` uses `BlobberAtkB` to spawn a 2-HP minor `BlobB`; that Blob Leader uses `BlobAtkB`, taking 1 self damage while dealing 2 damage to the four cardinal adjacent tiles. The split inner/outer damage matters: aliasing to `BlobberAtk2` / `BlobAtk2` predicts the wrong spawned pawn and often kills the Blob Leader too early. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Archive Corporate HQ turn 1 research gate found `BlobberBoss` at F2 and `BlobB` at F6; fixed in simulator v87.
81. **Move-status effects can settle after bridge MOVE.** A bridge `MOVE` can return before tile fire status finishes applying to the moved pawn; if a per-sub-action verify sees only predicted-true / actual-false unit status diffs, refresh and re-read briefly before logging a simulator desync. Flying units still catch ordinary tile fire once the state settles. Regression anchor: Easy Rusting Hulks run `20260508_134925_472`, Mission_Final_Cave turn 4: JetMech moved H3→F4 onto burning ground, immediate verify saw `fire=false`, but a settled bridge read showed `fire=true`; handled in simulator v89 plus auto-turn settle retry.
82. **Ramming recoil on sand creates smoke.** `Brute_Beetle` / Ramming Engines self-damage is engine damage on the charger's final tile; if Charge Mech ends on sand after an impact, the sand becomes ground with smoke. Do not limit sand→smoke terrain mutation to target weapon damage only. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Mission_Terratide / Razor Bay turn 1: ChargeMech C3→E3 recoil converted E3 sand into smoke; fixed in simulator v90.
83. **Focused Rust unit tests need the cargo test harness flags.** This PyO3 crate defaults to `extension-module`; direct local unit-test runs must use one filter plus `--no-default-features`, and on macOS may need `DYLD_FRAMEWORK_PATH=/Applications/Xcode.app/Contents/Developer/Library/Frameworks` so the test binary finds Python3.framework (for example `CARGO_INCREMENTAL=0 DYLD_FRAMEWORK_PATH=/Applications/Xcode.app/Contents/Developer/Library/Frameworks cargo test --no-default-features sand`). Passing multiple test-name filters, omitting `--no-default-features`, or omitting the framework path can fail before the useful simulator test signal. Rust unit tests that call Python APIs (`Python::with_gil`, pyfunctions, etc.) also need `pyo3::prepare_freethreaded_python()` before acquiring the GIL because this crate does not enable PyO3 auto-initialize in the test harness.
84. **Mission preview boards can absorb map clicks as Start Mission.** Once a red-region preview is open, the displayed mini-board is an active Start Mission target. Do not click through the board area while trying to compare other red missions; first exit/close the preview with a known-safe UI action or click a clearly separate map region outside the preview board. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, R.S.T. island after Tesla Fields: a comparison click on the open Blast Garden preview started deployment.
85. **Diagnostic score_plan must include spawn blocking.** `score_plan` is only an audit helper, but its post-enemy summary must still mirror replay/project_plan ordering: mech actions → enemy attacks/environment → spawn blocking. If a hand-scored repair or shield plan looks clean while the solver says a mech dies on an emerging Vek tile, first suspect missing spawn-blocking in the diagnostic path rather than a solve miss. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Mission_Volatile / Erosion Flats turn 2: webbed 1-HP Defense Mech at F3 repaired to 2 HP, then still took Scorpion damage plus spawn-blocking damage; fixed in simulator v91.
86. **Shield Projector is a building-defense action, not just unit support.** `Science_Shield` is `LineArtillery`: it can target live building tiles, shields the target plus the next tile in the shot direction, and a shielded building consumes the next damage instance before losing HP/grid. Do not filter shield artillery targets with the normal "never target buildings" artillery guard, and carry building tile shields in Rust tile flags/bridge extraction. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Corporate HQ `Mission_BurnbugBoss` dirty-chain deep dive: the solver could not consider shielding C3/F3-style building threats, forcing repeated dirty plans; fixed in simulator v92.
87. **Mission_BoomBots `*_Boom` pawns have Explosive Decay.** `Snowtank1_Boom`, `Snowlaser1_Boom`, and `Snowart1_Boom` use Lua `Explodes=true`: on death they deal 1 damage to the four cardinal-adjacent tiles, can kill adjacent mechs, damage/destroy buildings, and ignite forests into burning ground. Treat this as intrinsic Volatile-style decay rather than an enemy attack. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Pinnacle `Mission_BoomBots` / Central Processor turn 2: Defense Mech pulled `Snowart1_Boom` into itself, destroying the E5 building, killing Defense, damaging/burning Laser, and dropping grid after animation settled; fixed in simulator v93.
88. **Burst Beam Ally Immune needs upgraded weapon IDs.** `Prime_Lasermech_A` / `Prime_Lasermech_AB` must overlay from save-file powered mods; the Lua bridge's base SkillList only says `Prime_Lasermech`. The Ally Immune upgrade skips player-side unit damage but still lets the beam pass through the friendly tile and continue normal damage decay, while buildings/terrain remain vulnerable. If a C-Beam line appears to kill a friendly despite powered Ally Immune, check save overlay + Rust `WId` mapping before rejecting the line. Regression anchor: Easy Zenith Guard run `20260510_000847_773`, Detritus `Mission_AcidStorm` / Containment Zone D turn 4: LaserMech B3→A6 fired at B6, hitting B6/C6/E6/G6 through ScienceMech at D6 and unlocking Glittering C-Beam; fixed in simulator v94.
89. **Titan Fist Dash needs upgraded weapon IDs.** `Prime_Punchmech_A` / `_AB` must overlay from save-file powered mods; the Lua bridge's base SkillList only says `Prime_Punchmech`. Dash Punch is a `Charge`-style direction selector with no self-damage: click the adjacent cardinal direction tile, the Combat Mech charges to the last free tile, then punches the first blocker. `Prime_Punchmech_B` remains melee with 4 damage. The Lua script sets Dash cost to 2 cores and +2 Damage to 3 cores; do not assume cheaper metadata. Regression anchor: Ramming Speed setup on Easy Rift Walkers run `20260510_213059_819`, fixed in simulator v95.
90. **Safety widening must be wide enough before dirty consent.** When `auto_turn` safety-blocks, the emergency top-k pass must search deep enough to catch lower-ranked clean plans before asking for `--allow-dirty-plan`. Rank-515 clean Rift Walkers plan (PunchMech F4→E5 punches D5, TankMech fires F6, ArtiMech D7→C6 fires H6) was missed by a 500-candidate widening window and incorrectly looked like forced G5 grid loss. Keep `_SAFETY_WIDENING_TOP_K` at least 1000 unless a larger diagnostic replaces it. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Mission_Train turn 2.
91. **Mission preview mini-board hitboxes are wider than they look.** If a preview is open and you want to switch to another red region, do not click through any area visually overlapped by the preview mini-board or its projected diamond, even if the target label seems separate; that can start the current preview mission. Use a clearly outside-map/UI close path or accept the current mission intentionally. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, after Pumping Station, a click meant for Venting Center started Reprocessing.
92. **Installed reactor cores are not temporary cross-mech rentals.** A core installed into a mech's reactor stays on that mech; you can toggle powered upgrades within that mech, but do not assume the core can be pulled back and reassigned to another chassis later. When an achievement needs a specific upgraded weapon (for example Titan Fist Dash on Combat Mech), keep newly bought cores uninstalled or install them directly into the required mech. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, after Detritus Corporate HQ, a "temporary" Artillery Buildings Immune core could not be moved to Combat Mech for Ramming Speed.
93. **Mountain-destruction objectives need explicit attention until scorer coverage is proven.** Missions such as R.S.T. `Destroy 2 mountains` can be won while silently failing the mountain objective if the live solve spends all actions on kills/threats. Before ending the last two turns on these missions, check objective progress on the visible panel or solve output; if progress is still 0, prefer a clean mountain-damage line when grid/objectives remain safe, and file/fix scorer support after the run. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Thunderbolt Grid: kill-count and Coal Plant succeeded, but `Destroy 2 mountains` finished at 0/2.
94. **New Rust weapon IDs must refresh `known_types`.** After adding a `WId` variant or bridge-observed upgraded weapon ID, update `data/known_types.json` (or regenerate it and inspect the diff) before resuming live combat. Otherwise the research gate can block on a weapon the simulator already supports while `research_next` has no visual work to perform. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Restricted Area turn 1: `Prime_Punchmech_A` was modeled in simulator v95 but missing from `known_types`, so `auto_turn` returned `RESEARCH_REQUIRED` with `research_next` reporting `NO_WORK`.
95. **Do not bypass soft-disables during safety widening.** A soft-disabled weapon has already produced live evidence that its predicted outcome is unreliable. Emergency top-k widening should still search deeper, but it must keep `disabled_actions` in the payload; removing the mask can promote a plan that appears clean only because it repeats the bad prediction. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Mercury Ridge: `Brute_Tankmech` over-predicted an edge-bump kill on Scorpion1, the survivor plus later cataclysm flow killed Ralph, and the mission outcome only surfaced the KIA after the bridge phase flipped to `unknown`.
96. **Taurus/Artemis edge-push kills are v96 regression anchors.** Simulator v96 models Taurus Cannon direct edge pushes and Artemis adjacent edge pushes as weapon damage only, with no off-board bump damage; on-board blocker bumps remain intact. If a future solver/wheel again predicts a `Ranged_Artillerymech` or `Brute_Tankmech` off-board edge-push kill that the live board leaves at 1 HP, fresh-read before End Turn and treat it as a simulator regression. Regression anchors: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Restricted Area turn 3 Hornet1 survived Artemis edge push; R.S.T. Mercury Ridge turn 1 Scorpion1 survived Taurus edge push.
97. **Never double-click End Turn after an ambiguous transition.** After dispatching an emitted End Turn click, do not click End Turn again unless a fresh `auto_turn`, `read`, or `click_end_turn` command explicitly emits a new End Turn plan for the current turn. If the screen returns from enemy activity with the victory counter reduced, or if the End Turn button state is visually unclear, treat that as the next player turn and run `auto_turn --time-limit 10`; a second manual End Turn click can skip an entire turn. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, R.S.T. Corporate HQ turn 2 was skipped after an ambiguous End Turn transition, causing ArtiMech destruction before Turn 3.
98. **`execute` is session-mutating; never include it in diagnostic fan-out.** `game_loop.py execute <index>` sends the stored solver action through the Lua bridge in bridge mode, so treat it like `auto_turn`, `read`, `solve`, `verify_action`, and `deploy_recommended` for lock discipline. If you are inspecting command plumbing or dirty-plan behavior, read source files first; run `execute` only as a standalone command after explicit intent to mutate the live board. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Archive Safeguard Valley turn 3, where `execute 0` was accidentally bundled with read-only source inspection while confirming how to carry out an operator-approved objective-loss plan.
99. **Titan Fist Dash uses projectile-path AddCharge, not normal ground charge pathing.** `Prime_Punchmech_A` / `_AB` Lua computes the first projectile blocker with `GetProjectileEnd(..., PATH_PROJECTILE)` and then `AddCharge(Board:GetSimplePath(...))`, so Dash Punch can cross water/lava instead of stopping before it. Also, a killed Dash Punch target can still resolve the forward push into a live blocker behind it. Do not treat a `Prime_Punchmech_A` desync near water or a dead target behind an ally as a click miss until checking these semantics. Regression anchors: Easy Rift Walkers run `20260510_213059_819`, Archive Safeguard Valley turn 3 Combat E6→E5 crossed E4 water and killed Arti at E3; Archive Storage Vaults turn 4 Combat F7→G7 killed Blob1 and bumped Arti at H7; fixed in simulator v98.
100. **Final Cave danger kills flyers.** `Mission_Final_Cave` uses `Env_Final` falling-rock/tentacle death effects, not ordinary Cataclysm/Seismic chasm conversion. Even Prospero/flying mechs die on marked tiles. If `environment_danger_v2` shows `Mission_Final_Cave` with `flying_immune=1`, treat the bridge as stale/wrong and fix the bridge/Rust hardening before advancing. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Final Cave turn 1 Combat E6→C6 was predicted safe with Prospero but died when C6 collapsed; fixed in simulator v99.
101. **Mission-end commits must push the current branch, never hard-coded main.** `mission_end` is bookkeeping for the active live branch; if its helper ever targets `origin main` or another unexpected branch, run it with `--no-commit`, then stage/commit/push the intended artifacts manually on the current branch. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, where Final Cave loss logging was correctly committed to `codex` manually because the helper still used `origin main`; fixed to push `origin HEAD`.
102. **Timeline Lost pilot carry-forward is click/select UI, not drag setup.** On the post-loss Time Traveler screen, choose the carry-forward pilot by clicking/selecting the visible pilot/card controls and then continue to setup; do not drag pilots on this screen unless the game shows a specific drag target. Once the hangar/setup screen is visible, verify carried pilot, squad, difficulty, Advanced Edition, and target setup before pressing Start. Regression anchor: Easy Rift Walkers run `20260510_213059_819` post-loss cleanup.
103. **Stable / guarding units must come from the live bridge, not static pawn stats.** Bosses and mission units can be normal-pushable in `pawn_stats.py` but currently Stable in the engine (`pawn:IsGuarding()`), which blocks push and teleport effects. The Lua bridge must export live `pushable=false` when `IsGuarding()` is true, Python `Board.from_bridge_data` must preserve that live override, and Rust should treat the payload as authoritative. Regression anchor: Easy Rift Walkers run `20260510_213059_819`, Final Cave turn 2: Taurus hit the B6 FireflyBoss for damage but the Stable boss did not move to A6; fixed in simulator v100.
104. **Final-turn fire still kills mechs before rewards.** If a safety check predicts `mech_lost` because a 1-HP mech is on Fire, trust the warning even when the UI says `Victory in 1 turn`; do not assume mission end will beat the next fire tick. Try Reset Turn immediately before spending any more actions. If Reset Turn is unavailable, prefer any line that extinguishes/repairs the burning mech over optional objectives or pod greed; otherwise treat the line as a real pilot/mech loss before consenting. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Mission_Terraform / Razor Shore turn 4: JetMech at F3 was KIA by final-turn Fire after a safety-blocked partial re-solve predicted `mech_lost`.
105. **Active mission allies must be audited before squad actions cascade.** Controllable mission units such as Terraformer can be `team=Player`, `active=true`, `mech=false`, and still have a weapon. If a fresh read shows an active mission ally but the solve output lists only squad mech actions, stop and inspect before executing the squad line; the omitted ally action may be required to prevent a later safety block. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Mission_Terraform / Razor Shore turn 4: Terraformer at E3 was active and could kill the E4 Hornet, but the solver output omitted it, leading to a dirty partial re-solve after Rocket/Jet acted.
106. **Post-pod pilot assignment is drag-and-drop.** On the island map/hangar screen, a newly recovered pilot in Storage must be dragged from the storage portrait onto the empty mech pilot bay; clicking the portrait only opens the pilot tooltip and does not assign them. Regression anchor: Hard Rusting Hulks run `20260512_104120_903` after Razor Shore pod: Kai Miller in Storage needed to be dragged into JetMech's empty pilot slot after Morgan Lejeune died.
107. **Mission_Wind markers are not direct damage.** Wind Storm rows from `Mission_Wind` indicate a push effect, not 1 HP environment damage to buildings. If a solve/safety block predicts grid loss only because `env_type="wind"` rows overlap buildings, first check whether the bridge exported live `WindDir`; without a direction, keep those rows as non-damaging markers rather than letting `environment_danger_v2` drain grid. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Disintegration Flats turn 1: wind-marked F7/C7 buildings produced phantom grid loss until simulator v102 separated `env_wind` from `env_danger`.
108. **Status-only fuzzy desyncs must not cage weapons.** Repeated `status.*` diffs, especially `boosted` / active-state drift after bridge move/skip/attack verification, are too broad to prove a weapon semantic bug. Narrate and re-solve, but do not soft-disable Jet/Rocket/Pulse weapons on status-only evidence; if a safety block appears with soft-disables active, run the no-soft-disable emergency pass before asking for dirty consent. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Disintegration Flats turn 3: false `Brute_Jetmech` / `Ranged_Rocket` disables hid a clean line and made the solver report forced grid loss.
109. **Replay snapshots must preserve Boosted status.** If `auto_turn` repeatedly reports `status.boosted` diffs or skip `click_miss` diffs that are just `active true->false` plus `status.boosted false->true`, check Rust replay snapshot parity before treating it as an execution miss or asking for repeated dirty consent. `rust_solver/src/replay.rs::capture_snapshot` must include `status.boosted`, matching Python `verify.py` snapshots and live bridge `boosted=true` from Kai/Morgan. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Restricted Area turns 1-3, simulator v103.
110. **Kai Miller Boost is state-based, not one-shot.** `Pilot_Arrogant` must be modeled as Boosted whenever Kai's mech is at full HP. Repairing from damaged to full HP re-enables `status.boosted`, attacking while still full HP leaves it enabled, and taking damage below full HP clears it. If a repair or attack on Kai yields `status.boosted predicted false actual true`, check the Rust `PilotFlags::ARROGANT` path before treating it as a click miss or asking for another dirty-plan consent. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Corporate HQ turn 4 Jet repair at E1, simulator v104.
111. **Arachnid Psion death eggs appear during player actions.** `Jelly_Spider1` makes every other non-minor Vek death create a live `SpiderlingEgg1` on the death tile immediately, before the next mech acts. Player-phase replay snapshots and partial re-solves must materialize that egg with the engine-style next pawn UID; enemy-phase deaths still defer egg placement until after the hatch loop so fresh eggs do not hatch in the same enemy phase. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Corporate HQ turn 4: Rocket D8 hit Firefly at D4, the corpse bumped Arachnid Psion at D3, and live spawned `SpiderlingEgg1` uid 84 at D4; fixed in simulator v105.
112. **Mission_Belt conveyors fire before Vek attacks.** Conveyor tiles push live units one tile before queued enemy attacks resolve, and moved Vek re-aim from their conveyor-shifted tile while preserving the original queued direction. Save/bridge conveyor parsers must keep `loc` and `custom="conveyorN.png"` on the same serialized map row; cross-entry regexes can create phantom belts on buildings and miss the real belt that moves the attacker. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Detritus Venting Fields turn 1: the F3 Moth on bridge `(5,5)` rode conveyor3 to F4 `(4,5)` before firing and destroyed C7 `(1,5)`; fixed in simulator v106.
113. **Conveyors are tile-driven, not mission-id-driven.** Detritus conveyor tiles can appear outside `Mission_Belt`, including `Mission_Acid`; if any live tile has `conveyor >= 0`, the enemy phase must apply the belt tick before Vek attacks. Do not gate conveyor simulation solely on `mission_id == "Mission_Belt"`. Regression anchor: Hard Rusting Hulks run `20260512_104120_903`, Chemical Field A turn 2 had live conveyors in `Mission_Acid`; generalized in simulator v107.
114. **Repair platforms do not overheal full-health units.** `Item_Repair_Mine` still consumes and increments `repair_platforms_used` when a full or already-overcapped unit lands on it, but HP stays unchanged unless the unit was below max HP at trigger time. Damaged units can still overheal up to `max_hp + 2`. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Forgotten Hills turn 2 PulseMech moved to `G4` at `3/3`; live consumed the platform and counted progress but left Pulse at `3/3` instead of predicted `5/3`; fixed in simulator v108.
115. **Delayed grid scalars are pending debt, not Grid Defense luck.** The bridge can expose building HP reductions before `grid_power` catches up at the turn boundary or after player-phase building damage. Before safety-auditing a solve or partial re-solve, compare the current building HP total with the first resist-probe entry for that run/region/turn; any building HP loss not yet reflected in `grid_power` must be treated as pending grid debt. After End Turn, when a fresh `combat_player` board looks better than the saved post-enemy prediction, poll briefly until the grid/building/unit fingerprint settles instead of recording a false favorable read. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Forgotten Hills turn 2: Rocket wounded `F4`, the final Pulse-only re-solve saw stale `5/7` grid, then Scorpion destroyed `F4` and Firefly damaged `D5`; the true settled outcome was `2/7`, not the predicted `3/7`.
116. **Safety widening keeps active weights and soft-disable masks.** Emergency top-k widening is only a wider view of the same live tactical assumptions; it must preserve the active `eval_weights` and current `disabled_actions`. False-positive weapon cages should be diagnosed from desync evidence, not bypassed during safety widening, because a no-mask pass can hide the consequence of a real semantic desync. Dirty-frontier summaries must split by the full loss signature and magnitude, so a `grid_power -1` option remains visible beside a `grid_power -2` option.
117. **Do not let Region Secured snapshots stall the live run.** On reward / Region Secured screens, `game_loop.py snapshot <label>` can hang while bridge/save state is in a transition. If a snapshot produces no output within roughly 20 seconds, terminate that snapshot process, note it in the run log later, click through the visible reward/continue flow, and use `mission_end ... --no-commit` or a later stable snapshot once the island map/save phase has settled. Do not run another session-touching `game_loop.py` command until the hung snapshot process is gone.
118. **Leap attacks resolve landing effects and break web.** If Aerial Bombs, Bombing Run, Leap, or another Leap-style attack moves the firing unit to a new tile, that tile change breaks the unit's own web and then resolves landing effects: fire/ACID pickup, mines, repair platforms, teleporter pads, and WebbEgg adjacency. A webbed JetMech can still fire Aerial Bombs; if it lands on an already-burning tile at 1 HP, it will catch Fire and die at the next enemy-phase fire tick unless repaired or shielded. Burning Forest landing tiles are consumed to burning Ground. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Archival Flats turn 3, Jet landed on burning `F2` after Rocket ignited the forest; fixed in simulator v109.
119. **Predicted mech status debt is tactical debt.** If a solve predicts a mech will newly become ACIDed, burning, frozen, disabled, or otherwise status-impaired after the enemy phase, treat it as a safety-blocking dirty plan unless explicit dirty consent is given for that exact line. Do not downgrade a predicted two-HP / status swing to a casual warning just because grid and buildings are clean; on Hard it can strand the next turn into an unavoidable grid-or-pilot sacrifice. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Countdown Ridge turn 1 Rocket moved through `C3` before the enemy phase and emerged ACIDed at `B3` on turn 2, leaving no clean turn-2 candidate.
120. **Aerial Bombs occupied transit smoke is not generic terrain damage.** When JetMech flies over a live unit, the engine applies transit unit damage, and the bridge workaround then applies only smoke/status on that tile. Do not run the generic terrain damage path for that occupied transit tile: sand under a shielded Pulse stays Sand with smoke, while the shield is still consumed; bridge smoke-only occupied transit should use direct `Board:SetSmoke` rather than `SpaceDamage(0)` so the post-fire UI does not refresh through a bad weapon context. Empty transit terrain and explicit self-damage rules remain separate; Ramming Engines recoil on sand still consumes sand per rule 82. Regression anchors: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 2, JetMech `E3 -> C3` over shielded Pulse on sandy `D3`; Mission_Holes turn 3, JetMech `D5 -> D3` over Hornet at `D4` hit a visible `Brute_Jetmech::GetTargetArea` crash.
121. **Non-unique multi-HP bump damage can carry delayed grid debt.** A bump into a non-unique 2-HP building can reduce HP while the visible grid scalar stays unchanged during player-phase verification, but the debt must be charged no later than enemy-turn start even if that building survives; if the same building is destroyed before then, charge and clear the debt immediately. Do not mark every such building unique just to force immediate grid loss: action verification may correctly see HP damage with no instant grid drop. Carry per-tile deferred bump debt through the simulated line and flush it at the enemy-turn boundary. Regression anchors: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 2, Pulse Repulse at `E4` bumped a Hornet into the `C4` building (HP 2→1, grid stayed 7), then enemy-phase destruction settled true grid at 2 instead of predicted 3; fixed in simulator v111. Hard Rusting Hulks run `20260513_144310_771`, Mission_Holes turn 2, Rocket bumped `B4` 2→1, grid stayed 4 during player phase, then became 3 next player turn while `B4` survived; fixed in simulator v114.
122. **Reviewed dirty candidates may need exact-rank execution after partial recovery.** If a bridge timeout/stale heartbeat leaves the board mid-turn, recover with `read` + `solve` from the actual board. When the new top raw dirty candidate is strategically worse than another dirty-frontier class (for example mech death vs. one grid loss), do not hand-edit session state or call bridge sub-action commands directly. Use `game_loop.py solve --candidate-rank <N>` / `auto_turn --candidate-rank <N> --allow-dirty-plan` only after the specific candidate rank and loss profile have been reviewed and explicitly accepted. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 3, Jet moved to `D5`, Aerial Bombs attack hit a stale bridge heartbeat, and the partial re-solve ranked a mech-loss candidate above the strategically preferable grid-loss rank 3.
123. **Bridge-fired Aerial Bombs must not depend on global `Pawn` target-area context.** Vanilla `Brute_Jetmech:GetTargetArea` reads `Pawn:GetPathProf()`, but bridge `pawn:FireWeapon()` can leave the post-action UI refresh without a valid global `Pawn`, crashing the game after a legal attack. Keep the bridge-installed Aerial Bombs family target-area wrapper nil-safe: prefer `Pawn:GetPathProf()`, then `Board:GetPawn(point):GetPathProf()`, then `PATH_FLYER`. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, Mission_Holes turn 3, Jet fired from `D5` to `D3` and the visible error screen reported `Brute_Jetmech::GetTargetArea`.
124. **Scarab Leader artillery has adjacent push splash.** `ScarabBoss` / `ScarabAtkB` is not merely an Alpha Scarab with higher damage: Expectorating Glands deals 4 artillery damage to the target tile, then queues zero-damage outward pushes on all four cardinal-adjacent tiles. Model the boss as Massive, ranged, 3-move, default weapon `ScarabAtkB`, and preserve the adjacent push splash when evaluating Corporate HQ tower/building lines. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, R.S.T. Corporate HQ turn 1 research gate with Scarab Leader at `C2` targeting `C6`; fixed in simulator v112.
125. **Synthetic no-belt boards must use `conveyor_dir=-1`.** `Tile::default()` is used heavily by focused simulator tests and ad hoc replay fixtures. Its `conveyor_dir` must mirror the bridge's no-conveyor sentinel (`-1`), not Rust's numeric default `0`; otherwise every default tile behaves like a belt and enemy attacks re-aim before the tested weapon resolves. Regression anchor: the first Scarab Leader v112 test shifted the attacker before firing and hid the actual artillery behavior until `Tile::default()` was fixed.
126. **Timeline-collapse plans are not dirty-consent plans except the last-turn final-cave resist Hail Mary.** `--allow-dirty-plan` can accept reviewed grid/building loss only while predicted grid remains above 0. If `plan_safety` includes `grid_timeline_collapse` (`predicted.grid_power <= 0`), stop unless an explicit debug bypass is set; do not spend a live Hard Victory run on a plan the simulator already says reaches Timeline Lost. Exception: on `Mission_Final_Cave` turn `total_turns`, if the Renfield Bomb is predicted alive, no deterministic grid-positive line exists after deep search, the best line minimizes pylon loss, and the only remaining win path is grid-defense resistance, an explicit user instruction may authorize the exact best resist-gamble line. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, final cave turn 4; rank-493 line `Jet A3→A6/A4`, `Rocket F3→F1`, `Pulse E1→D5/D5` predicted grid `4→0` but reached the Victory screen.
127. **Post-enemy audits are a persistent turn gate.** On `Mission_Final` / `Mission_Final_Cave`, if the next player turn exists but the previous turn has no `mNN_turn_TT_post_enemy.json`, backfill it from the fresh bridge board before solving. If any post-enemy audit is missing or worse than prediction in grid, building HP, buildings alive, pylons, or protected objective state, persist `session.post_enemy_block`. While that block exists, `read`/`status` reports it and `solve`, `auto_turn`, `execute`, `click_action`, `click_end_turn`, and `end_turn` refuse to advance. Clear only after understanding the miss with `python3 game_loop.py resolve_post_enemy_block --reason "<specific cause/fix>"`, or use the explicit debug env bypass outside live Hard attempts. Regression anchor: Hard Rusting Hulks run `20260512_181719_119`, surface turn 1 predicted `6/7` but actual reached `4/7` after Hornet2 hit `C3`; missing `m11_turn_01_post_enemy.json` let the miss escape.
128. **Pre-End-Turn threat coverage audit can hold the click.** After bridge-executed mech actions and before dispatching the End Turn click, record a threat audit for every building/pylon threatened at solve time, including projected WebbEgg / SpiderlingEgg hatch destinations. The audit should name the cancellation reason (`attacker_killed`, `attacker_smoked`, `attacker_moved`, `retargeted_nonbuilding`, `target_no_longer_building`, `still_threatened`, `still_threatened_hatch`, `attacker_transformed`, or `hatch_retargeted`) in A1-H8 terms. On low grid, HQ/final missions, or already-dirty safety states, `still_threatened*` returns `THREAT_AUDIT_BLOCKED` with a held End Turn batch and no bridge `END_TURN` side effect; inspect before clicking. Exception: the final-cave emergency pylon-loss policy from rule 30 may allow a still-threatened pylon count no greater than the already-predicted pylon-loss count, after the exact dirty line has been reviewed and all other emergency guards hold.
129. **Hard Rusting Hulks should prefer a third island unless Hive-ready.** For Hard Victory with Rusting Hulks, a 2-island Volcanic Hive attempt is acceptable only when grid is full or near-full *and* the kit is final-ready: Jet has range/control enough to cover stacked threats, Rocket has meaningful damage/range, Pulse has powered Repulse, and pilots/mech HP are stable. `new_run` stamps Hard Rusting Hulks attempts with `Hard Victory` / `hard_victory` metadata so Hard-only gates fire. `6/7` grid alone is not readiness; if Jet and Rocket are still base kit, take island 3 for cores/grid/control before the Hive.
130. **Mission preview clicks must target the intended region or visible Start Mission text.** On the island map, mission preview cards, board previews, and CEO dialogue overlays can overlap other regions; a stray click inside the preview or text overlay can select the underlying mission instead of starting the intended one. After a region is chosen, use the CEO portrait or another non-map edge area to dismiss briefing text when needed; do not click the text strip itself, because the dismiss click can pass through to an adjacent region after the dialogue clears. Then click only the visible **Start Mission** label on that region's preview board. If the selected mission changes, reselect the intended red region and reconfirm its title/objectives before starting.
131. **WebbEgg hatch uses `sPawn` fallback, not in-place mutation.** `WebeggHatch1` queues `SpaceDamage(p1)` with `sPawn="Spiderling1"` at the occupied egg tile; the engine may place the new Spiderling on an adjacent fallback tile and can destroy a building there for its full HP. If a post-enemy audit shows a missing adjacent building after an egg hatches, first check the hatch destination before blaming the egg's queued self-target or another Vek. Regression anchor: Hard Rusting Hulks run `20260513_144310_771`, Mission_FireflyBoss turn 2, WebbEgg at E6 hatched onto F6 and destroyed a 2-HP building, fixed in simulator v115.
132. **Mission_Wind is not dirty-consent-safe until WindDir is simulated.** Wind Storm rows are push lanes, not direct damage tiles: the live mission script applies `SpaceDamage(... iPush = WindDir)` across marked rows before enemy attacks. As of simulator v115 the bridge exports `env_type="wind"` and wind rows, but not live `WindDir`, and Rust stores them in `board.env_wind` without applying the push. Do not trust clean/dirty candidates on Mission_Wind when a mech, Vek, or building-impacting push chain sits on a wind row; resolve any post-enemy miss as unmodeled wind push, snapshot, and either play manually with visible wind arrows or stop for a WindDir bridge + Rust simulator fix. Regression anchor: Hard Rusting Hulks run `20260513_213619_103`, Mission_Wind turn 1, rank-114 dirty line predicted grid `5/7` and all mechs alive, but live WindDir push left grid `3/7` and destroyed RocketMech.
133. **Aerial Bombs transit-smoke is a threat answer.** `rust_solver/src/solver.rs` now scores Aerial Bombs actions that smoke a live attacking Vek on a transit tile, with extra weight when that Vek's queued target is a building, so the line survives the four-active-unit pruning cap. If a future safety block repeats this shape, first check whether the installed wheel includes the fix and whether `solver::top_k_tests::aerial_bombs_transit_smoke_building_threat_survives_pruning` still passes before falling back to manual inspection. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Forgotten Hills turn 4: `JetMech E6 -> B5, fire Aerial Bombs at B3` smokes `B4` and cancels the Bouncer's `A4` building attack while Archive/Rocket/Pulse handle the other threats.
134. **Landing on smoke extinguishes carried unit fire.** The simulator's landing pipeline clears a unit's existing Fire status when it lands on a smoky tile, before any fire-tile pickup. This matters for Aerial Bombs and other movement/teleport effects that land a burning unit onto pre-existing smoke. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Mission_Airstrike turn 2, Jet used Aerial Bombs from H3 to already-smoked F3; live cleared Jet fire, fixed in simulator v116.
135. **Reward-screen KIA / failed-objective text is authoritative.** If `read` returns `phase="unknown"` or another transitional value after End Turn, but the visible reward screen says **Killed in Action**, failed objective, Region Secured, or similar, trust the visible reward screen. Snapshot immediately, stop before **Continue**, and investigate any mismatch against the solver's predicted post-enemy outcome; do not treat a missing post-enemy trigger as a clean match. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Mission_Airstrike turn 4, RocketMech was visibly KIA on Region Secured while the solver predicted all mechs alive.
136. **Beetle charges push, and Bouncer edge recoil is harmless.** `BeetleAtk1` / `BeetleAtk2` are Charge attacks with forward target push; the target can be shoved into a later Vek's queued tile before that Vek attacks. Bouncer self-recoil into the board edge should not apply off-board bump damage to the Bouncer, but the horn hit still resolves. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Mission_Airstrike turn 4: Beetle shoved Rocket from B3 to B4, then Alpha Bouncer at A4 hit B4 and pushed the KIA Rocket to C4; fixed in simulator v117.
137. **Unit-based bonus objectives must be declared in `data/mission_unit_objectives.json`.** If a mission asks to defend/destroy a pawn unit rather than a building tile, ensure the mission id maps that pawn type into `protect_objective_unit_types` / `destroy_objective_unit_types` before trusting clean safety. `Mission_Bomb` protects `ProtoBomb`; train missions protect `Train` units; `Mission_Terraform` protects `Terraformer`. If `plan_safety.current.protected_objective_units_alive` is `0` while the visible objectives mention bombs, trains, robots, tanks, terraformers, or another pawn target, stop and add the metadata/test before continuing. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, R.S.T. Kern's Folly lost one `ProtoBomb` while safety compared `protected_objective_units_alive: 0`.
138. **Mission_Terraform Terraformer is a live fourth actor.** `Terraformer_Attack` is a controllable mission-ally weapon: click one adjacent direction tile and the engine erases a 3x2 rectangle in front of E3-style Terraformers, killing units and converting non-mountain terrain to sand. If a `Mission_Terraform` read shows `Terraformer` active but Rust solve permutations equal only the three squad mechs (for example `6/6` instead of `24/24`), stop before dirty consent and check the Rust `WId` mapping/simulator path. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Sierra Quadrant turn 1 safety-blocked until `Terraformer_Attack` was modeled.
139. **Mission_Terraform grassland is a custom-sprite objective, not terrain id.** The "Terraform the grassland back to desert" bonus tracks `ground_grass.png` custom tiles in the save, while the bridge terrain id can still say ordinary `ground`, `water`, or `sand`. At a fresh player-turn boundary, overlay active-region custom grass from saveData into `tile.grass`; do not overlay it mid-turn because saveData is stale after bridge actions. Rust must score remaining grass on `Mission_Terraform`, and `Terraformer_Attack` must clear the grass flag when it converts a non-mountain tile to sand. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Sierra Quadrant reward screen showed `Transform the grassland back to desert (Failed)` even though grid, pod, and Terraformer protection were clean.
140. **Boss leader kills are unit-objective metadata, not just generic boss flavor.** Corporate HQ boss missions must map their leader pawn into `destroy_objective_unit_types` in `data/mission_unit_objectives.json`; the generic `boss_killed_bonus` is turn-scaled and can become too weak in late turns. If a Region Secured screen says `Destroy the <Leader> (Failed)`, check the solve payload for an empty `destroy_objective_unit_types` list before clicking Continue. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, R.S.T. Corporate HQ ended at 7/7 with the Tower protected but `Destroy the Spider Leader (Failed)` because `Mission_SpiderBoss` had no explicit `SpiderBoss` destroy objective.

141. **Tumblebug live Lua ids are `Dung*`.** The shipped AE scripts define Tumblebugs as `Dung1` / `Dung2` with `DungAtk1` / `DungAtk2`, while local wiki-derived metadata and Rust weapon names use Tumblebug labels. Keep the live ids in `data/known_types.json`, `src/model/pawn_stats.py`, `src/model/weapons.py`, and Rust `wid_from_str` / `enemy_weapon_for_type` as aliases to the existing Tumblebug model before resuming from a `Dung2` research gate. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Pinnacle District Z-1001 turn 1 gated on `Dung2` / `DungAtk2` even though `Tumblebug2` / `TumblebugAtk2` were already modeled.

142. **Tumblebug boulders are neutral explosive pawns.** `DungAtk*` spawns `BombRock` / Unstable Boulder as a 1 HP, Move 0, neutral pawn with the engine `Explodes` flag. Destroying it deals 1 bump-class damage to cardinal-adjacent tiles; the Tumblebug's own queued detonation excludes its source tile, matching live targeted tiles. Keep `BombRock` in known types and pawn stats, and make Rust simulate the explosion before clearing a `BombRock` research gate. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Pinnacle Shielded Vaults turn 1 gated on a live `BombRock` at F5.

143. **Mission-start save lag is not post-mission stale bridge.** Right after confirming deployment, `saveData.lua` can still parse as `between_missions` while the fresh Lua bridge is already in `combat_enemy` / `combat_player` for turn 0 or turn 1 of the new mission. If the bridge says `in_active_mission=true`, has a `mission_id`, and the combat turn is 0/1, trust the bridge instead of the save phase. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Pinnacle Shielded Vaults opened with `Mission_SnowStorm` turn 1 visible while `cmd_read` incorrectly ignored the bridge as stale.

144. **Soft-disable safety widening must show the emergency pass.** After a safety block with `session.disabled_actions` active, `solve` / `auto_turn` output should include a `safety_widening` entry with `ignored_soft_disables=true` before asking for dirty consent. If it does not, fix the widening path first; otherwise a temporary weapon penalty can hide the best reviewed line. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Pinnacle Lifeless Basin turn 2 initially widened only `top_k_safety` while Rocket Artillery was soft-disabled from a Digger death desync.

145. **Effective mech max HP comes from saveData when bridge and save disagree.** The live bridge can report base pawn max HP after a repair/action while `saveData.lua` still carries the effective `pawn*.max_health` from pilot perks and powered +Health upgrades. Overlay save-backed `max_health` before Board construction and Rust solve/verify payloads, but do not overlay save `health` mid-turn because it can lag after bridge actions. When an overlay fires, surface `pilot_calibration_requests` so the harness can capture the visible pilot panel later and map perk text into solver metadata. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, Corporate HQ turn 3, Jet at D5 with Michael Lee `+2 Mech HP` verified as `3/2` instead of effective `3/4`, and Pulse at C6 had a +Health core making bridge `4/3` vs save max `5`.

146. **`auto_turn` safety blocks must carry the full dirty frontier.** If `cmd_solve` finds no clean line, the `SAFETY_BLOCKED` payload from `auto_turn` must preserve `candidate_count`, `selected_candidate_rank`, `selected_candidate_source`, `dirty_frontier`, `lookahead_frontier`, `robust_frontier`, and `safety_widening` from the solve record. Dirty consent IDs must use the selected candidate rank, not the requested rank or `None`, so exact-rank Hail Marys can be reviewed and rerun without token drift. Regression anchor: Hard Rusting Hulks run `20260513_230944_542`, final cave turn 4; the rank-493 `Jet A3→A6/A4`, `Rocket F3→F1`, `Pulse E1→D5/D5` resist line was visible in `solve` but hidden by `auto_turn`'s first safety block.

147. **Time Pods are fragile under non-collecting contact.** A player mech landing on a pod collects it, but an enemy or non-mech unit landing on a pod destroys it without `pods_collected` credit; direct tile damage also destroys it. Safety should then see `pods_present` drop with `pods_collected=0` and block as `pod_lost`. Do not treat a disappeared pod as recovered unless reward/save state says recovered (`pod == 3`); save `pod == 2` is failed/destroyed. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive The Pasture turn 2, Taurus pushed a Bouncer onto F3 and the reward screen showed `Protect the Time Pod (Failed)` while simulator v121 kept `has_pod=true`; fixed in simulator v122.

148. **Mission-panel clicks can commit the selected mission.** After a mission detail panel is open on the island map, do not click through the visible map to inspect another region; a click in the panel/preview area can advance into deployment for the already selected mission. To compare missions, dismiss/back out to the plain island map or rely on `recommend_mission` / screenshots before opening a panel. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, after The Pasture, an attempted Chronology Hall inspection from the Old Earth Park panel started Old Earth Park deployment instead.

149. **Mite objectives need active cleanup even on quiet turns.** On `Mission_Dam` / "Knock Mites off the Mechs", do not treat no-enemy turns as free skip turns while the objective counter is still incomplete; idle moves/attacks may be needed to knock mites off mechs before mission end. If `auto_turn` emits skip-only or mostly-skip plans with the mite objective still visible, snapshot and investigate objective modeling rather than assuming `plan_safety=CLEAN` protects reputation objectives. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Historic County reward screen showed `Knock Mites off the Mechs (Failed)` after T2/T3 solver skips while core safety stayed clean.

150. **Large Goo is a split-on-death boss, not a single kill.** `Mission_BlobBoss` / Large Goo (`BlobBoss`) splits immediately into 2 Medium Goos (`BlobBossMed`) on death; each Medium Goo splits into 2 Small Goos (`BlobBossSmall`), including when killed by chasm/instant-death terrain. The split search uses the Lua diamond around the death tile, primary radius 1-2 then backup radius 3-4, with deterministic representatives in Rust because the live game uses `random_removal`. Goo attacks are 4-damage queued squishes that persist after displacement, double-hit mountains, and move into non-mech target tiles when the squish clears them. If `auto_turn` wants to kill Large/Medium Goo and predicted post-attack does not show child Goos, stop and fix the simulator before continuing. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive HQ turn 1, Large Goo at B3; fixed in simulator v123.
151. **Moved queued attackers preserve their original target offset.** Save `piQueuedShot` / `piTarget` can remain expressed relative to `piOrigin` after a queued Vek moves, but the live attack target shifts by the same attacker displacement. Bridge/Python must normalize raw queued targets with `current_position + (piQueuedShot - piOrigin)`, and Rust enemy-phase replay must use the captured original position plus the current position rather than treating queued targets as fixed absolute tiles. Regression anchor: Easy Rift Walkers Ramming Speed run `20260516_120646_726`, Archive HQ turn 1, BlobBoss B3->B2 retargeted A3->A2 and Scarab G3->G2 retargeted G6->G5; fixed in simulator v124.

152. **Blitzkrieg Chain Attack setup should avoid protected-unit missions before Building Chain.** For `Chain Attack` runs, getting Lightning Mech's Building Chain powered is more important than taking a high-rep mission with fragile non-building objectives. Before Building Chain or Networked Shielding, avoid Train, Disposal Unit, Renfield Bomb, satellites, tanks, and other protected unit/object missions unless there is no alternative; prefer lower-rep city/building-heavy missions with no entity objective. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus `Mission_Train` turn 3, where a damaged train became a non-overridable `protected_objective_unit_lost` hard stop despite dirty consent.

152. **Bridge REPAIR must directly mutate HP/status.** In the bridge command context, `Board:AddEffect(Skill_Repair:GetSkillEffect(...))` can ACK as `[skill]` while leaving HP/status unchanged. The REPAIR handler should set mech HP directly, use zero-damage `SpaceDamage` only for Fire/ACID/Frozen/Injure cleanup, consume Boosted manually, preserve Kai Miller's full-HP state boost, and emulate Harold Schmidt's adjacent repair push. If repair verify shows `hp predicted +1/+2 actual unchanged` plus `status.boosted predicted false actual true`, patch/reinstall the bridge before treating it as a dirty tactical line. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, R.S.T. Perimeter turn 4, Tank repaired at E6 but live stayed `2 HP` and Boosted until bridge REPAIR stopped using `Board:AddEffect`.
153. **Bridge REPAIR max HP needs the same fallback as state export.** Some live pawn wrappers do not expose `GetMaxHealth()`, even though bridge state extraction already survives by falling back to `_G[pawn:GetType()].Health` and then current HP. The REPAIR handler must wrap `pawn:GetMaxHealth()` in `pcall` and use that same fallback before computing direct heal amounts; otherwise a tactical repair can crash after the mech moves, leaving a partial live turn that must be recovered by fresh `read` + `solve`. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Lone Mesa turn 3, Tank D3 repair crashed with `attempt to call method 'GetMaxHealth' (a nil value)`.
154. **Mission preview overlays can eat adjacent-region clicks.** After selecting an island-map region, the tilted preview board and large **Start Mission** text sit over nearby red regions; clicking what looks like another region through that overlay can immediately start the currently selected mission. To inspect a different mission, first click a clearly exposed non-start part of that red region outside the preview board, or use save/visual evidence and decide without cycling previews. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility preview started when a Scrapheap-looking click landed on the active Start Mission overlay.
155. **Mission_Disposal gives you a controllable Disintegrator ally.** The `Disposal_Unit` / A.C.I.D. Launcher is a READY friendly non-mech with `Disposal_Attack`; it cannot move, is not pushable, ignores smoke for firing, and must be included in solver actor generation when active. `Disposal_Attack` targets an artillery tile and instantly kills/acids the target plus the four cardinal adjacent tiles, clearing mountains to ground; omitting this weapon can leave preventable building threats such as D5 unresolved. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 1, Disposal_Unit at G7 was omitted until simulator v126.
156. **Boost Psion death clears visible Boosted immediately.** When the last `Jelly_Boost1` dies, all surviving Vek immediately lose the `Boosted` status flag as well as the +1 attack-damage aura. If verify shows only `status.boosted predicted true actual false` after a Boost Psion kill, patch `on_enemy_death` / simulator status cleanup rather than treating the weapon push as wrong. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 1, Tank F6 fired at F5, killed Jelly_Boost1 at F4, and live cleared Boosted from E4/F3/D4; fixed in simulator v127.
157. **Disposal_Attack dissolves buildings in its acid cross.** The A.C.I.D. Launcher Lua uses `DAMAGE_DEATH` on the target tile plus all four cardinal adjacent tiles, so buildings in that cross are destroyed just like units/mountains are affected; the solver must not treat city tiles as merely acidified. If a Disposal verify shows a building tile such as G4 becoming rubble after targeting an adjacent tile such as G3, patch `sim_disposal` / `apply_disposal_tile` before clicking the held End Turn. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 3, Disposal_Unit fired at G3 and live dissolved the adjacent 2-HP G4 building; fixed in simulator v128.
158. **Post-enemy audits wait for READY actors and delayed grid settlement.** The Lua bridge can report `phase=combat_player` before controllable actors are active and before delayed grid scalar updates settle; the raw bridge payload may also omit `active_mechs`. Post-enemy recording must count READY player actors from the parsed board and keep polling while that count is zero. If buildings/building HP are already worse than predicted but grid is not yet worse, keep polling to the settlement cap instead of treating two matching samples as stable. Otherwise a stale sample can record Turn 4 with DONE mechs and grid 5, then the same command later sees READY mechs and grid 3. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Detritus Teleport Facility turn 3→4 after Disposal destroyed G4; fixed in the post-enemy settle gate.
159. **Saved conveyor sprites are not always active belt environments.** Detritus maps can store `conveyor*.png` custom tiles even when the mission environment is `Env_Null`; those decorative sprites still matter for deployment routing, but they do not push units at enemy-phase start unless the live mission is `Mission_Belt` / `Mission_BeltRandom`. If a pre-End-Turn threat audit reports any `still_threatened*` building/pylon, hold the click on every mission except the explicit final-cave pylon-loss emergency allowance; do not downgrade the warning just because grid is 3 or higher. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Mission_Missiles Landfill turn 1, B5 Mosquito2 on a saved conveyor sprite still fired at B4 and destroyed a 2-HP building, dropping grid `3 -> 1`.
160. **Tumblebug Leader bridge ids are `DungBoss` / `DungAtkB`.** Live AE Corporate HQ can expose the visible **Tumblebug Leader** as pawn type `DungBoss` with weapon `DungAtkB`; treat it as a massive 3-move melee leader using the Alpha Tumblebug boulder-hit model (3 damage). The bridge already exposes spawned Unstable Boulders/BombRocks as units, so Rust only needs to map the boss attack id and apply the existing Tumblebug BombRock exclusion when it detonates the queued boulder. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Corporate HQ turn 1 research gate after selecting Tumblebug Leader.
161. **BombRock explosions are immediate under damage+push.** If Taurus Cannon, Rocket-style pushes, or another damage+push weapon kills a `BombRock`, the Unstable Boulder explodes from the hit tile before any corpse push; it does not continue onward as a dead pushable body. Adjacent BombRocks chain immediately and can damage buildings/mechs before the current action verifies. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_120646_726`, Corporate HQ turn 2: Tank C7 fired at C6, C6 BombRock exploded into Tank/D6/C5, then C5 exploded into D5; fixed in simulator v131.
162. **Weapon-ignited Forest becomes burning Ground immediately.** When direct weapon damage hits a Forest tile, including a unit standing in that Forest, the live engine consumes the Forest and leaves a burning Ground tile in the same action verification window. Do not model this as `terrain=forest, fire=true`; partial re-solves will false-desync on terrain after otherwise-correct kills. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Tumblebug Leader HQ turn 4, Punch B3→A2 killed Jelly_Boost1 on B2 and live reported B2 as burning Ground; fixed in simulator v132.

163. **Threat audit credits lethal pre-attack fire.** Enemy-phase fire ticks before Vek attacks; if a still-targeting attacker is on Fire, has 1 HP, lacks Shield/Frozen/fire immunity, and no Fire Psion is protecting it, the pre-End-Turn threat audit should mark the threat covered as `attacker_will_die_to_fire` rather than block as `still_threatened`. If this shape blocks again, first inspect the attacker's Fire/HP/shield/psion flags before resetting or accepting building loss. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, R.S.T. Corporate HQ turn 4, Leaper1 at F2 targeted G2 but was already 1 HP + Fire and the solver predicted it would burn before attacking.

164. **Off-axis Artemis bridge shots are no-ops.** Artemis Artillery looks directionless in the Rust weapon definition, but live `pawn:FireWeapon()` still uses a cardinal target area: off-axis bridge calls can return OK, spend the mech action, and leave the target/adjacent tiles unchanged. Do not rely on C6→E2/F2-style lobs for rescue geometry; keep player `WeaponType::Artillery` enumeration and replay validation cardinal-only unless a live UI capture proves a specific weapon differs. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, ArtiMech D6 fired Artemis at F2 and live Crab1 stayed 3 HP while E2 Firefly did not move; fixed in simulator v134.

165. **Pending-grid-debt probes must be mission-keyed, not just region/turn.** Regions can be reused across islands and missions within one run, so `resist_probe.jsonl` entries for `region1` turn 1 from an older mission must not seed pending grid debt for a later `region1` turn 1. Match run id, region, turn, mission id, mission slot, and master seed when available; otherwise safety summaries can show false grid baselines such as 7/7 visible grid becoming 4/7 before any live damage. Regression anchor: Easy Rift Walkers/Ramming Speed run `20260516_192519_866`, Detritus Nano Silos turn 1, stale `Mission_Tides` region1 turn1 probe inflated pending grid debt by 3 before the B5/C6/D5/D7 solve.

166. **Positive player HP drift is harmless but should not hide damage misses.** If per-action verify shows only player HP higher than predicted, with no enemy/tile/scalar diffs, treat it as a harmless HP-gain drift and continue from fresh bridge state; do not enqueue it as a tactical desync or soft-disable the weapon. Still flag any enemy HP being higher than predicted, player HP being lower than predicted, or mixed diffs. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus `Mission_Disposal` turn 3, ElectricMech Chain Whip killed the remaining Vek and live HP became `5/3` while the solver conservatively predicted `3/3`.

167. **ACID Storm acidifies newly spawned rocks immediately.** On `Mission_AcidStorm`, while the `Storm_Generator` objective is alive, a Rock Accelerator `RockThrown` spawned onto an empty tile appears with `acid=true` in the same action verification window. Apply the storm status to spawned neutral rocks; do not treat a single `RockThrown status.acid predicted false actual true` as a tactical desync once this sim rule is present. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus The Wasteland turn 1, RockartMech H6 fired at E6 and the live RockThrown spawned ACIDed.

168. **Repair does not clear ACID while ACID Storm is active.** On `Mission_AcidStorm`, while the `Storm_Generator` objective is alive, a mech Repair or Repair Drop should still heal/clear normal repair statuses but the unit remains `acid=true` in the same verification window. Do not soft-disable Repair or re-solve on a lone `status.acid predicted false actual true` for a repaired player unit after this sim rule is installed. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus The Wasteland turn 2, WallMech E3 repaired under the storm and stayed ACIDed.

169. **Disposal teleporter tiles can move mechs during action verification.** On Detritus disposal/teleporter missions, a solver move that lands on a teleporter tile can immediately relocate the mech and produce `mech_position_wrong` drift before the planned attack fires. Treat teleporter tiles as live hazards for movement planning and review any Rockart/Ranged move onto a teleporter before trusting the stored continuation; if the bridge has already resolved the teleport, fresh-read/re-solve from the actual board before End Turn. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Detritus Nanite Farms turn 1, RockartMech D6->D7 landed on a teleporter and re-solved from the actual position.

170. **Status checks are session commands too.** Treat `python3 game_loop.py status` like `read`, `solve`, `auto_turn`, and other session-touching commands: run it alone, never inside `multi_tool_use.parallel` with file reads, helper scripts, screenshots, or UI inspection. A quick status check can still take the active session lock and should stay auditable as its own step. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Archive Artifact Vaults turn 2, where a status check was accidentally bundled with a skill file read.
171. **Rock Launcher side pushes outrun center Boom Bot decay.** When Rock Launcher kills a `*_Boom` / Volatile center target, its perpendicular side pushes resolve before the killed center target's Explosive Decay splash. Adjacent side Boom Bots can be shoved out of the blast radius and survive, then act in the enemy phase; do not count them dead from pre-push adjacency. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_BoomBots` turn 1, RockartMech D6 fired at D4, the E4 Boom Tank survived by moving to F4 and later burned the F7 building; fixed in simulator v138.
172. **Boom Bot post-enemy grid misses can come from newly moved non-queued enemies.** `Mission_BoomBots` starts with frozen/unqueued Boom Bots and can leave surviving bots with non-building or empty targets after player actions. During enemy activity those units may move/re-aim and damage buildings that were not present in the pre-End-Turn threat audit. If a post-enemy block shows clean player-action verifies, unchanged building count, and only `grid_power/building_hp -1`, first trace the enemy-phase snapshots for a newly moved Snowtank/Snowlaser before looking for a weapon-definition desync. Clear only after recording the exact moved attacker and continue from the fresh board. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_BoomBots` turn 2, Snowtank moved from G6 to G5 and damaged G4 after the threat audit saw no live building target.
173. **After dirty-consent auto_turn output anomalies, read before executing anything manually.** If `auto_turn --allow-dirty-plan --dirty-consent-id ...` appears to print only the solve/safety block but the dirty token is consumed, do not immediately call `execute <index>` or `verify_action <index>`. First run `game_loop.py read` alone and compare mech active states/positions against the reviewed line; the bridge may already have executed the full plan while stdout missed the action section. A later `verify_action 0` against an already-after-action-2 board creates a false multi-diff desync. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 1, dirty pod-loss consent `0617a6ce7c11dc86` moved all three mechs before an extra `execute 0` caused a bogus per-action desync.

174. **Chain Whip is pawn/building-chain damage with narrow terrain side effects.** `Prime_Lightning` emits damage for pawns, and with `Prime_Lightning_A/AB` it treats live Grid Buildings as zero-damage chain nodes (`Buildings=true`) rather than damaging the building. Do not simulate Chain Whip as generic weapon tile damage: it must not crack Ice, break cracked Ice to Water, smoke Sand, or destroy Time Pods unless another effect actually touches those tiles. Live captures do show Forest under a zapped pawn igniting and applying Fire to that pawn, so model that specific terrain side effect. Regression anchors: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 2, Chain Whip hit `Starfish2` on cracked E4 ice and live left the tile as Ice while simulator v138 broke it to Water and falsely drowned the Starfish; same run/mission turn 4, Chain Whip hit WallMech on D6 Forest and live converted the tile to burning Ground with WallMech on Fire.

175. **Modeled upgraded weapon IDs must be known to the research gate.** When adding Rust/Python support for a `_A`, `_B`, or `_AB` weapon overlay, add the exact IDs to `data/known_types.json` in the same change. Otherwise the next live `auto_turn` can correctly overlay the weapon, then immediately stop with `RESEARCH_REQUIRED` for a weapon we already modeled. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 3, `Prime_Lightning_A` appeared after the Building Chain overlay and blocked solving until it was registered as known.

176. **Save `primary_power` is base weapon power, not upgrade suffix state.** Do not infer `_A` / `_B` / `_AB` weapon IDs from `primary_power` or `secondary_power`; those lists can merely indicate the base weapon is powered. Use the save's explicit weapon ID if it already has a suffix, or the upgrade mod groups (`primary_mod1` / `primary_mod2`, secondary equivalents) when they are fully powered. Regression anchor: Blitzkrieg Easy run `20260517_105759_344`, Pinnacle `Mission_FreezeMines` turn 4, `primary_power={1}` on base `Prime_Lightning` was mis-overlaid as `Prime_Lightning_A`, causing the solver to expect Building Chain through D7/E7 and falsely predict the F7 Bouncer death.

## Phase Protocols

Each phase: read → act → verify. Detailed command semantics are in **Game Loop Command Reference** below.

- **NEW_GAME_SETUP** (very start of a new run, before ISLAND_SELECT): Run `python3 game_loop.py achievements --sync` if Steam env keys are present, then `python3 game_loop.py recommend_squad --tags achievement` or initialize with `python3 game_loop.py new_run auto --tags achievement`. On the new-game screen, set the target difficulty (**Hard** / `--difficulty 2` for Hard Victory; **Easy** / default for ordinary squad achievements), ensure **Advanced Edition** content is toggled ON, select the recommended actual squad card, then **Start**. Use **Balanced Roll** only when `recommend_squad` / `new_run --mode solver_eval` / a random-squad achievement explicitly says to. See `feedback_playstyle.md`.
- **ISLAND_SELECT** (after the CEO intro): `python3 island_select.py` picks one of 4 corporations. Click its coord. Click through CEO intro. → ISLAND_MAP.
- **ISLAND_MAP:** `game_loop.py read` → pick mission (prefer bonus objectives for the achievement target) → click mission on map → mission briefing → click preview or start button → DEPLOYMENT. On the island map, red regions are the selectable frontier missions and green regions are already secured/completed; choose from red regions, not green ones, even if the bridge/save metadata is stale or sparse.
- **DEPLOYMENT:** `game_loop.py deploy_recommended` → verify PASS for all placed mechs → click visible CONFIRM. If bridge deployment is unavailable, use `game_loop.py read` MCP coords and click 3 recommended tiles manually, then CONFIRM. → COMBAT_PLAYER_TURN.
- **COMBAT_PLAYER_TURN:** `auto_turn --time-limit 10` → dispatch the emitted End Turn click plan or run `click_end_turn` → loop. The next `auto_turn` blocks in Python until the next player turn or mission end. On game_over or empty solution, falls back to screenshot-based reasoning.
- **MISSION_END:** screenshot reward screen → click reward → `snapshot <label>` → ISLAND_MAP (or ISLAND_COMPLETE if all missions done). If the screen visibly shows **Region Secured**, a promotion popup, a reward panel, or the island map while `auto_turn` / `read` keeps returning the previous `combat_player` / `combat_enemy` board from a later turn, treat the bridge as stale and follow this MISSION_END flow; do not keep polling or try another combat command. `cmd_read` cross-checks `saveData.lua` and will now ignore that stale combat bridge board once the save phase is `between_missions` or `mission_ending`; the Lua bridge also dumps `phase="unknown"` immediately on `MissionEnd` so future mission endings don't leave combat JSON behind.
- **SHOP:** appears *only after winning a whole island* (4 missions + finale), not mid-island. Do NOT expect a shop between missions — grid repair opportunities come from time-pod rewards and island completions, full stop. Screenshot when it appears (neither save nor bridge distinguish shop from map). **GRID-FIRST RULE: if Grid Power is below max, buy Grid Power until it reaches 7/7 before considering anything else.** If you accidentally buy a core/weapon/pilot while Grid Power is still below max, click **Undo All** immediately and repurchase Grid Power first. Verify the post-buy state from `game_loop.py status` or another reliable non-visual read before leaving with unspent reputation; do not rely on visually counting the bar pips. If reputation remains and grid is still below max, buy another Grid Power before cores/weapons or leaving the island. Once the grid is full, the same slot becomes Overpower / +Grid Defense; that is optional, not mandatory, unless the current achievement target explicitly wants Grid Defense or full-grid overpower purchases. With a full grid, shop normally: prefer reactor cores, solver-supported weapons/passives, useful pilots, and achievement-relevant buys. Rationale: `feedback_grid_buy_priority.md`. → ISLAND_MAP (next island) or RUN_END.
- **RUN_END:** `snapshot run_end`. If defeat, analyze critical turns from decision log. Check achievement progress. Start new run with next target.

### Error Recovery

- **Unexpected screen:** screenshot, log to decision log, diagnose visually.
- **State not updating:** `refresh_bridge_state`. If bridge dead, retry save-file verify up to 5× with 1.5s delay.
- **Solve/auto_turn stall:** inspect the latest `recordings/<run_id>/mNN_turn_NN_solve_input.json` first. That file captures the exact Rust payload before solving starts, so reproduce with `itb_solver.solve` / `solve_top_k` / `solve_beam` from it. Do not rely on the next `*_board.json` after a stall; live recovery may have already executed actions or advanced animations, making that board post-action or stale.
- **Grid power = 0:** log, snapshot, analyze.
- **Crash/timeout:** fresh `cmd_read` + `cmd_solve`. Never resume an old solution.

## Game Loop Command Reference

All commands are `game_loop.py <name> [args]`. Each is stateless: read state, compute, output, exit. Session state persists in `sessions/active_session.json`.

**State reading:**
- `read` — Bridge state (primary) or save file (fallback). Prints phase, board, threats, active mechs, deployment zone with MCP coords, env hazards. Auto-records to `recordings/<run_id>/`.
- `status` — Quick summary: turn, grid, mech HP, threats, objectives; also surfaces any persistent `post_enemy_block`.
- `verify_action <index>` — Per-action diff: refreshes bridge, diffs actual vs the predicted snapshot captured during `replay_solution`, classifies (click_miss / death / damage_amount / push_dir / grid_power / status / terrain / tile_status / repair_platform / spawn / pod), writes desync to `recordings/<run_id>/failure_db.jsonl`. Never re-solves.
- `verify [index]` — Legacy save-parser-based path (retries 5×, 1.5s each). Superseded by `verify_action` in bridge mode.

**Solving & recording:**
- `solve [--candidate-rank N]` — Runs the solver, stores solution in session, prints action sequence. Recording includes structured actions, per-action `ActionResult`, per-action `predicted_states` (input to `verify_action`), predicted post-enemy board, score breakdown, search stats. If the selected plan is safety-blocked, the JSON includes the exact `dirty_consent_id` needed for a reviewed dirty run.
- Recordings from `read`/`solve` land in `recordings/<run_id>/m<NN>_turn_<NN>_<label>.json`. Dedup guard prevents duplicate `(mission, turn)` pairs.

**Combat execution:**
- `auto_turn [--time-limit N] [--no-wait] [--max-wait S] [--candidate-rank N] [--allow-dirty-plan --dirty-consent-id ID]` — Full turn via bridge with per-sub-action verification. Polls at entry for `combat_player` phase **and** `active_mechs > 0` (up to `--max-wait` seconds, default 45; disable with `--no-wait`). Returns an MCP click plan for End Turn only after safety gates and the pre-End-Turn threat audit pass. On desync, re-solves from actual board with partial mech states (DONE = inactive, MID_ACTION = can_move=false, ACTIVE = full search).
- `click_action <i>` — Pure planner for manual play. Emits a `computer_batch`-ready sequence for ONE mech action (select-tile, optional move, weapon icon, target). Handles dash weapons (skip move click), Repair (click Repair button), passives (no-op).
- `click_end_turn` — Pure planner. Emits a single click on End Turn.
- `recommend_squad [squad|auto] [--achieve ...] [--mode achievement_hunt|solver_eval|random_squad|custom]` — Pure strategist. Uses `data/achievements_detailed.json` to choose a named squad for achievement hunting, or Balanced Roll for solver-eval/random-squad runs.
- `click_balanced_roll` — Pure planner. Emits a single click on the Balanced Roll button on the squad-select screen. Dispatch before clicking Start only when the setup recommendation says Balanced Roll.
- `deploy_recommended` — Bridge deployment helper. Sends `DEPLOY uid x y` for the ranked recommended tiles, verifies placed mech coordinates, and leaves only the visible CONFIRM click for Computer Use.
- `execute <index>` / `end_turn` — Bridge-mode action commands. Used internally by `auto_turn`. In manual play, use `click_action` / `click_end_turn` instead. These refuse to run while a persistent post-enemy block exists.
- `research_peek [--limit N]` — Read-only view of the pending research queue. Use this instead of the nonexistent `research_queue_peek` command.
- `resolve_post_enemy_block --reason "<specific cause/fix>"` — Clears a persistent post-enemy investigation block after the miss has been understood and recorded.

**Full-mission automation:**
- `auto_mission [--max-turns N]` — Full mission: auto-deploy → combat loop → mission end. Final turn is force-flushed to failure_db on exit. Falls back to the agent for reward/shop/map screens.

**Analysis & tuning:**
- `replay <run_id> <turn> [--time-limit N]` — Reconstruct Board from a recorded JSON and re-run the solver. Compares new solution with original.
- `analyze [--min-samples N]` — Read failure_db.jsonl, report patterns by trigger / severity / squad / island.
- `validate <old.json> <new.json> [--failures-only] [--time-limit N]` — Compare two weight versions across recorded boards.
- `tune [--iterations N] [--min-boards N] [--time-limit N]` — Auto-tune EvalWeights. Hybrid objective: `mean_fixed_score − 100 * fired_failure_count`.

**Run management:**
- `new_run [squad|auto] [--achieve X Y] [--difficulty N] [--mode achievement_hunt|solver_eval|random_squad|custom] [--tags audit ...]` — Initialize new session. Omit the squad or pass `auto` to let `recommend_squad` choose the achievement-aware setup. Use `--tags audit` / `--mode solver_eval` for environment-audit playthroughs (those failures stay out of the tuner corpus).
- `snapshot <label>` — Save current state for regression.
- `log <message>` — Append reasoning to the decision log.

## Achievement Context

Progress persists in session. Local checklist currently tracks 25/70 earned, 45 remaining across 4 difficulty tiers (Green >40%, Yellow 20–40%, Orange 10–20%, Red <10%).

- Squad-specific achievements need the matching squad — check `data/ref_achievement_strategies.md`.
- Cumulative achievements (reputation, civilians, pilot reuse) accrue across runs.
- Achievement strategies bias solver scoring via `EvalWeights` in `src/solver/evaluate.py`.
- Full checklist: `TODO.md`. Detailed metadata: `data/achievements_detailed.json`.

## Reference Material

Codebase layout, knowledge base tables, and data file inventory live in `docs/reference.md` — read that when you need to find where something is.
